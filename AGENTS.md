## Goal
Polish UX, complete database portability with auto-setup, and align documentation to real stack.

## Constraints & Preferences
- No SQL changes by the assistant; user runs all database modifications manually.
- Build must pass TypeScript and Vite build inside Docker container (`pnpm build`).
- Docker Vite dev server may show stale parse errors; `docker restart jadda_frontend` resolves them.
- Database auto-creates tables + seeds reference data on startup via `setup.js`.
- User uses MySQL Workbench; password prompts remain unresolved.
- `.gitignore` has `mysql_data/` in root and `.env` in backend/ + frontend/.
- Class-based FontAwesome icons (`<i className="fas fa-...">`) used across ~10 files; CSS import kept.
- `.env.example` should contain placeholders, never real credentials.
- Docs structure kept unchanged: `docs/RFs/`, `docs/HUs/`, `docs/RNFs/`.

## Progress
### Done
- RF-023 cancelar pedido COMPLETO + liberación de stock + RN-010 real (2026-08-13): `comprasController.cancelarCompra` reescrito con **transacción**: (1) SELECT con `FOR UPDATE` de VENTAS + ENVIOS (LEFT JOIN, ESTADO_ENVIO); (2) **RN-010 real**: si `ESTADO_ENVIO` ∈ (`EN_CAMINO`, `ENTREGADO`) → 400 "El pedido ya salió del almacén y no puede cancelarse" (antes solo miraba VENTAS.ESTADO y la UI ocultaba el botón, pero la API lo permitía); (3) **liberación de stock**: lee DETALLE_VENTAS con ID_VARIANTE NOT NULL y hace `STOCK + CANTIDAD` por variante + `registrarMovimientoStock(ENTRADA)` dentro de la transacción (rollback revierte todo); (4) UPDATE estados VENTAS/ENVIOS; commit; notificación tras el commit con try/catch (nunca bloquea). El reembolso sigue siendo semi-manual (botón → DEVOLUCIONES → admin aprueba en /admin/devoluciones) — documentado como tal en RF-023.md → **Implementado** (antes "parcial": no liberaba stock — inconsistencia creada por RF-029 que ya decrementa en checkout — y RN-010 no se validaba en backend). NOTA: ventas viejas (antes del 2026-08-13) nunca decrementaron stock, así que su cancelación reingresa unidades no descontadas (aceptado, verificado en RF-023.md). Verificado E2E por API con admin: checkout 2× variante 1 (stock 6→4, mov SALIDA 2) → cancelar (stock 4→6, mov ENTRADA 2, notificación creada); checkout 1× variante 1 → admin set EN_CAMINO → cancelar → 400 RN-010 → admin restaura PENDIENTE → cancelar 200 (stock restaurado). Venta 7 (demo, con devolución aprobada) quedó intacta.
- RF-029 inventario detallado COMPLETO + fix registro sin dirección (2026-08-13): (1) `backend/utils/movimientosStock.js` (nuevo) — helper `registrarMovimientoStock({conn?, idProducto, tipo, cantidad})` inserta en MOVIMIENTOS_STOCK (tabla ya existía, SIN cambios de esquema) con TIPO_MOVIMIENTO mayúsculas y FECHA CURDATE(); nunca lanza (no rompe checkout/edición/devolución). (2) **checkoutController.js**: el comentario "Pendiente de habilitar tras pruebas" queda atrás — dentro de la transacción, por cada item con ID_VARIANTE hace `UPDATE PRODUCTO_VARIANTES SET STOCK = STOCK - ?` + registra SALIDA; el rollback revierte ambos (verificado: stock variante 16 20→17 al comprar 3). (3) **productoController.js**: `actualizarVariante` ahora lee también ID_PRODUCTO en el SELECT previo y registra ENTRADA/SALIDA con la diferencia (`Math.abs`) si el stock cambió; `agregarVariante` registra ENTRADA si nace con stock > 0. (4) **devolucionController.js**: al APROBAR se registra ENTRADA con sol.CANTIDAD dentro de la transacción. (5) **Fix bug preexistente en registro** (authController.registro): `direccion.split(',')` explotaba con 500 SI el frontend no enviaba direccion tras haber insertado el usuario — ahora la fila DIRECCIONES solo se crea si `direccion` viene (el usuario ENTRADA quedaba huérfano+500 antes). Verificado E2E por API con admin: PUT variante 1 stock 6→9 (mov ENTRADA 3) → 9→7 (SALIDA 2) → restaurado 6 (SALIDA 1); checkout de 3 uds variante 16 (mov SALIDA 3) → devolución aprobada (mov ENTRADA 3, stock vuelve a 20); RF-029.md → Implementado con la RUTA REAL corregida (`PUT /api/productos/variantes/:idVariante`, el doc decía `/api/productos/:id/variantes/:idVariante` que NO existe). Datos de prueba dejados como demo: venta 7 (admin, 3× producto 2, Nequi), devolución 3 APROBADA, usuario `movtest.rf029@gmail.com` (sin confirmar). NOTA: cancelar pedido sigue SIN reingreso de stock (decisión RF-023).
- Newsletter automática + desuscripción + envío manual admin (2026-08-13): antes el newsletter SOLO guardaba el correo en `data/newsletter.json` — nunca se enviaba nada. Ahora (1) **scheduler** en `server.js` (setInterval cada `NEWSLETTER_INTERVAL_HORAS`, default 72 h, configurable en `.env`): `enviarNewsletterAhora()` consulta **ofertas reales** de la BD (6 productos con `ID_DESCUENTO` activo, `ORDER BY RAND()`, imagen embebida con `imagenComoDataUri`, precio tachado + final `es-CO` + chip `-X%`), elige **al azar** entre 4 plantillas (💪 ofertas / 🏆 mejor página deportiva / ⚽ novedades / 🔥 ofertas) con pitch de marca ("somos la mejor página para comprar artículos deportivos"), CTA al `?descuento=true` y pie con envío gratis $200.000 + link de desuscripción; nunca lanza (errores SMTP capturados correo a correo, delay 150 ms); historial en `data/newsletter-estado.json` (último envío + total acumulado). (2) **Desuscripción**: `POST /api/newsletter/desuscribir` (body) y `GET /api/newsletter/desuscribir?email=` (página HTML de confirmación servida por el propio backend — el link va en el correo); (3) **Admin**: `GET /api/newsletter/suscritos` (lista + estado de envíos) y `POST /api/newsletter/enviar` (dispara envío manual ahora) — ambos con `esAdmin`; (4) `frontend/vite.config.ts` ganó `preview.proxy` igual al `server.proxy` (el link de desuscripción `/api/newsletter/...` funciona en modo preview `MODE=preview`); (5) `backend/.env.example` creado con placeholders (incluye `NEWSLETTER_INTERVAL_HORAS`). RF-047.md actualizado (proceso con auto-envío + 5 endpoints). Sin cambios de esquema.
- "Ser vendedor" completo + fix timbre marca-1 (2026-08-09): (1) **Fix timbre de notificaciones**: el bug "abrir el timbre marcaba todo como leído" eran DOS bugs en `notificacionController.js` — (a) `filtroUsuario` generaba el where SIN paréntesis (`ID_USUARIO IS NULL OR ID_USUARIO = ?`) y al concatenar `AND LEIDA`/`AND ID_NOTIFICACION` la precedencia rompía el filtro; ahora `(ID_USUARIO IS NULL OR ID_USUARIO = ?)`; (b) `marcarLeida` pasaba los params INVERTIDOS: SQL `UPDATE ... WHERE ID_NOTIFICACION = ? AND ${where}` pero params `[...params, req.params.id]` → ahora `[req.params.id, ...params]` (el `?` de ID_NOTIFICACION es el 1º). Verificado en BD con notifs TIMBRE-TEST: marcar la #48 (global) como admin → solo #48 LEIDA=1, #47 y #49 quedan en 0; el contador vuelve a 1 al llegar notificaciones nuevas. (2) **Ser vendedor backend**: tablas `SOLICITUDES_VENDEDOR` (ID_SOLICITUD, ID_USUARIO, EMPRESA, NIT UNIQUE, REPRESENTANTE, EMAIL_EMPRESA UNIQUE, TELEFONO, DEPARTAMENTO, CIUDAD, DIRECCION, CATEGORIAS, DESCRIPCION, ESTADO PENDIENTE|APROBADA|RECHAZADA, OBSERVACION_ADMIN, FECHA_SOLICITUD, FECHA_PROCESADA) y `VENDEDORES` (ID_VENDEDOR PK auto, ID_SOLICITUD, ID_USUARIO, EMPRESA, NIT UNIQUE, EMAIL_EMPRESA UNIQUE, FECHA_APROBACION) en setup.js + rol 6 'Vendedor' — NOTA: el runner de MIGRACIONES no puede insertar el rol (chequea que la columna exista, no los datos) → `INSERT IGNORE INTO ROLES (6, 'Vendedor', ...)` dedicado en `migrarTablasExistentes` (verificado en BD). Columnas nuevas en USUARIOS: `DEBE_CAMBIAR_PASSWORD` (TINYINT default 0). (3) **vendedorController.js + vendedorRoutes.js** (montado en server.js `/api/vendedor`): `POST /solicitud` (usuario; validación: empresa 3-100, NIT 5-20 dígitos, email empresa formato, teléfono 7-10 dígitos, departamento/ciudad/dirección obligatorios; bloquea si ya existe solicitud PENDIENTE/APROBADA → 400 "Ya tienes una solicitud en revisión"; NIT/EMAIL_EMPRESA duplicados → 409; inserta con ESTADO PENDIENTE), `GET /solicitud` (mi solicitud + `vendedor` asociado si existe), `GET /admin` (esAdmin, lista con solicitante), `POST /admin/:id/procesar` (esAdmin, transacción con FOR UPDATE: APROBADA → crea USUARIO vendedor con EMAIL=email de empresa (404 si ya existe), USUARIO único `vendedor_<slug empresa>.<4dígitos>`, ID_ROL=6, CONFIRMADO=1, DEBE_CAMBIAR_PASSWORD=1, password temporal 10 chars bcrypt; inserta VENDEDORES; respuesta NO devuelve la contraseña; credenciales por correo vía plantillaCorreo (async, no bloquea); notificación in-app al solicitante ("¡Felicidades! Tu cuenta de vendedor está activa") y al nuevo vendedor; RECHAZADA → OBSERVACION_ADMIN + ESTADO RECHAZADA + notificación; RECHAZADA permite reenvío (misma fila → PENDIENTE); doble proceso → 400 "Esta solicitud ya fue procesada"; rateLimit 30/min). (4) **auth/seguridad**: login y obtenerPerfil devuelven `DEBE_CAMBIAR_PASSWORD` (0/1); `seguridadController.cambiarPassword` ahora hace `UPDATE USUARIOS SET CONTRASENA = ?, DEBE_CAMBIAR_PASSWORD = 0`. (5) **Admin**: `adminController.obtenerPendientes` suma `vendedores` (COUNT SOLICITUDES_VENDEDOR PENDIENTE); `obtenerDashboard` agrega `vendedoresPend`; `AdminVendedores.tsx` (`/admin/vendedores`, lazy AdminRoute, tab "Vendedores" en AdminNavbar con badge de pendientes): tabla con empresa/NIT/solicitante/fecha/badge estado, filtro por estado, modal detalle (datos completos + categorías), botones Aprobar (Swal confirmación) / Rechazar (Swal con input de observación); badges `.av-badge-*` en adminDashboard.css. (6) **Frontend usuario**: Navbar dropdown → item **"Ser vendedor"** (FaStore) entre "Planes" y "Ayuda y Soporte"; `SerVendedor.tsx` + `SerVendedor.css` (`/ser-vendedor`, lazy ProtectedRoute): banner con políticas de vendedor (Colombia — incluye "tu cuenta puede ser eliminada si incumples las políticas"), formulario completo con los 9 campos, checkbox obligatorio de aceptación, card de éxito "¡Solicitud enviada! Nuestro equipo verificará tu información en un máximo de 48 horas"; si ya hay solicitud → card de estado (APROBADA muestra "Cuenta de vendedor activa" + correo de acceso; RECHAZADA muestra banner rojo con la observación + formulario para reenviar). **AJUSTE POST-E2E (2026-08-09)**: `/ser-vendedor` YA NO usa ProtectedRoute (el usuario puede ver el formulario sin login; SerVendedor ya manejaba el 401 mostrando el formulario); el navbar muestra item **"VENDER"** en el menú (`.nav-item-vendedor`, al lado de OFERTAS) SOLO cuando NO hay sesión (`{!usuarioLogueado && ...}`), y con sesión se oculta del menú y sigue en el dropdown como **"Vender"**. **SIN LOGIN COMPLETO (2026-08-09)**: el formulario de vendedor ya no exige cuenta — `SOLICITUDES_VENDEDOR.ID_USUARIO` es NULL (createSql + migración idempotente en setup.js vía IS_NULLABLE), `POST /api/vendedor/solicitud` sin `verificarSesion` (solo rateLimit; duplicados por NIT/email con `(? IS NULL OR ID_USUARIO <> ?)`); sin sesión no hay notificación in-app (solo correo): al RECHAZAR solicitudes sin usuario se envía correo de rechazo con la observación (`enviarRechazo` en vendedorController); `obtenerSolicitudes` admin con LEFT JOIN a USUARIOS → `SOLICITANTE_NOMBRE` null se muestra "Sin cuenta (formulario público)" en AdminVendedores. Verificado: POST sin sesión 201, lista admin con solicitante null, rechazo 200, duplicado NIT 409, validación 400; UI Playwright sin sesión formulario visible + envío + Swal éxito 0 errores JS; suite vendedores 23/23. **Dato de test**: el doble navbar (AppLayout renderiza Navbar global + Principal/Catalogo/etc. renderizan el suyo) hace que Playwright vea 2 navbars superpuestos → los clicks requieren `{ force: true }`; en este test el force no navegaba porque ProtectedRoute redirigía a "/" (401 sin sesión). **Cookies Playwright**: `expires: 0` en el jar → Playwright la trata como expirada (401) → inyectar `sameSite: 'Lax'` y omitir `expires` cuando vale 0 (aplicado en `cookieJar` de test_vendedores.mjs y parseJar de test_nav_vendedor.mjs). (7) **Swal cambio de contraseña forzado**: `AuthContext.tsx` — `forzarCambioPassword` (inputs Swal pass actual/nueva/confirmar, llama `/api/auth/cambiar-password`, error con Swal, si descarta → logout; éxito → refreshPerfil + toast); se dispara vía `forzarCambioPasswordRef` en `fetchPerfil` y `login` cuando `DEBE_CAMBIAR_PASSWORD`=1. Verificado: `node --check` todos los archivos tocados OK, `npx tsc -b` OK, schema.sql regenerado (49000 bytes), contenedores reiniciados. **E2E Playwright 1440px (playwright 1.48.2)**: Test A (solicitud aprobada: card visible, correo de acceso, usuario vendedor, nota de contraseña temporal flag=1 — 4/4), B (rechazada: banner + observación + formulario reenvío + política eliminación + checkbox — 5/5), C (admin: título, 2 filas, badges APROBADA/RECHAZADA, modal detalle con NIT y categorías, filtro pendientes vacío — 7/7), D (dropdown: item "Ser vendedor" arriba de "Ayuda y Soporte", navega a /ser-vendedor — 3/3), E (login vendedor con temporal ClaveTemporalX → Swal "Debes cambiar tu contraseña" → nueva MiNuevaClave123 → "Contraseña actualizada" → sesión activa — 4/4). 0 errores JS/red (solo 401 esperado al cargar sin sesión). NOTA tests: click en `.user-profile-trigger`/`.btn-login-new` requiere `{ force: true }` (un overlay del menú móvil intercepta el hit-test). **NOTA re-ejecución suite**: el Test E dejó al vendedor con password `MiNuevaClave123` y flag=0 → para re-correr hay que re-aplicar `set_pass_vendor.js` (restaura ClaveTemporalX + flag=1) y el A4/E dependen de ese estado. Usuarios demo en BD: Pepe (prueba.usuario.vendedor@test.com / Prueba123, solicitud "Deportes Andinos SAS" APROBADA, vendedor con email prueba.vendedor@test.com, NIT 901123456) y Pedro (prueba.pedro@test.com / Prueba123, solicitud "Textiles Norte Ltda" RECHAZADA "NIT no verificable en la DIAN"). Credenciales admin: yeison/Losquiero7. SIN cambios que requieran setup.js manual (migraciones idempotentes aplicadas al reiniciar jadda_backend).
- Email con productos + anti doble-clic + reembolso con botón + página de detalle + timbre admin abre (2026-08-09): (1) **Email de estado con nombres de productos**: `estadoPedido.js` consulta DETALLE_VENTAS JOIN PRODUCTOS (LIMIT 10) y agrega al email la tabla "Productos de tu pedido" (`N. nombre ×cantidad`, texto escapado con helper `escapar()` — evita inyección de HTML por nombres con `<`/`>`), más "Referencia de pago" y "Total" formateado es-CO. Verificado capturando el mail (patched `mailer.sendMail` en contenedor): TO yeisiton922@gmail.com, HTML con "1. Malla porta balones ×1, 2. Espinilleras ×1, 3. Tenis Lebron Witness ×1" + referencia SIM_1786230238198_9 + $588.000. (2) **Idempotencia anti emails duplicados**: `adminController.actualizarEstadoCompra` y `actualizarEstadoEnvio` leen el estado ACTUAL primero; si es igual al pedido → responden `{ ok: true, sinCambios: true }` SIN UPDATE y SIN `notificarCambioEstado` (ya no se envían N emails al repetir "Por empaquetar"). En `AdminOrdenes.tsx` nuevo estado `procesandoEnvio` (number|null): guard en `cambiarEstadoEnvio` (`if (procesandoEnvio !== null) return`), set/clear en try/finally, stepper/avanzar/cancelar-envío con `disabled` mientras hay petición (`.ao-btn-avanzar/.ao-btn-cancelar/.ao-step` disabled con opacity 0.55 + cursor not-allowed en adminDashboard.css), y el Swal success se omite si `data.sinCambios`. Verificado por API: PUT POR_EMPAQUETAR → 200 + 1 notificación; repetido → `sinCambios:true` y 0 notificaciones nuevas. (3) **Reembolso por BOTÓN (no prompt automático)**: tras cancelar un pedido YA NO se pregunta automáticamente por reembolso — en el detalle del pedido CANCELADO aparece botón **"Solicitar reembolso"** (FaWallet, `.comp-acciones`) → Swal aviso "se reembolsará en un máximo de 7 días al método de pago con el que compraste" + monto → `POST /api/compras/:id/reembolso` (transacción con FOR UPDATE, solo ventas propias CANCELADAS, 400 "Ya solicitaste un reembolso" si existe DEVOLUCIONES SOLICITADA/APROBADA, inserta 1 fila DEVOLUCIONES por producto con MOTIVO 'Reembolso por cancelación del pedido' y ESTADO SOLICITADA) → Swal "REEMBOLSO SOLICITADO" con botones "Ver mi reembolso"/"Cerrar". El botón del pedido cambia a **"Ver mi reembolso"** (cuando `REEMBOLSO_ESTADOS` de `obtenerCompras` contiene SOLICITADA/APROBADA — subconsulta GROUP_CONCAT de DEVOLUCIONES agregada al SELECT). (4) **Página `/perfil/reembolso/:id`** (`ReembolsoDetalle.tsx` + `ReembolsoDetalle.css`, clases `rr-*`, ruta lazy en App.tsx): consume `GET /api/compras/:id/reembolso` (nuevo en comprasController.obtenerReembolso + comprasRoutes: datos venta + reembolsos con NOMBRE/IMAGEN/PRECIO_UNITARIO vía JOIN a DETALLE_VENTAS y `totalReembolso` calculado) — badge de estado EN REVISIÓN/APROBADO/RECHAZADO, monto a devolver, **aviso 7 días con fecha estimada** (FECHA_CREACION + 7d) si SOLICITADA, timeline 3 pasos (Solicitud recibida → En revisión → Reembolsado), información del pedido (método/referencia), productos del reembolso con subtotales, franja "Total a reembolsar" y botón Factura PDF. (5) **Admin**: AdminDevoluciones renombrado "Devoluciones y reembolsos" con **filtro por tipo** (chips Todos/Reembolsos/Devoluciones), badge rojo REEMBOLSO (FaWallet) o gris DEVOLUCIÓN en la columna Tipo/Motivo, y el Swal de aprobar dice "el dinero se reembolsará al cliente en un máximo de 7 días" para reembolsos. (6) **Timbre admin no abría**: `BellNotificaciones` en el pie del sidebar (`.admin-sidebar-actions .bell-container`, izquierda x=14) — el `.bell-panel` heredaba `right: 0` y abría hacia abajo: quedaba fuera de pantalla (x=-316). Fix en Navbar.css: `.bell-container.oscuro .bell-panel` abre HACIA ARRIBA (`top: auto; bottom: calc(100% + 14px)`) y anclado a la izquierda (`left: 0; right: auto`) → panel 370px dentro del viewport; se requirió `docker restart jadda_frontend` (CSS stale de Vite). Verificado con Playwright 1440px, 0 errores JS: panel dentro del viewport, 17 items, click en item → navega a /perfil/compras; flujo UI completo MisCompras: cancelar → solo "PEDIDO CANCELADO" (SIN prompt), botón "Solicitar reembolso" → aviso 7 días → "REEMBOLSO SOLICITADO" + "Ver mi reembolso" → navega a /perfil/reembolso/15 (EN REVISIÓN, $198.000, fecha estimada domingo 16 ago 2026, 3 pasos, 1 producto) → botón cambia a "Ver mi reembolso" → admin filtro Reembolsos (4) con badge REEMBOLSO y fila #15. `npx tsc -b` OK. SIN cambios de esquema (no requiere setup.js ni schema.sql). Nota: el usuario dejó su propio demo en BD (venta 12 CANCELADA con reembolso SOLICITADA x3 productos — visible en su nueva página /perfil/reembolso/12 y en AdminDevoluciones); la devolución de prueba de venta 15 se eliminó y su estado restaurado (COMPLETADA/PENDIENTE).
- Fix notificación admin + email "No recipients" + corte tabla órdenes (2026-08-09): (1) **Email**: el error `EENVELOPE: No recipients defined` al cambiar estado de envío ocurría porque el EMAIL del admin en BD es `yeison` (no es un correo válido) — `estadoPedido.js` ahora valida `^[^\s@]+@[^\s@]+\.[^\s@]+$` antes de sendMail (si es inválido, salta el correo sin error); los correos reales de registro siguen enviándose. (2) **Notificación "que falta"**: `notificarCambioEstado` SÍ creaba la notificación, pero con `idUsuario` = el cliente de la venta (admin, ID 1) y el timbre del admin (`filtroUsuario` en notificacionController) solo mostraba `ID_USUARIO IS NULL` — ahora el admin ve **globales + las suyas** (`ID_USUARIO IS NULL OR ID_USUARIO = ?`, mismo params en las 4 rutas: misNotificaciones/noLeidas/marcarLeida/marcarTodasLeidas). (3) **Corte de la tabla de órdenes**: el botón "Gestionar" (y el detalle expandido) quedaban cortados a 1440px porque `.ap-tabla-wrap` tenía `overflow: hidden` — ahora `overflow-x: auto` (scroll si hace falta, sin cortar; también beneficia AdminProductos) + `.ao-tabla` con `min-width: 940px` y padding de celdas `12px 12px` → a 1440px la tabla entera cabe sin scroll (verificado: wrap 1104px, scroll horizontal 0px, botón Gestionar completo, detalle 2 columnas visible). Verificado con Playwright 0 errores JS: PUT envio 15 → EN_CAMINO 200 → timbre admin muestra "Tu pedido #15 está en camino…" → restaurado PENDIENTE; logs sin EENVELOPE.
- AdminOrdenes profesional con envíos y entregas (2026-08-09): (1) **Backend** `obtenerTodasLasCompras` (adminController.js) ahora trae del ENVIOS completo: BARRIO, CODIGO_POSTAL, OBSERVACIONES, TELEFONO_CONTACTO, COSTO_ENVIO (numeric/null), FECHA_ENVIO + TELEFONO del cliente; DETALLE_VENTAS con LEFT JOIN a PRODUCTO_VARIANTES (COLOR/NOMBRE_ATRIBUTO/ATRIBUTO) y agregados `TOTAL_ARTICULOS`/`TOTAL_UNIDADES`. (2) **AdminOrdenes.tsx reescrito** con clases `ao-*` (CSS nuevo al final de adminDashboard.css): header profesional; **5 KPI cards clickeables** (`.ao-kpi` con iconos de colores: Total, Pendientes, En camino, Entregadas, Canceladas → click filtra); toolbar reutilizando `ap-search/ap-filtro/ap-count` con buscador (#/cliente/correo), filtro estado venta, estado envío y fecha (7/30 días); tabla `.ao-tabla` sin Bootstrap: orden con fecha (FaCalendarAlt), cliente con mail, artículos/unidades, total + método, badges de estado `.ao-bv-*` y envío `.ao-be-*` con FaTruck; botón "Gestionar" expande **detalle en 2 columnas**: IZQ = artículos con imagen 52px, chips de variante (FaPalette/FaTag), xCantidad + subtotal, y desglose totales (subtotal/envío/total con fila final) — DER = "Envío y entrega": **timeline stepper de 5 pasos** (Pendiente→Por empaquetar→Empacado→En camino→Entregado, `.ao-step` con círculos hecho-verde/activo-rojo, clickeable para saltar, línea conectora), banner rojo si CANCELADO, botón "Avanzar: marcar como X" (`.ao-btn-avanzar`), tarjeta de info de envío con íconos (dirección+barrio, ciudad/depto/CP, teléfono, observaciones, costo envío gratis/$X, fecha envío), y acciones: select de estado de la orden + Factura PDF + "Cancelar envío" (con Swal de confirmación); paginación reutiliza `.ap-paginacion` (10/pág); cancelar envío ahora confirma con Swal. Verificado con Playwright 1440px, 0 errores JS: 5 KPIs (9 órdenes), búsqueda "admin" → 1 fila, filtro EN_CAMINO → 0, detalle con 5 pasos/activo Pendiente/1 producto/2 chips variantes/botón avanzar/info completa ($198.000 + $9.000 = $207.000); flujo real: click Avanzar → PASO activo "Por empaquetar" + API confirma POR_EMPAQUETAR → click paso "Entregado" → salta directo + oculta avanzar → restaurado a PENDIENTE por API.
- Breadcrumb + confirmación al quitar imagen + guardia de cambios sin guardar (2026-08-09): (1) **Breadcrumb** en EditarProductoAdmin pasa de "Dashboard › Editar Producto #29" a **"Dashboard › Productos › Producto %nombre%"** (link "Productos" → `/admin/productos`; último segmento = estado `nombre`, fallback `Producto #id`). (2) **Confirmación al quitar imagen**: el ✕ de `SubirImagenes.tsx` ahora abre Swal "¿Quitar esta imagen?" (question, confirmButtonColor #d33, reverseButtons; aplica también al modal crear de AdminProductos, compartido). (3) **Guardia de cambios sin guardar**: nuevo `frontend/src/utils/navigationGuard.ts` (guardia a nivel de módulo: `setGuardiaNavegacion/puedeNavegar/navegarConGuardia`); `AdminNavbar.tsx` enruta TODAS sus navegaciones por `navegarConGuardia` (sidebar tabs, logo, botón tienda) y `cerrarSesion` chequea `puedeNavegar` antes de logout; `EditarProductoAdmin.tsx` — **dirty tracking por snapshot**: al cargar guarda `JSON.stringify` de los 9 campos (nombre, marca, precio, ids, descripcion, imagenes, variantes, caracteristicas) en `snapshotRef` y un useEffect compara y setea `dirty`; con dirty=true registra la guardia (Swal "Cambios sin guardar" → "Salir sin guardar"/"Seguir editando"), `beforeunload` (dialogo del navegador) y bloqueo de **back del navegador** (popstate → pushState + Swal, `permitirSalidaRef` para permitir salida real tras confirmar); "Cancelar" de edición también confirma si hay dirty; tras guardar o cancelar, `cargarProducto` actualiza el snapshot → dirty vuelve a false. Verificado con Playwright 1440px, 0 errores JS: breadcrumb "Dashboard › Productos › Guayos profesionales" + link visible; editar nombre → click sidebar → Swal → "Seguir editando" queda en la página → 2º click → "Salir sin guardar" navega a Órdenes; quitar imagen → Swal → Cancelar conserva 3 → Quitar deja 2; reload con cambios → dialog `beforeunload`; cancelar edición con cambios → Swal → salir → nombre recargado "Guayos profesionales"; carga limpia sin dirty.
- Orden tienda por popularidad + admin por ID + carpetas de subida (2026-08-09): (1) `obtenerProductos` ahora ordena `ORDER BY visitas DESC, ID ASC` (visitas = COUNT de HISTORIAL por producto) — la TIENDA muestra primero lo más visitado; (2) `AdminProductos` por defecto ordena por **ID ascendente** con nueva columna `#` ordenable (`.ap-id`), las filas se ven 1..44 en orden; (3) `POST /api/productos/imagenes` acepta `idProducto` opcional → guarda en `uploads/Producto_NN/img_{siguiente}.{ext}` (continúa numeración del max existente; URL `/images/productos/Producto_NN/img_N.ext`); sin idProducto (modal crear) sigue en `subidas/`; `SubirImagenes` acepta prop `idProducto` y EditarProductoAdmin la pasa; (4) `subirFotoPerfil` ahora guarda en **carpeta por usuario** `uploads/perfiles/u{ID}/perfil.{ext}`: elimina la foto anterior de esa carpeta (regex `perfil.\w+`) y la reemplaza — siempre 1 archivo; las fotos planas viejas quedan intactas; sigue sirviéndose por express.static del backend (recursivo, sin bug de Docker). Cupones confirmado: SOLO `RETO-` se marca USADO=1 en checkout (checkoutController.js:120-121,162); los descuentos de producto nunca se destruyen. "Ver página" 404 resuelto (era la versión con `/api/producto/` roto): verificado con Playwright popup renderiza `Guayos profesionales` sin errores. Verificado: upload API → archivo en Producto_01/img_4.png servido 200 (limpiado), foto → u1/perfil.png reemplazada (1 archivo), API tienda ordena 1,2,3,4,5 (visitas 2,1,1,1,1), Playwright admin # 1..10 asc + sort precio OK, 0 errores JS.
- READMEs profesionales + fix ruta Ver página (2026-08-09): los 5 READMEs reescritos con badges shields.io, tablas actualizadas y datos reales del stack: **raíz** (resumen 3 capas, arquitectura, inicio rápido Docker/sin Docker, credenciales admin seed, features usuario/admin al día, docs con 49 RF/39 HU/15 RNF, equipo); **backend** (stack real con multer/pdfkit, rutas corregidas: eliminado `GET /google-client-id` inexistente, agregados `social-login`, `/foto`, `cambiar-email`, `confirmar-cambio-email`, `verificar-password`, `DELETE /retos/evidencias/:id`, `/admin/pendientes`, bases `/api/envio|newsletter|contacto|notificaciones`, env vars `FRONTEND_URL`/`ADMIN_EMAIL`/`ADMIN_PASSWORD`, notas de BD 33 tablas + migraciones); **frontend** (estructura con todos los admin pages/componentes nuevos, rutas con `/producto/:id`, `/admin/editar/:id`, `/perfil/metodos-pago`, NotFound catch-all, nota de mounts y proxy); **movil** (profesional con status table ✅/🚧 y nota Expo SDK 54); **docs** (badges 49/39/15 + verificación). **BUG FIX**: el replace de `/productos/${id}`→`/producto/${id}` en EditarProductoAdmin rompió los fetch del API (`/api/producto/` → 404) — corregido a `/api/productos/${id}`; los 3 `window.open` quedaron en `/producto/${id}` (ruta pública real, verificada con Playwright: popup `http://localhost:5173/producto/1`, 0 errores JS). Probe completo de edición re-corrido OK (galería, modal 15 chips/7 filas, reorden, 0 errores).
- Editar producto admin con galería ordenable + vista previa (2026-08-09): `SubirImagenes.tsx` (compartido con AdminProductos) ahora permite **ordenar miniaturas con flechas ◀▶** (`.si-*` en adminDashboard.css): badge `Portada` rojo en la 1ª (el resto numeradas 2..N), botón ✕, hint "Usa las flechas para ordenar". `EditarProductoAdmin.tsx` (+ CSS `pp-*` en editarProducto.css): componente `GaleriaAdmin` (principal aspect-ratio 1/1 + tira de thumbs con badge Portada/número, activa con borde rojo) usado en el card "Vista previa" del sidebar y en "Imágenes del Producto" (thumbs SIEMPRE visibles, en modo ver y editar — antes solo un texto "N imagen(es)"); **modal "Vista previa del producto"** (`.pp-backdrop/.pp-modal`) que replica la página pública: galería clickeable, chip categoría, nombre, marca, precio con viejo tachado + chip -X% (precio original = precio/(1-pct/100)), badge stock ok/bajo/agotado, chips de variantes con FaPalette/FaTag + "N uds" (agotadas en rojo), descripción y ficha técnica en filas zebradas; botones "Vista previa" y "Ver página" (window.open `/productos/:id`) en save bar (ambos modos) y sidebar; guardado ya persiste el ORDEN (backend `actualizarProducto` borra PRODUCTO_IMAGENES y reinserta con ORDEN 1..N según el array). Verificado con Playwright 1440px: galerías renderizan, click thumb cambia principal, modal con 15 chips variante/7 filas ficha, reorden con flechas funciona y se cancela sin guardar, modal crear de AdminProductos abre OK, 0 errores JS. NOTA ruta: `/admin/editar/:id` (no `/admin/productos/:id`).
- AdminProductos rediseñado (2026-08-09): `AdminProductos.tsx` reescrito con clases `ap-*` (CSS en adminDashboard.css): toolbar con buscador (`.ap-search` con icono FaSearch), filtro por categoría (`.ap-filtro`) y contador de productos (`.ap-count`); tabla `.ap-tabla` con columnas imagen 52px, nombre+marca, categoría como chip `.ap-chip` con color derivado por hash del nombre, precio `.ap-precio`, stock con badges `.ap-stock ok/bajo/agotado` (≤10 "¡Solo quedan N!", 0 "Agotado") y acciones `.ap-btn-accion editar/eliminar`; **columnas Nombre/Precio/Stock ordenables** (`.ap-sortable` + FaSort/FaSortUp/FaSortDown, estado `orden:{campo,dir}`); paginación `.ap-paginacion` (Anterior/páginas/Siguiente, `.ap-page-num.activa` con gradiente rojo); estado vacío `.ap-vacio`; SIN scroll interno (paginación 10/pág); modal de creación/edición intacto. Verificado con Playwright 1440px: 10 filas, orden precio asc $25.000 → desc $1.800.000, búsqueda "balon" → 3 filas, filtro Cardio → solo chips Cardio, última página 5 activa, tabla sin scroll, 0 errores JS.
- Admin rediseñado (2026-08-09): (1) **AdminNavbar → sidebar lateral oscuro** (`AdminNavbar.tsx` reescrito, clases `admin-sidebar*`): fijo 252px, gradiente #0f172a→#1e293b, logo JS + "JADDA SPORTS / Panel de control", 8 links con iconos react-icons (no emoji) y **badges de pendientes** (evidencias retos + devoluciones) traídos de `GET /api/admin/pendientes` (nuevo en adminController.obtenerPendientes + adminRoutes) al montar y al cambiar de ruta; pie con avatar/letra, usuario, BellNotificaciones oscuro, botones tienda/logout. Responsive: <992px el sidebar se oculta (translateX -105%) y aparece botón hamburguesa `.admin-sidebar-toggle` + backdrop que abren el drawer (`.admin-sidebar-open`). (2) **AdminDashboard reescrito** (`AdminDashboard.tsx` + clases `adm-*`): header con saludo y fecha, botones Reportes/Ver tienda; 4 KPI cards con gradientes (Ingresos 30d, Pedidos 30d, Clientes, Productos) clickeables; tarjeta HOY con ingresos/pedidos del día + **% vs ayer** (↑verde/↓rojo/–) + ticket promedio 30d + unidades 30d; tarjeta "Por revisar" (evidencias retos, devoluciones, stock bajo, avisos de reposición con contadores); **gráfica de barras CSS** de ventas 30 días (tooltip con día/ingresos/pedidos); top 5 más vendidos (pos, imagen, unidades, barra proporcional); últimas órdenes con badges de estado `.adm-estado-*`; usuarios recientes con avatar; resumen global (ingresos confirmados, órdenes, usuarios, productos). (3) **Backend `obtenerDashboard` ampliado**: stats globales + 30d (ordenes/ingresos/ticket/unidades), comparativa hoy vs ayer, serie diaria 30d, top 5 más vendidos, 6 órdenes recientes, 5 usuarios recientes, pendientes (evidencias/devoluciones/stock bajo/avisos). (4) **adminDashboard.css reescrito** manteniendo compatibilidad con clases viejas (custom-modal, edit-*, evidencia-*, reporte-*, admin-input/label/select): fondo #f1f5f9, `.admin-content` con margin-left 252px (0 en móvil, padding 80px top), `.admin-footer` restyling con mismo margin. Verificado E2E: dashboard con 4 KPI ($2.843.000), 2 barras reales, top 5, 6 órdenes, badges sidebar, navegación KPI→reportes y sidebar→productos con content left=252, móvil drawer abre, 0 errores JS. **Ajustes post-feedback (2026-08-09)**: sidebar SIN scroll (quitar `overflow-y:auto` de `.admin-sidebar-nav`), tarjeta HOY de azul oscuro → **fondo blanco** (`.adm-hoy-card` #fff, métricas #f8fafc, texto #0f172a), y en AdminProductos se eliminó el `maxHeight/overflowY` de la tabla (`style` inline de `.table-responsive`) — la paginación de 10 queda sin scroll interno. Verificado con Playwright: sidebar no scrollable, hoy-card rgb(255,255,255), tabla sin maxHeight y sin scroll, 10 filas + paginación, 0 errores JS.
- Navbar brand con nombre (2026-08-09): "JADDA SPORTS" junto al logo — `Navbar.tsx` agrega `<span class="navbar-brand-name">JADDA <span class="navbar-brand-name-sports">SPORTS</span></span>` dentro del Link brand; `Navbar.css`: `.navbar { padding: 0 16px }` (antes 40px — logo más a la izquierda), `.navbar-brand` flex gap 10px, nombre blanco 1.05rem/800/letter-spacing 1.5px con SPORTS dorado #ffd166, white-space nowrap. Verificado con Playwright 1440px: logo x=16 (antes 40), nombre x=89 (justo a la derecha, gap 10px), menú INICIO x=296 sin solape.
- Evidencias visibles y eliminables por el usuario (2026-08-09): el aviso "N avances en revisión" (amarillo) y el de rechazados (rojo) en Mis Retos son CLICKEABLES → abren modal Swal "Mis avances enviados" (fondo #1a1a1a) con lista de evidencias (previews img/video 120x90 con preload=metadata, chip de estado EN REVISIÓN/APROBADO/RECHAZADO, cantidad + fecha) y botón "Eliminar avance" SOLO en las pendientes (Swal de confirmación con reverseButtons; al confirmar cierra los dos modales, recarga y muestra toast). En retos COMPLETADOS hay link "Ver avances enviados". Backend: `DELETE /api/retos/evidencias/:id_evidencia` (verificarSesion) en retoController.eliminarEvidencia — valida que sea del usuario y ESTADO='pendiente' (aprobada/rechazada → 400 "Solo puedes eliminar avances que siguen en revisión", ajena/inexistente → 404), borra del disco RUTA + RUTAS_EXTRA (unlink con try/catch, `uploads/retos/`) y la fila. E2E: evidencia pendiente creada → archivo existe en disco → DELETE 200 → archivo desaparece → aprobada → 400 → inexistente → 404; Playwright: alert clickeable → modal con 2 previews + chip APROBADO + botón eliminar → confirmación → CANCELAR no borra (verificado en BD). Datos demo dejados a propósito para que el usuario pruebe: reto_usuario 10 (admin, reto 1 "Semana Activa"), evidencia 12 pendiente con imagen, venta 15 (admin, 1 producto).
- MisCompras en 2 columnas (2026-08-09): el detalle expandido ya no apila todo hacia abajo — `.comp-detalles` es grid `minmax(0,1.55fr) minmax(0,1fr)` (breakpoint 992px → 1fr): columna IZQUIERDA = productos + total final; columna DERECHA (fondo #f8fafc, borde, radius 14px) = badge de envío, dirección, pago y `.comp-acciones` (Cancelar + Factura PDF juntos). Resultado medido con Playwright en viewport 1440x900: detalle 604px de alto (antes ~1100px), 0 errores JS.
- Retos + Favoritos UX (2026-08-09): (1) `Favoritos.css` compacto — grid `minmax(155px,1fr)`, imagen 125px, paddings/fuentes menores (móvil 130px/105px). (2) Porcentajes de retos bajados en `setup.js` (los 2 bloques seed) con tope 10%: ID1 5, ID2 8, ID3 10, ID4 6, ID5 3, ID6 4, ID7 5, ID8 10, ID9 5, ID10 5, ID11 8, ID12 5, ID13 6, ID14 4, ID15 6, ID16 8 — schema.sql regenerado; **BD EXISTENTE pendiente de actualizar en Workbench** (INSERT IGNORE no pisa; SQL listo abajo). (3) Reportar avance REDISEÑADO + videos grandes: estrategia = **multipart con multer 2.2.0 (streaming a disco, sin base64 ni memoria)** — `docker exec jadda_backend pnpm add multer`; `retoRoutes.js` sube `materiales[]` a `uploads/retos/r{id}/` (mkdirSync en destination, fileFilter jpg/png/webp/gif/mp4/webm/**mov**, límite 100 MB/archivo, máx 10) con mensaje 400 por límite; `retoController.reportarProgreso` acepta la rama multipart (TIPO según mimetype, RUTA=1º, RUTAS_EXTRA=resto) y mantiene la rama base64 legacy; helper `limpiarArchivos()` borra subidas si falla la validación (restante≤0, >10, catch general). Frontend `Retos.tsx`: modal nuevo — stepper −/+ para cantidad, **dropzone click + drag&drop**, previews con badge FOTO/VIDEO y chip de tamaño, acepta .mov, subida con **barra de progreso real** (XHR `upload.onprogress` → % en Swal "Subiendo evidencia…", no cierra), mensaje de revisión ≤24h; `MAX_MB_POR_ARCHIVO` 10 → 100. E2E: curl multipart imagen png + video mp4 → 2 evidencias pendientes OK (archivos en disco); Playwright flujo UI completo (dropzone/stepper/preview → + → enviar → barra → "Avance en revisión", 0 errores JS); datos de prueba limpiados (evidencias 5-8, retos_usuarios 8-9, usuario 13, notificaciones, carpetas r8/r9).
- Direcciones + métodos de pago UX (2026-08-09): `DireccionesPerfil.tsx` — tarjeta de dirección con más info en filas con íconos (etiqueta con FaHome, dirección destacada con FaMapMarkerAlt, ciudad/depto/barrio con FaCity, teléfono FaPhone, CP FaEnvelope como `<small>` → líneas `.dir-linea` con íconos rojos). `PerfilMetodosPago.tsx` — el `window.confirm` de eliminar (alert básico del navegador) reemplazado por SweetAlert2 dark (fondo #1a1a1a, confirmButtonColor #e63946, reverseButtons, texto con titular si existe). Verificado con Playwright: dirección muestra todas las líneas, eliminar abre Swal y cancelar no borra, 0 errores JS; datos de prueba (dirección 11, método 8) eliminados vía API.
- Mis Compras con variantes y rediseño profesional (2026-08-09): DETALLE_VENTAS YA NO guardaba la variante — se agregó columna `ID_VARIANTE` (NULL, FK a PRODUCTO_VARIANTES ON DELETE SET NULL) en CREATE_TABLES_RAW + MIGRACIONES idempotente en setup.js (aplicada con `docker restart jadda_backend`); `checkoutController` ahora graba `item.ID_VARIANTE` en el INSERT de DETALLE_VENTAS; `obtenerCompras` y `obtenerCompraPorId` (comprasController) hacen LEFT JOIN a PRODUCTO_VARIANTES y devuelven `COLOR`/`NOMBRE_ATRIBUTO`/`ATRIBUTO` por producto (ventas viejas → NULL, sin chips). Frontend `MisCompras.tsx`: fila de producto rediseñada (grid 4 columnas) — miniatura 64px en wrap, nombre con ellipsis, chips de variante (`<FaPalette/> COLOR` y `<FaTag/> NOMBRE_ATRIBUTO: ATRIBUTO`), precio unitario "X c/u", badge xCANTIDAD, subtotal + botón Devolver a la derecha, y pie `.comp-total-final` (franja azul marino `#0f172a→#1e293b` con total en negrita); header de productos con contador "N artículos"; media query móvil (grid 2 filas, total al final). schema.sql regenerado. E2E completo: compra real vía API (login admin → carrito con id_variante 2 → checkout → GET /api/compras devuelve COLOR "Blanco"/Talla "40") + Playwright (login admin → /perfil/compras → chips y diseño OK, 0 errores JS); datos de prueba (venta 14 + ENVIOS + DETALLE + PLANES) eliminados; venta 13 (usuario 9) quedó con variantes asignadas vía UPDATE como dato demo.
- Última conexión en /perfil/seguridad (2026-08-09): columnas `ULTIMA_CONEXION`/`ULTIMA_IP`/`ULTIMA_UBICACION` en USUARIOS (CREATE_TABLES_RAW + MIGRACIONES en setup.js, aplicadas al reiniciar backend); `registrarConexion()` en authController actualiza fecha+IP y geolocaliza best-effort con ip-api.com (timeout 2.5s, salta IPs privadas 10./192.168./172.16-31/127/::1) — NUNCA bloquea el login; se dispara en `login` y `socialLogin`; `obtenerPerfil` expone los 3 campos. `Seguridad.tsx` muestra "Última conexión: <fecha hora> · <ciudad, región, país>" (si no hay, "Aún no hay registros de conexión") con estilo .ultima-conexion en Seguridad.css. E2E: POST login → GET perfil → ULTIMA_CONEXION/ULTIMA_IP llenos (IP Docker 172.18.0.1 → ubicación null correcto). schema.sql regenerado.
- PerfilEditar seguro (2026-08-09): el correo YA NO se guarda con `PUT /perfil` directo — flujo `cambiar-email` (correo nuevo + contraseña actual validada con bcrypt → guarda `EMAIL_PENDIENTE` (columna nueva vía MIGRACIONES en setup.js, createSql+alterSql) y envía código de 6 dígitos al correo NUEVO) → `confirmar-cambio-email` (valida TOKEN/TOKEN_EXPIRA/EMAIL_PENDIENTE → recién ahí UPDATE EMAIL). Teléfono pide `verificar-password` antes de guardar. Nombre/usuario siguen directo. Rutas: `POST /api/auth/cambiar-email`, `POST /api/auth/confirmar-cambio-email`, `POST /api/auth/verificar-password` (todas con verificarSesion + rateLimit). Email removido de `actualizarPerfil`. E2E probado: pass mala → 400, duplicado → 409, código malo → 400, código bueno → 200 + login con correo nuevo OK. Usuario de prueba `prueba.cambioemail.nuevo@gmail.com` (ID 12) + `prueba.username.4812/7391@gmail.com` (IDs 10-11, + DIRECCIONES) pendientes de borrar en Workbench. schema.sql regenerado (EMAIL_PENDIENTE en CREATE_TABLES_RAW).
- PerfilEditar rediseñado (2026-08-09): solo info personal en filas desplegables — click en la fila despliega el editor (Nombres → nombre+apellido; Correo; Teléfono; Nombre de usuario al final); foto de perfil REMOVIDA de esta página (vive en Perfil.tsx); tipo/número de documento fuera; sección "Información de tu cuenta" queda con Miembro desde + Cambiar contraseña. Backend: `registro` asigna `USUARIO` aleatorio único `base.XXXX` (helper `generarUsuarioUnico` — normaliza nombre sin tildes, 6 reintentos, fallback timestamp; usado también en socialLogin); `actualizarPerfil` ahora acepta EMAIL (400 si formato inválido, 409 si ya existe otro usuario con ese correo), valida usuario (regex `^[a-zA-Z0-9._-]{3,30}$`, 409 duplicado) y teléfono (7-10 dígitos, 400). Probado E2E: 2 registros con mismo nombre OK (usernames únicos), PUT usuario válido/cambio/revert OK, espacio → 400, email inválido → 400, email duplicado → 409. Pendiente de borrar en Workbench: usuarios `prueba.username.4812@gmail.com` y `prueba.username.7391@gmail.com` (+ DIRECCIONES).
- Slot del slider de precio del catálogo (2026-08-09): el BUG "No encontramos productos en la categoría Cardio" era el filtro de precio (`PRECIO <= precioMaximo` con tope fijo $1M) — la Elíptica ($1.8M) y el Reloj ($1.2M) de Cardio superaban el tope y se descartaban silenciosamente. Fix en `Catalogo.tsx`: tope dinámico `precioTope` = mayor precio real redondeado a $50.000 (mín $1M) calculado al cargar los productos; `sliderTocado` evita pisar la selección del usuario; LIMPIAR FILTROS restablece al tope real. Se mantienen: filtro por ID_CATEGORIA robusto, búsqueda por categoría en backend y mensaje vacío contextual. Diagnóstico DIAG-FETCH/DIAG-FILTRO removido del código.
- ResumenCompra: nested fragment `<>...</>` → `<div>` fix for OXC parser error.
- Catalogo variant selector modal: opens on cart icon click, fetches `GET /api/productos/:id/variantes`, color + attribute buttons filtered by color, stock display, quantity selector, confirm button, 80x80 thumbnail.
- Search improved: prefix matching `LIKE 'word%'` (was `%word%`), all query words must match.
- Related products improved: `ORDER BY RAND()`, fallback to other categories if fewer than 4.
- ProductDetailPage: `addToCart` now passes `ID_VARIANTE` (not `cantidad`); selecting attribute first then color no longer resets attribute.
- Review form in ProductDetailPage: star hover effect, comment textarea, logged-in gate, auto-name from AuthContext, avatar circle.
- Review form in CompraExitosa: product selector buttons, star hover, comment, submit, confirmation.
- Footer compact: reduced padding/margins, `margin-top: auto` via AppLayout flex column.
- FloatingCart draggable: mouse/touch drag, viewport clamped, navbar boundary, click vs drag detection.
- MiniCartMenu: position synced via `cartButtonX/Y` in CartContext, closes on click-outside.
- `backend/database/setup.js` created: 22 `CREATE TABLE IF NOT EXISTS` + `INSERT IGNORE` for all reference data. Retries MySQL connection up to 10 times.
- `server.js` modified: added `require('./database/setup')` at line 9.
- `docker-compose.yml` modified: bind mount `./mysql_data` → named volume `mysql_data:`.
- READMEs created/updated: root, backend, frontend, movil.
- Mobile app reviewed: Inicio, Catalogo, Detalle Producto, Login, Registro functional. AuthContext ya persiste con AsyncStorage (`@jadda_usuario`).
- Catalogo two carts bug fixed: removed duplicate FloatingCart/MiniCartMenu.
- Categorias slowness fixed: bg placeholder, fadeIn animation, reduced margin-top.
- Docs analyzed: 39 HUs, 39 RFs, 7 RNFs. Stack mismatches found.
- RNF-001.md: Module 1 only (Security) — separated from monolithic original.
- RNF-002.md through RNF-006.md: extracted from original RNF-001.md modules 2–6, each with proper format.
- RNF-007.md: extracted Module 7 (Documentation/Maintainability) from original.
- RNF-008.md through RNF-011.md: 4 new modules — DevOps, Mobile App, Internationalization, Error Handling.
- `docs/plan de trabajo.md` created.
- `docs/Restricciones del Proyecto.md` created.
- RF-001 through RF-012: stack/endpoints corrected (JWT → express-session, `/api/v1/...` → `/api/...`, table names to real DB schema).
- RF-013 through RF-029, RF-036, RF-039: status/endpoints aligned to real implementation.
- HUs updated to match corrected RFs: statuses, acceptance criteria, endpoints, table names aligned.
- HU-002 fixed: was incorrectly containing RF-002 content; replaced with proper HU.
- HU-005 fixed: corrupted acceptance criteria content restored.
- HU-003 through HU-012, HU-013 through HU-029, HU-036, HU-039: statuses updated (Implementada), acceptance criteria rewritten.
- Diagrams created: `docs/diagrams/arquitectura.md`, `docs/diagrams/base-de-datos.md`, `docs/diagrams/flujos.md`.
- `docs/README.md` created: index with links to all docs, state table, stack.
- `docs/plan de trabajo.md` fixed: auth stack corrected (JWT → express-session).
- `backend/README.md`: auth stack corrected (JWT removed), route table expanded to 40+ real endpoints, env vars aligned, Facebook route removed.
- `frontend/README.md`: auth stack corrected (jwt-decode → express-session cookie-based).
- `movil/README.md`: API URL reference updated to `EXPO_PUBLIC_API_URL`, status table corrections (added verificar-codigo as Funcional).
- HU-001.md: verification flow corrected — 24h link → 15min 6-digit code, removed "NN Auth" reference.
- RF-001.md: verification flow corrected — 24h → 15min, endpoint `/api/auth/verificar-codigo` → `/api/auth/confirmar`, added `/api/auth/reenviar-codigo`.
- `docs/README.md`: RNF count updated 7 → 11.
- AGENTS.md: comprehensive update with all changes from this session.
- AdminProductos: búsqueda (nombre/marca/categoría), paginación 10/pág, orden aleatorio, sticky header, scroll acotado.
- NotFound page (`frontend/src/pages/NotFound.tsx`): 404 con botón "Volver al inicio"; catch-all en App.tsx apunta a ella (antes Principal).
- ErrorBoundary (`frontend/src/components/ErrorBoundary.tsx`) + ErrorFallback (`frontend/src/pages/ErrorFallback.tsx`): página "¡Oops!" con botón Reintentar; envuelve todas las rutas.
- LoadingPage (`frontend/src/components/LoadingPage.tsx`): pantalla de carga reutilizable; usada como fallback de Suspense y en Catálogo (pantalla completa hasta que carguen productos).
- Catalogo: estado de error con botón "Reintentar" si falla el fetch de productos.
- `backend/database/setup.js`: `seedAdminUser()` crea cuenta admin en CADA arranque (incluso si las tablas ya existen). Credenciales: `ADMIN_EMAIL`/`ADMIN_PASSWORD` en .env; default `yeison` / `Losquiero7`, ID_ROL=1, CONFIRMADO=1.
- `backend/controllers/checkoutController.js`: total calculado 100% en servidor (ignora `totalFinal`/`descuentoAplicado` del cliente); valida cupón contra DESCUENTOS; transacción completa (BEGIN/COMMIT/ROLLBACK); rollback en catch; conexión liberada en finally. Decisión del usuario: decremento de stock + MOVIMIENTOS_STOCK REMOVIDOS temporalmente hasta que se hagan pruebas.
- `backend/middlewares/rateLimiter.js` (nuevo): limitador en memoria (ventana deslizante por IP, sin dependencias). Aplicado a login (10), registro (5), confirmar (10), recuperar-password (5), verificar-codigo (10), update-password (10), reenviar-codigo (5), contacto (5).
- `backend/controllers/authController.js`: registro valida email/nombre/password (min 8) antes de BD; login devuelve 400 si faltan campos y usa mensaje unificado "Correo o contraseña incorrectos" (anti-enumeración).
- Protección de rol admin COMPLETA: middleware `esAdmin` (sesión + ID_ROL === 1) aplicado en `adminRoutes.js` (router.use), CRUD de productos (`productoRoutes.js`) y evidencias de retos (`retoRoutes.js`); `deserializeUser` en passport.js SÍ selecciona `ID_ROL`.
- Batch UX (2026-08-02): checkout valida stock SIN decrementar (400 con mensaje por producto); envío real por departamento; rating promedio + badges de stock en cards; cancelar pedido; dirección de pedido en servidor; CompraExitosa recupera venta al refrescar; guía de tallas; recientemente vistos; newsletter; plan con días persistidos; fixes Navbar/AuthModal.
- Migración imágenes locales (2026-08-02): 44 carpetas `Producto_01..44` (3 archivos `img_1/2/3` c/u) copiadas de `Downloads\imagenes_productos` → `frontend/public/images/productos/`; URLs en BD pasan de externas a `/images/productos/Producto_NN/img_K.ext`. Producto 45 ('Balón basket oficial', duplicado del 6) ELIMINADO de seeds y de BD viva.
- `backend/database/schema.sql` (UNICO archivo SQL): esquema completo (33 tablas) + seeds con 132 rutas locales (sin producto 45) — importable en Workbench para BD NUEVAS; regenerado con `node backend/database/exportarSchema.js`. Para BD EXISTENTES no se importa: al reiniciar el backend, `setup.js` aplica las migraciones idempotentes (columnas/tablas/índices nuevos).
- `backend/database/setup.js` + `schema.sql`: bloque PRODUCTO_IMAGENES regenerado con 132 rutas locales; filas del producto 45 removidas de PRODUCTOS y PRODUCTO_VARIANTES.
- Métodos de pago en perfil COMPLETO (2026-08-06): página `frontend/src/pages/PerfilMetodosPago.tsx` (`/perfil/metodos-pago`, tarjeta "Métodos de pago" en Perfil.tsx, ruta lazy en App.tsx) con CRUD completo + marcar principal; `backend/controllers/metodoPagoController.js` `guardarMetodo` valida ID_METODO contra METODOS_PAGO (400 si no existe) y auto-asigna ES_PRINCIPAL=1 si es el primer método; `ResumenCompra.tsx` checkbox "Guardar este método" ahora por defecto TRUE (al pagar queda guardado); CSS en `frontend/src/css/MetodosPagoPerfil.css`. Sincronización bidireccional: métodos guardados en checkout → aparecen en perfil y viceversa (ResumenCompra ya precarga el principal). Probado E2E: 1er método → esPrincipal 1, 2do → 0, id_metodo inválido → 400, PUT principal cambia orden, DELETE limpia.
- Subida de imágenes desde el PC en panel admin: `POST /api/productos/imagenes` (base64 JSON, `express.json({ limit: '25mb' })` global en server.js + límite 25mb en la ruta). Guarda en `backend/uploads/subidas/` que es bind-mount de `frontend/public/images/productos` (docker-compose: `./frontend/public/images/productos:/app/uploads`) → URL `/images/productos/subidas/<ts>-<rand>.<ext>`.
- `crearProducto` y `actualizarProducto` ahora aceptan `IMAGENES` (array de URLs, ORDEN secuencial 1..N); `URL_IMAGEN` individual sigue funcionando como fallback.
- `frontend/src/admin/SubirImagenes.tsx` (nuevo): componente reutilizable — botón "Subir imágenes del computador" (multi-archivo, accept jpg/png/webp/gif), previews con botón ✕, input "o pega una URL". Usado en AdminProductos (modal agregar) y EditarProductoAdmin (sidebar, modo edición); reemplaza el `<input type="url">`.
- Correo de checkout: helper `imagenParaCorreo()` antepone `FRONTEND_URL` (env, default `http://localhost:5173`) a rutas relativas para que las imágenes locales funcionen en el email.
- Móvil: `movil/constants/api.ts` exporta `resolverImagen(url)` (prefija `EXPO_PUBLIC_FRONT_URL`, default `http://10.2.178.124:5173`); aplicado en `movil/app/producto/[id].tsx` (imagen principal + miniaturas).
- Perfil + foto de perfil (2026-08-02): `POST /api/auth/foto` (base64 ≤10mb → `backend/uploads/perfiles/u{ID}/` → `/images/perfiles/u{id}/...`, mount nuevo en docker-compose); `Perfil.tsx` rediseñado (hero pegado al navbar, avatar con borde rojo + menú Ver foto/cargar desde navegador, modal fullscreen, dashboard de 7 tarjetas); `PerfilEditar.tsx` con botón "Subir foto desde el navegador" y sección "Información de tu cuenta" (correo, miembro desde con día, documento, botón cambiar contraseña).
- Migración automática de BD: `migrarTablasExistentes()` en setup.js aplica ALTERs idempotentes al arrancar cuando las tablas ya existen (lista `MIGRACIONES`; hoy: `ENVIOS.COSTO_ENVIO`, `DESCUENTOS.USADO`, `RETO_EVIDENCIAS`); schema.sql actualizado (la nota ya no pide ALTER manual).
- Lote UX grande (2026-08-02): navbar "Ver perfil" (antes "Miembro JADDA"); páginas de perfil pegadas al navbar (`padding-top: 0` — el offset lo da `body { padding-top: 65px }` global); foto por carpeta `u{ID_USUARIO}`; input file fuera del menú del avatar + `setTimeout 50ms`; PerfilEditar sin input URL (se mantiene FOTO_URL como almacenamiento); fecha con día; Seguridad/MisCompras/Direcciones/Favoritos pegadas; lápiz en DireccionesPerfil; favoritos con deshacer (barra 5s + POST restaurador); FloatingCart estático (sin drag, se eliminó estado de posición del contexto); MiniCartMenu calcula posición sobre el botón fijo.
- Retos completos (2026-08-02): retoController reescrito (evidencias + aprobación + cupón); retoRoutes con 9 rutas; checkout valida `DESCUENTOS.USADO` (cupones RETO- de un solo uso: no aplica si ya usado, marca USADO=1 en la transacción); docker-compose mount `./frontend/public/images/retos:/app/uploads/retos`; `Retos.tsx` con botón atrás + adjuntar imagen/video (base64 ≤10mb) + mensaje "nuestros asesores... 24 horas" + badge de avances en revisión; `AdminRetos.tsx` (`/admin/retos` + tab en AdminNavbar) con tabla, modal de vista (img/video) y Aprobar/Rechazar.
- Verificaciones: `node --check` OK (retoController, retoRoutes, checkoutController, setup.js) + `npx tsc -b` OK (frontend, tras quitar `pos` muerto en FloatingCart y `guardarFoto` muerta en PerfilEditar).
- RF-035 Aviso de stock COMPLETO (2026-08-06): tabla `AVISOS_STOCK` (ID_VARIANTE+ID_USUARIO UNIQUE, ENVIADO) auto-creada vía `MIGRACIONES.createSql` en setup.js (patrón NOTIFICACIONES, aplicada con `docker restart jadda_backend`); `backend/utils/correo.js` (nuevo) con `imagenParaCorreo()` extraído de checkoutController; `productoController.js`: `suscribirAvisoStock`/`estadoSuscripcionAviso`/`cancelarAvisoStock` + helper `notificarReposicion()` (email + notificación in-app + ENVIADO=1, nunca bloquea al admin); hooks de reposición en `actualizarVariante` (stock 0→>0), `agregarVariante` (stock>0) y `actualizarProducto` (reemplazo por posición: captura agotadas con avisos antes del DELETE); 3 rutas nuevas en `productoRoutes.js` (`POST/GET/DELETE /api/productos/variantes/:id/suscribir|suscripcion`); `ProductDetailPage.tsx`: botón "🔔 Avísame cuando vuelva a estar disponible" cuando la variante seleccionada está agotada (login obligatorio → Swal), estado "Te avisaremos — quitar aviso". Probado end-to-end con curl (login admin → suscribir → estado → admin sube stock 0→7 → notificación in-app creada + ENVIADO=1; email falla por SMTP no alcanzable pero capturado).
- RF-037 Compartir en redes COMPLETO (2026-08-06): `ProductDetailPage.tsx` inyecta meta tags OG dinámicos al cargar (`og:title`, `og:description`, `og:image`, `og:url`, `og:price:amount` COP, `twitter:card`) con cleanup al desmontar; botón "Compartir" ahora es dropdown con WhatsApp/Facebook/X (share URLs con window.open) + "Copiar enlace". Nota: en SPA los crawlers no ejecutan JS → preview no garantizada en todos los scrapers.
- Docs alineados (2026-08-06): RF-020/023/030/031 → Implementado (endpoints reales `/api/admin/compras`); RF-035/037 → Implementado; HUs HU-020/023/030/031/035/037 → Implementada; `docs/README.md` + `plan de trabajo.md` actualizados (aviso stock y compartir agregados); AGENTS.md corregido (esAdmin aplicado, ID_ROL en deserializeUser, defaults admin `yeison/Losquiero7`, móvil con AsyncStorage).
- RF-021 Factura PDF COMPLETO (2026-08-06): dependencia `pdfkit` agregada al backend (`docker exec jadda_backend pnpm add pdfkit`); `backend/utils/facturaPdf.js` (nuevo) genera el PDF con pdfkit (dimensiones A4 fijas — `doc.page` no está disponible al inicio del Promise; encabezado JADDA SPORTS, facturado a, referencia/método de pago, tabla de productos, subtotal/descuento/envío/total, datos de envío, pie con `ALTO_PAGINA`); `GET /api/compras/:id/factura` (usuario dueño) y `GET /api/admin/compras/:id/factura` (`esAdmin`) devuelven `application/pdf` + `Content-Disposition: attachment; filename="factura-<id>.pdf"`; el email de confirmación del checkout ahora adjunta el PDF (attachments de nodemailer, generado con try/catch para no romper la compra). UI: botón "Descargar factura PDF" (FaFilePdf) en MisCompras y "Factura PDF" en AdminOrdenes (window.open del endpoint). Probado end-to-end: registro + confirmación + login + checkout → PDF 200 con header `%PDF-1.3` (usuario y admin).
- RF-025 Notificaciones de estado COMPLETO (2026-08-06): `backend/utils/estadoPedido.js` (nuevo) con `notificarCambioEstado(idVenta, tipo, estadoNuevo)` — consulta venta+cliente, crea notificación in-app (NOTIFICACIONES, tipo `pedido`, ruta `/perfil/compras`) y envía email con mensajes por estado (TEXTO_ENVIO/TEXTO_ESTADO); disparado en `actualizarEstadoCompra` (VENTAS.ESTADO), `actualizarEstadoEnvio` (ENVIOS.ESTADO_ENVIO) y `cancelarCompra` (usuario). El envío nunca bloquea (errores SMTP capturados). Probado end-to-end: admin cambió envío a EN_CAMINO + venta a CONFIRMADA + cancelación → 3 notificaciones creadas para el cliente con título/mensaje/emoji correctos (verificado vía `GET /api/notificaciones`).
- RF-032 + RF-034 Reportes y más vendidos COMPLETOS (2026-08-06): `adminController.reporteVentas` (`GET /api/admin/reportes/ventas?desde&hasta`, defaults últimos 30 días) → `{totalOrdenes, totalIngresos, ticketPromedio, totalUnidades, serie[]}` (excluye CANCELADAS, AVG ticket, serie diaria para el gráfico); `adminController.masVendidos` (`GET /api/admin/analytics/mas-vendidos?desde&hasta&limite`) → ranking por unidades con ingresos y stock (ojo: usar subconsultas para IMAGEN/stock, NO JOIN a PRODUCTO_VARIANTES — multiplicaba unidades); página `frontend/src/admin/AdminReportes.tsx` (`/admin/reportes`, tab 📈 en AdminNavbar): inputs de rango + presets 7/30/90 días, 4 tarjetas KPI, gráfico de barras CSS (sin librería de charts), tabla top 10 con stock badge; ruta en App.tsx. Probado end-to-end: 403 para no-admin, cancelar venta la excluye del reporte, fechas inválidas → defaults. Datos de prueba limpiados.
- RF-027 CRUD categorías COMPLETO (2026-08-06): `crearCategoria`/`actualizarCategoria`/`eliminarCategoria` en productoController (POST/PUT/DELETE `/api/productos/categorias` con `esAdmin`; nombre único 3-100 chars; DELETE bloqueado si la categoría tiene productos → 400 con conteo); `obtenerCategorias` ahora devuelve DESCRIPCION + TOTAL_PRODUCTOS; página `frontend/src/admin/AdminCategorias.tsx` (`/admin/categorias`, tab 🗂️ en AdminNavbar) con tabla + modal crear/editar (maxLength 100) + Swal de confirmación; ruta en App.tsx. Probado end-to-end: crear 201, duplicado/nombre corto 400, renombrar, eliminar 200, categoría con productos 400, DELETE repetido 404, no-admin 403.
- RF-033 Devoluciones COMPLETO (2026-08-06): tabla `DEVOLUCIONES` (ID_USUARIO, ID_VENTA, ID_PRODUCTO, CANTIDAD, MOTIVO, ESTADO SOLICITADA|APROBADA|RECHAZADA, FECHA_CREACION, FECHA_PROCESADA) auto-creada vía `MIGRACIONES.createSql` en setup.js (con `docker restart jadda_backend`); `backend/controllers/devolucionController.js` + `backend/routes/devolucionRoutes.js` (montadas en `/api/devoluciones`): `POST /` (usuario, solo ventas COMPLETADAS propias, cantidad ≤ comprada − ya solicitada/aprobada, motivo ≤ 500), `GET /` (mis solicitudes), `GET /admin` (admin, con cliente/producto), `POST /admin/:id/procesar` (APROBADA → transacción con FOR UPDATE: reingresa CANTIDAD al stock de la variante por defecto `MIN(ID_VARIANTE)` + notificación in-app tipo `devolucion` ruta `/perfil/compras`; RECHAZADA → solo estado + notificación; doble proceso → 400). UI: botón "Devolver" (FaUndoAlt) por producto en MisCompras (solo COMPLETADA) con Swal cantidad/motivo; `frontend/src/admin/AdminDevoluciones.tsx` (`/admin/devoluciones`, tab ↩️ en AdminNavbar) con tabla, badge de estado, modal de detalle y Aprobar/Rechazar. Probado end-to-end: 201/400 (cantidad excedida, venta ajena 404), aprobada → stock 50→51 + notificación, rechazada sin reingreso, stock restaurado y datos de prueba limpiados.
- RF-038 Recomendaciones COMPLETO (2026-08-06): `obtenerRecomendados` en productoController (`GET /api/productos/recomendados`, sesión): productos de categorías compradas (ventas no CANCELADAS, excluye productos ya comprados), ordenados por frecuencia de compra de la categoría, LIMIT 8, con forma igual a `obtenerProductos` (STOCK, RATING, ID_VARIANTE_POR_DEFECTO); fallback a más vendidos (JOIN subconsulta pop) cuando no hay datos; responde `{origen: 'categorias'|'populares', productos[]}`. Sección "✨ RECOMENDADOS PARA TI" en Principal (solo con sesión, fetch on `usuarioLogueado`). OJO BUG: la consulta fallback quedó sin el array de parámetros `db.query(sql, [idUsuario])` → `?` literal → ERROR 1064 "near '?'"; la consulta de categorías sí lo tenía y pasaba — verificar SIEMPRE que cada `?` tenga su valor en TODAS las ramas. Probado end-to-end: sin compras → origen 'populares'; con compras → origen 'categorias' excluyendo lo comprado; 401 sin sesión.
- Auditoría de seguridad/robustez RF-027/033/038 COMPLETA (2026-08-06): (B1) `actualizarCategoria` ya no devuelve 404 en updates sin cambios (mysql2 `affectedRows` = filas cambiadas; ahora verifica existencia con SELECT previo); (B2) validación de tipos en `description`/`motivo` (`String(x).trim()` — antes `123`.trim()` lanzaba 500); (B3) índice único `uq_categoria_nombre` en `CATEGORIAS.NOMBRE_CATEGORIA` (en CREATE_TABLES_RAW para BD nuevas + migración idempotente vía INFORMATION_SCHEMA.STATISTICS para BD existentes) + catch de `ER_DUP_ENTRY` → 400 en crear/actualizar; (B4) `backend/database/schema.sql` REGENERADO con `backend/database/exportarSchema.js` (script que extrae CREATE_TABLES_RAW + DEVOLUCIONES + SEED_DATA de setup.js — fuente única, 33 tablas; ejecutar `node backend/database/exportarSchema.js` tras cada cambio de esquema); (V1) `frontend/src/utils/escapeHtml.ts` nuevo — usado en Swal `html`/`title` de MisCompras (devolución) y AdminCategorias (los campos title/html de SweetAlert2 se interpretan como HTML → XSS almacenado vía nombres de producto/categoría); (V2) rate limiting en rutas nuevas: devoluciones POST (10/15min), admin list (60), procesar (30), categorías CRUD (30), recomendados (30); (V4) recomendados ahora excluyen productos agotados (`HAVING SUM(pv.STOCK) > 0` en ambas ramas). Verificado end-to-end: PUT no-op → 200, PUT id inexistente → 404, description numérica → 201 ("123"), duplicado → 400, 11º POST devolución → 429, producto con stock 0 desaparece de recomendados y vuelve al restaurar. `docs/diagrams/base-de-datos.md` nota de 24 → 33 tablas.

### In Progress
- (none)

### Blocked
- (none)

## Key Decisions
- Search uses prefix matching (`LIKE 'word%'`) so "za" only matches products starting with "za".
- FloatingCart draggable: touchAction: none + passive events; MiniCartMenu follows via CartContext shared position.
- Reviews only for logged-in users; name auto-fetched from AuthContext; avatar uses first letter.
- Database setup: `CREATE TABLE IF NOT EXISTS` + `INSERT IGNORE` safe for every restart.
- Docker volume: named volume for portability (no `mysql_data/` dir needed on new clones).
- `.env.example` contains only placeholders; real credentials shared outside git.
- Docs corrections preserve existing structure and format.

## Next Steps
0. (User) Probar en el navegador "Ser vendedor": (a) dropdown del navbar → "Ser vendedor" → formulario con políticas y los 9 campos; (b) enviar solicitud → card de éxito 48h; (c) como admin (yeison/Losquiero7) → /admin/vendedores → Aprobar (llega correo con credenciales temporales) o Rechazar (con observación); (d) el vendedor al iniciar sesión con la temporal DEBE cambiar la contraseña (Swal) antes de usar la cuenta. Demo en BD: Pepe ya aprobado (vendedor prueba.vendedor@test.com), Pedro rechazado → probar el reenvío desde su cuenta.
1. (User) Probar en el navegador el timbre: abrir el panel NO marca todo como leído; marcar UN item solo ese; con notificaciones nuevas el contador vuelve a 1.
2. (User) Workbench: aplicar los nuevos porcentajes de retos (INSERT IGNORE no pisa la BD existente):
   `UPDATE RETOS SET RECOMPENSA_PORCENTAJE = CASE ID_RETO WHEN 1 THEN 5 WHEN 2 THEN 8 WHEN 3 THEN 10 WHEN 4 THEN 6 WHEN 5 THEN 3 WHEN 6 THEN 4 WHEN 7 THEN 5 WHEN 8 THEN 10 WHEN 9 THEN 5 WHEN 10 THEN 5 WHEN 11 THEN 8 WHEN 12 THEN 5 WHEN 13 THEN 6 WHEN 14 THEN 4 WHEN 15 THEN 6 WHEN 16 THEN 8 END WHERE ID_RETO BETWEEN 1 AND 16;`
3. (User) Probar en el navegador el reporte de avance con un video grande (>10 MB, incluso .mov de iPhone): dropzone arrastra el archivo, barra de progreso real, evidencia en /admin/retos.
4. (User) Probar favoritos (tarjetas compactas) y que los % de retos se vean nuevos en la portada de retos.
5. (User) Restart containers: `docker compose up -d` (aplica los mounts nuevos `./frontend/public/images/perfiles` y `./frontend/public/images/retos`) + `docker restart jadda_backend` (aplica `migrarTablasExistentes` → `ENVIOS.COSTO_ENVIO`, `DESCUENTOS.USADO`, tablas `RETO_EVIDENCIAS`/`NOTIFICACIONES`/`AVISOS_STOCK`/`DEVOLUCIONES` + retos 5-16, rutas nuevas); `docker restart jadda_frontend` si hay parse errors stale.
6. (User) Terminar app móvil: carrito real (hoy placeholder), checkout, favoritos, reseñas; `resolverImagen` no está aplicado en `ProductoItem.tsx` (catálogo móvil probablemente no muestra imágenes locales).
7. (User) Create `backend/.env.example` with placeholder values for git sharing (add ADMIN_EMAIL/ADMIN_PASSWORD, FRONTEND_URL).
8. (User) Remove legacy `jsonwebtoken` dependency, unused `verificarToken` middleware and dead `resenaController.js`.

## Critical Context
- `MYSQL_ROOT_PASSWORD: tu_password_secreto` in `docker-compose.yml` and `.env` — must match between backend and database container.
- `docker compose down -v` deletes the named `mysql_data` volume; on next `up`, setup.js recreates tables + seeds.
- Docker Vite dev server may show stale parse errors; `docker restart jadda_frontend` resolves.
- **Verificación frontend**: usar `npx tsc -b` (NO `tsc --noEmit` — el tsconfig raíz solo tiene referencias y no revisa nada). El Docker corre `pnpm build` = `tsc -b && vite build`.
- `addToCart(idProducto, idVariante, cantidad)` — second param is always a variant ID, never a quantity.
- FloatingCart and MiniCartMenu are rendered globally by `App.tsx`; individual pages should NOT import them.
- 49 RFs con estado verificado (2026-08-08): RF-001..RF-049; los RF-040..049 (retos, planes, métodos de pago, notificaciones, login social, foto de perfil, subir imágenes, newsletter, contacto, historial) y los RNF-012..015 (no-bloqueo correo, subidas, BD portátil, RBAC) documentados como nuevos. RF-023/029 → "Implementado parcialmente"; 39 HUs actualizadas (Implementadas). Sin pendientes de RF.
- Real stack: Express sessions + Passport.js (not JWT), `USUARIOS` table, `CONFIRMADO` field, `POST /api/auth/login`, Spanish error messages.
- Admin seed: `seedAdminUser()` corre en cada arranque (setup.js); si borras al admin en Workbench, se recrea al reiniciar el backend.
- Checkout seguro: el servidor recalcula subtotal/descuento/total; el decremento de stock por variante queda pendiente de habilitar tras pruebas (ver checkoutController.js).
- Checkout 2026-08-02: VALIDA stock por variante SIN decrementar (400 con mensaje por producto); costo envío calculado en servidor (`utils/envio.js`, gratis ≥ $200.000) y sumado al total; `ENVIOS.COSTO_ENVIO` se migra AUTOMÁTICAMENTE vía `migrarTablasExistentes()` en setup.js (ALTER idempotente al arrancar, verifica INFORMATION_SCHEMA) — ya no es necesario correr el ALTER a mano en Workbench.
- Foto de perfil: `POST /api/auth/foto` (base64, límite 10mb) guarda en `backend/uploads/perfiles/u{ID_USUARIO}/` → mount Docker `./frontend/public/images/perfiles:/app/uploads/perfiles` → URL `/images/perfiles/u{id}/...`; después se guarda la URL con `PUT /api/auth/perfil` (`foto_url`) y `refreshPerfil()` del AuthContext. Funciona SOLO en Docker (como la subida de imágenes de productos).
- Foto de perfil UX: el input file está FUERA del menú del avatar (el menú se desmontaba y el diálogo nunca abría) y se hace click con `setTimeout(() => fileInputRef.current?.click(), 50)`; PerfilEditar ya NO tiene input de URL (solo subida desde navegador) — la columna `FOTO_URL` se mantiene como almacenamiento de la ruta.
- Perfil (frontend): `Perfil.tsx` con hero pegado al navbar (padding-top 0 + body global), avatar con borde rojo clickeable → menú "Ver foto" (modal fullscreen) y "Cargar foto desde el navegador"; fecha con día (`{ weekday: "long" }`); PerfilEditar con sección "Información de tu cuenta" (correo, miembro desde con día, documento, botón "Cambiar contraseña" → `/perfil/seguridad`). Fallback avatar: ui-avatars.com.
- Carrito estático (2026-08-02): FloatingCart es un botón fijo `right:25px; bottom:25px` SIN drag (se eliminó todo el estado de posición `cartButtonX/Y/setCartButtonPos` de CartContext); MiniCartMenu posiciona sobre el botón fijo (`botonX = innerWidth - 65 - 25`, abre hacia la izquierda).
- Favoritos con deshacer (2026-08-02): al eliminar aparece barra fija bottom 90px right 20px con countdown de 5s (`fav-undo-bar`/`fav-undo-btn` en Favoritos.css); deshacer hace `POST /api/favoritos { id_producto }` + recarga. Icono de editar en DireccionesPerfil: `FaPencilAlt` (antes FaStar).
- Retos completos (2026-08-02): tabla `RETO_EVIDENCIAS` (ID_EVIDENCIA, ID_RETO_USUARIO, ID_USUARIO, TIPO imagen|video, RUTA, CANTIDAD, ESTADO pendiente|aprobado|rechazado, FECHA_SUBIDA); 16 retos seed (5-16 nuevos); `DESCUENTOS.USADO` (cupones de un solo uso). Flujo: reportar avance SIN material suma directo; CON material (imagen/video base64 ≤10mb → `backend/uploads/retos/r{id_reto_usuario}/`, mount `./frontend/public/images/retos:/app/uploads/retos`) queda PENDIENTE → admin aprueba/rechaza (`GET/POST /api/retos/admin/evidencias*`) → al aprobar suma CANTIDAD y si llega a META genera cupón `RETO-XXXX-XXXX` con 30 días de vigencia y USADO=0. Checkout: cupones `RETO-` ya usados (USADO=1) no aplican descuento y al aplicarse se marcan USADO=1 dentro de la transacción.
- Retos UI (2026-08-02): `Retos.tsx` con botón atrás, Swal de adjuntar material (file input, preConfirm base64, validación 10MB), mensaje "Deja que nuestros asesores revisen el material... hasta 24 horas", badge de avances en revisión, cupón mostrado con "un solo uso"; panel `AdminRetos.tsx` (`/admin/retos`, tab en AdminNavbar) con tabla de evidencias, modal para ver imagen/video, botones Aprobar/Rechazar.
- Navbar dropdown (2026-08-02): header del menú de usuario es `<Link to="/perfil">` con avatar (foto o ui-avatars), nombre y "Ver perfil" (antes "Miembro JADDA"); se quitaron "Mi Perfil" y "Mi Historial" (ruta `/historial` sigue existiendo).
- Páginas de perfil pegadas al navbar (2026-08-02): `body { padding-top: 65px }` es global (principal.css importado en main.tsx), así que las páginas ya no usan padding-top propio: Perfil/PerfilEditar/Seguridad/MisCompras/DireccionesPerfil/Favoritos con `padding-top: 0`.
- Mega-menú Catálogo compacto (2026-08-02): dropdown de 560px centrado bajo CATÁLOGO (antes full-width, tapaba la pantalla); izquierda = lista de categorías con hover activo (rojo), derecha = panel con hasta 4 productos de la categoría activa (categorías de productosMenu se filtran por `p.CATEGORIA === cat.NOMBRE_CATEGORIA`); pie "Ver todo el catálogo →".
- Footer reestructurado (2026-08-02): reescrito con grid CSS propio (sin Bootstrap cols), 4 columnas (Marca, Navegación, Atención al cliente, Síguenos con iconos react-icons), línea inferior de copyright. CSS `footer.css` reescrito completo.
- Newsletter del footer (2026-08-02): movida de barra roja superior a franja compacta abajo (antes de copyright), sin fondo rojo (borde sutil, input oscuro redondeado, botón outline).
- Recientemente vistos con descuento/stock (2026-08-02): `historialController.obtenerHistorial` ahora trae `ID_DESCUENTO`, `STOCK` (SUM de PRODUCTO_VARIANTES) e `ID_VARIANTE_POR_DEFECTO` (MIN) vía subqueries; entrada de localStorage en ProductDetailPage guarda los 3 campos nuevos; Principal enriquece cada item del historial con datos completos de `productos` y pasa `descuentoPorcentaje` + `onAgregarCarrito` a ProductCard (antes ni descuento ni badge de stock se mostraban).
- Envío: `GET /api/envio/calcular?departamento=&subtotal=` (tarifas por departamento, normaliza tildes); frontend ResumenCompra lo consulta al cambiar departamento (debounce 400ms).
- Checkout resumen (2026-08-02): el panel derecho muestra desglose completo — Precio base, Descuento de productos (si hay ID_DESCUENTO), Descuento cupón (si aplica), Envío y Total; `subtotalBase/descuentoProductos/subtotal` calculados por separado; row del checkout con `align-items: flex-start` (fix: la columna derecha ya no salta al cambiar método de pago — antes se estiraba con la izquierda).
- UX checkout + factura + correo (2026-08-08): (1) documento en ResumenCompra ya NO es obligatorio (se quitaron `*`, mensajes "Requerido" y bordes rojos; no se envía al backend); (2) panel resumen muestra SIEMPRE todas las filas: Precio base, Descuento de productos, **Subtotal** (`.subtotal-jadda` con borde punteado CSS), Descuento cupón, Envío y Total (antes los descuentos solo aparecían si existían y no había subtotal); (3) factura PDF con colores de página — franjas en azul marino `#002244` (antes `#111827`), logo `backend/assets/LogoJadda.png` (copia de `frontend/public/LogoJadda.png`, sincronizar a mano si cambia) en chip blanco redondeado, "JADDA" blanco + "SPORTS" rojo `#e63946`, frase **"Lo mejor en deportes"**; (4) `backend/utils/correo.js` nuevo helper `imagenComoDataUri(url)`: lee la imagen de `/app/uploads` (mount de `frontend/public/images/productos`) y la incrusta como base64 `data:image/...` en el email de checkout — las imágenes YA se ven en Gmail/Outlook/móvil (antes `http://localhost:5173/...` no cargaba en clientes externos); fallback a URL si el archivo no existe (dev fuera de Docker) + caché por ruta. Verificado: `node --check`, `npx tsc -b` y test en contenedor (PDF 324KB con logo, data URI OK). NOTA: las URLs de la BD coinciden con la extensión real (ej. Producto_01 usa `img_1.jpg`).
- Fixes 2da fase (2026-08-08): (1) dirección del checkout SÍ se guarda en DIRECCIONES: el botón "Siguiente" llama `guardarDireccionEnPerfil()` (PUT a la dirección seleccionada/principal o POST nueva si no existe) antes de ir a pago — antes solo se guardaba con el botón "Guardar dirección" explícito; (2) factura PDF: logo centrado dentro del chip blanco (y=15, antes 39 que se salía), título "FACTURA DE COMPRA" (16pt para caber en 150px) + `doc.info.Title`, separadores de la tabla de productos (y-9→y-4) y de totales (y-8→y-2) ya no se montan sobre el texto; (3) asunto del correo de checkout: "¡Gracias {nombre} por tu compra! Aquí está tu factura" (antes "🧾 Factura #N") + adjunto `factura-compra-{id}.pdf`.
- Foto de perfil DAÑADA FIX (2026-08-08): causa raíz — Docker Desktop no propaga carpetas/archivos NUEVOS al contenedor de Vite al instante (el `sirv` de Vite cachea el listado de `public/`), así que la foto recién subida devolvía el `index.html` del SPA (200 text/html) → img "dañada" y navbar sin foto. Solución: (1) `backend/server.js` sirve `/images/perfiles` con `express.static` desde `backend/uploads/perfiles` (SIEMPRE al día, sin depender de la sincronía del mount); (2) `frontend/vite.config.ts` proxy `/images/perfiles` → `http://backend:5000` (como `/api`); (3) `authController.subirFotoPerfil` ahora guarda ARCHIVOS PLANOS `perfil-{id}-{ts}.{ext}` en la raíz de `perfiles/` (no subcarpeta `u{id}` — también evita carpetas nuevas). Las fotos viejas en `u2/u7/` siguen sirviéndose (static recursivo). E2E: login admin → POST foto → PUT foto_url (-X PUT) → GET por el frontend 200 image/jpeg INMEDIATO. Advertencia: al cambiar vite.config se usan los `hostnames` Docker (`backend:5000`), funciona con proxy; prueba de movil usa el mismo proxy.
- Estado de envío admin (2026-08-02): `PUT /api/admin/compras/:id/envio` actualiza `ENVIOS.ESTADO_ENVIO` (PENDIENTE, POR_EMPAQUETAR, EMPACADO, EN_CAMINO, ENTREGADO, CANCELADO); AdminOrdenes tiene columna "Envío" con select + badge de estado (colSpan del detalle: 9).
- MiniCartMenu: botón del footer dice "Comprar" (antes "Ver carrito").
- Cancelar pedido: `POST /api/compras/:id/cancelar` (solo COMPLETADA/PENDIENTE → CANCELADA + ESTADO_ENVIO CANCELADO); botón en MisCompras.
- Dirección de pedido: `PUT /api/compras/:id/direccion` actualiza ENVIOS en servidor; MisCompras ya NO usa sessionStorage (`jadda_direcciones_edit` eliminado).
- CompraExitosa: si se refresca (sin location.state) recupera `GET /api/compras/:id` (trae TOTAL, REFERENCIA_PAGO, productos, planGenerado).
- Rating en cards: `GET /api/productos` incluye RATING (AVG RESENAS) y RESENA_COUNT vía subconsultas (no rompen SUM de stock); ProductCard y Catalogo muestran estrellas; badges "AGOTADO"/"¡Solo quedan X!" (umbral 10) según STOCK.
- Planes: `dias_completados` persistidos en `backend/data/planes-progreso.json` (id_plan → array); `misPlanes` devuelve DIAS_COMPLETADOS; archivo `backend/data/` se crea automáticamente (mkdirSync recursive).
- Newsletter: `POST /api/newsletter` persiste en `backend/data/newsletter.json`; input en Footer.
- Otros fixes: Navbar mega-menú usa `?cat=` (antes `?categoria=`); AuthModal sin delay artificial de 2s; guía de tallas modal (zapatos + ropa) en ProductDetailPage si el atributo parece talla; sección "Recientemente vistos" en Principal (API si hay sesión, localStorage si no); ResumenCompra muestra "Solo quedan X" por item y errores de checkout con Swal (antes alert); bug `selectClass(numeroDocumento)` → `selectClass(tipoDocumento)` corregido.
- Fixes 2026-08-02 tarde: (1) `actualizarPerfil` (authController) ahora actualiza SOLO los campos presentes en el body (antes ponía NULL en todos, rompiendo al subir foto — "Column 'NOMBRE_USUARIO' cannot be null"); (2) ResumenCompra: `descuentosMap/costoEnvio/envioCargando` movidos ANTES del reduce de `subtotal` (TDZ → "Cannot access before initialization"); (3) Navbar: timers separados `megaTimer`/`ofertasTimer` + cada menú cierra al otro al abrir (antes al pasar de CATÁLOGO a OFERTAS se cancelaba el timer y el menú de catálogo quedaba abierto).
- Rate limiting en memoria (rateLimiter.js): se reinicia si el contenedor backend se reinicia; suficiente para dev, no para multi-instancia en producción.
- UX verificación 2026-08-08: `AuthModal.tsx` — link siempre visible "¿No verificaste tu correo? Reenviar código" bajo ENTRAR; `volverOrigen` ('register'|'login') para el botón "VOLVER" de verify; cuenta regresiva en tiempo real "El código expira en X min Y s" (localStorage `token_expira_<email>` = 15 min, alineado con `TOKEN_EXPIRA` del backend) en verify Y en forgot-code; cooldown de reenvío = **100 s** en los 4 flujos (registro, reenvío login, reenviar verify, reenviar forgot). Anti-spam multi-capa: 100s UI + `MAX_REINTENTOS=3/30min` por correo + rateLimiter 5/15min por IP + 404 sin envío si no existe.
- Docs verificados y ampliados (2026-08-08): los 49 RFs revisados contra el código real — corregidos los estados de RF-015 (era "Ni implementado"), RF-025/029/031/032/033/034/035/038/039 (estaban "No implementado" pero SÍ implementados); RF-023 → "Implementado parcialmente" (cancelar ya no menciona reembolso ni liberación de stock; solo cambia estados + notifica); RF-029 → "Implementado parcialmente" (sin MOVIMIENTOS_STOCK); RF-007/008 actualizados (filtros y orden son client-side en `Catalogo.tsx`, el backend solo `search` prefix); RF-018 endpoint real `/api/checkout/procesar`; RF-020 proceso real (Nodemailer síncrono con factura adjunta, sin webhook/Celery). **10 RFs nuevos** (RF-040..049): retos deportivos, plan entrenamiento, métodos de pago perfil, notificaciones in-app, login social, foto perfil, subir imágenes admin, newsletter, contacto, historial navegación. **4 RNFs nuevos** (RNF-012..015): no-bloqueo correo, manejo de subidas, BD portátil, RBAC. RNF-002/003/004 → "Implementado parcialmente" con notas exactas de lo que falta. `docs/README.md` actualizado (49 RF, 15 RNF + nota de verificación).
- Rate limiting en memoria (rateLimiter.js): se reinicia si el contenedor backend se reinicia; suficiente para dev, no para multi-instancia en producción.
- Flujo "cuenta sin verificar" (2026-08-08): `authController.js` — login valida bcrypt ANTES de revisar `CONFIRMADO` (401 genérico "Correo o contraseña incorrectos" evita enumeración de correos; solo con contraseña correcta devuelve 403 `{ message: "Debes verificar tu correo", requiereVerificacion: true }`); `reenviar-codigo` devuelve 404 sin enviar si el correo no existe (anti email-bombing; rate limiter 5/15min ya aplicado). Frontend `AuthModal.tsx`: al recibir 403 con `requiereVerificacion` muestra el bloque `.verify-prompt` ("PRIMERO DEBES VERIFICAR TU CUENTA" + "¿Quieres que te enviemos al correo otro código?") con botón "SÍ, ENVIAR CÓDIGO" → `POST /api/auth/reenviar-codigo` + timer 60s (localStorage) + `setView("verify")`, y link "No, luego" (oculta el aviso). Auto-login post-verificación usa `verifyEmail`/`passwordLogin` (no `form.email` — vacío en vista login). Al escribir el correo o cambiar de vista el aviso se limpia. E2E probado: registro → login malo 401 → login bueno 403+flag → reenviar 200 → confirmar 400 código malo / 200 bueno → login 200. Usuario de prueba `prueba.noverif.*@gmail.com` (ID 4) pendiente de borrar en Workbench por el usuario.
- Rate limiting en memoria (rateLimiter.js): se reinicia si el contenedor backend se reinicia; suficiente para dev, no para multi-instancia en producción.
- Todos los RFs implementados (2026-08-06): RF-027 (CRUD categorías admin), RF-033 (devoluciones) y RF-038 (recomendaciones) fueron los últimos en cerrarse. RF-020 (email confirmación pedido), RF-021 (factura PDF), RF-023 (cancelar pedido), RF-025 (notificaciones de estado), RF-030/031 (admin pedidos), RF-032 (reportes de ventas), RF-034 (más vendidos), RF-035 (aviso stock) y RF-037 (compartir redes) ya estaban implementados.
- Imágenes de producto: 44 carpetas `Producto_01..44` en `frontend/public/images/productos/` (3 imágenes c/u, `img_K.ext`, K = ORDEN en BD); subidas del admin van a `frontend/public/images/productos/subidas/`. Mapeo: carpeta `Producto_NN` ↔ `ID_PRODUCTO NN`. Producto 45 no existe (eliminado).
- Subida de imágenes: el backend escribe en `/app/uploads/subidas` (mount de `./frontend/public/images/productos`) — funciona SOLO en Docker (fuera de Docker esa carpeta no existe). Body JSON ≤ 25mb.
- FRONTEND_URL (backend env, default `http://localhost:5173`) se usa en el correo de checkout y en el aviso de stock (imagenParaCorreo en `utils/correo.js`); la app móvil usa `EXPO_PUBLIC_FRONT_URL` (default `http://10.2.178.124:5173`).
- Verificación final frontend tras este cambio: `npx tsc -b` + `npx vite build` OK; backend con `node --check` OK en todos los archivos tocados.

## Relevant Files
- `backend/database/setup.js`: full DB schema (33 tablas: 32 en CREATE_TABLES_RAW + DEVOLUCIONES por migración) + seed data (imágenes locales, sin producto 45) + `MIGRACIONES` (ALTERs/createSql idempotentes: ENVIOS.COSTO_ENVIO, DESCUENTOS.USADO, RETO_EVIDENCIAS, NOTIFICACIONES, AVISOS_STOCK, DEVOLUCIONES, índice único `uq_categoria_nombre` vía INFORMATION_SCHEMA.STATISTICS).
- `backend/database/schema.sql` + `backend/database/exportarSchema.js`: archivo SQL único para Workbench (BD nuevas), regenerado automáticamente desde setup.js (`node backend/database/exportarSchema.js` tras cada cambio de esquema).
- `backend/controllers/devolucionController.js` + `backend/routes/devolucionRoutes.js`: solicitudes de devolución cliente + procesamiento admin con reingreso de stock (RF-033).
- `frontend/src/admin/AdminCategorias.tsx` (`/admin/categorias`): CRUD de categorías (RF-027).
- `frontend/src/admin/AdminDevoluciones.tsx` (`/admin/devoluciones`): revisión y aprobación/rechazo de devoluciones (RF-033).
- `backend/utils/correo.js`: `imagenParaCorreo()` compartido entre checkout y aviso de stock.
- `backend/utils/facturaPdf.js`: generador de factura PDF con pdfkit (RF-021).
- `backend/utils/estadoPedido.js`: `notificarCambioEstado()` email + notificación in-app al cambiar estado pedido/envío (RF-025).
- `backend/controllers/productoController.js`: CRUD productos/variantes/características/reseñas + avisos de stock (RF-035).
- `backend/database/schema.sql`: único archivo SQL — schema completo (33 tablas) + seeds (imágenes locales, sin producto 45), para BD nuevas (importable en MySQL Workbench; generado desde setup.js con exportarSchema.js).
- `backend/controllers/imagenController.js` + `POST /api/productos/imagenes`: subida de imágenes base64 desde el panel admin.
- `frontend/src/admin/SubirImagenes.tsx`: componente de subida de imágenes reutilizable (admin).
- `docs/README.md`: docs index with state table and stack.
- `docs/plan de trabajo.md`: project phases, module status, cronograma.
- `docs/Restricciones del Proyecto.md`: stack, security, dev workflow constraints.
- `docs/diagrams/arquitectura.md`: system architecture + auth/checkout sequence diagrams.
- `docs/diagrams/base-de-datos.md`: ERD + full table structure.
- `docs/diagrams/flujos.md`: registration, purchase, admin, and navigation flow diagrams.
- `docs/RFs/RF-001.md` through `docs/RFs/RF-049.md`: 49 RFs (implementados/parciales, verificación 2026-08-08).
- `docs/HUs/HU-001.md` through `docs/HUs/HU-039.md`: 39 HUs actualizadas (Implementadas).
- `docs/RNFs/RNF-001.md` through `docs/RNFs/RNF-015.md`: 15 RNFs restructured (7 original modules extracted + 4 new + 4 new 2026-08-08).
- `backend/utils/envio.js`: tarifas de envío por departamento (envío gratis ≥ $200.000).
- `backend/controllers/envioController.js` + `backend/routes/envioRoutes.js`: `GET /api/envio/calcular`.
- `backend/controllers/newsletterController.js` + `backend/routes/newsletterRoutes.js`: `POST /api/newsletter` (persiste en `backend/data/newsletter.json`).
- `backend/controllers/comprasController.js`: `obtenerCompras`, `obtenerCompraPorId`, `cancelarCompra`, `actualizarDireccionCompra`.
- `backend/controllers/vendedorController.js` + `backend/routes/vendedorRoutes.js` (montadas en `/api/vendedor`): solicitud de vendedor (POST/GET /solicitud), listado admin (GET /admin) y procesamiento (POST /admin/:id/procesar) con creación de usuario vendedor + credenciales temporales.
- `frontend/src/pages/SerVendedor.tsx` + `frontend/src/css/SerVendedor.css` (`/ser-vendedor`): formulario con políticas, estados de solicitud (APROBADA/RECHAZADA/reenvío).
- `frontend/src/admin/AdminVendedores.tsx` (`/admin/vendedores`): revisión y aprobación/rechazo de solicitudes de vendedor.
- `frontend/src/context/AuthContext.tsx`: `forzarCambioPassword` (Swal obligatorio cuando DEBE_CAMBIAR_PASSWORD=1).
