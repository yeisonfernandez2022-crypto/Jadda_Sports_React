## Goal
Polish UX, complete database portability with auto-setup, and align documentation to real stack.

## Constraints & Preferences
- **(2026-08-25) Suite E2E versionada + docs RF/HU-050..055**: `tests/smoke.mjs` (22 checks: auth 401, vendedor bloqueado 403, cupón exacto JADDA/JADDA-parcial, panel disponibles, checkout mínimo 400/venta+cancel+delete limpieza, no-leidos, pendientes.chats, ES_DE_VENDEDOR+403 admin, UI home/admin/chats/vendedor-reportes sin errores JS) con `npm run test:e2e` en package.json raíz nuevo; playwright-core aislado en tests/node_modules (NO instalar en frontend — pnpm pide purge); login rate-limit es 10/15min por IP → suite reusa sesión por cookie inyectada al browser y reintenta 429 esperando 20s; **detectó bug latente**: eliminarCompra borraba de tabla PLANES inexistente → corregido a PLANES_USUARIO; aserciones con tildes escribir por [IO.File]+UTF8 (consola las corrompe). Docs: RF-050..055 + HU-050..055 creados (chat, escalación, ventas vendedor, reportes, cupones mínimo, regla compra vendedores), README.md badges 55/55, plan de trabajo con actualización 2026-08-25.
- **(2026-08-25) Reportes del vendedor DESCARGABLES**: `utils/reporteVendedorDatos.js` (fuente única: detalle de renglones propios con cliente/variante — OJO faltaba el LEFT JOIN PRODUCTO_VARIANTES pv para pv.COLOR, "Unknown column" — resumen sin canceladas, serie diaria, top 15), `reporteVendedorExcel.js` (exceljs: hojas Ventas/Resumen con fórmulas COUNTIFS+SUMIFS+ROUND(B6/B5)/MasVendidos con SUM totales/Serie con barras REPT), `reporteVendedorPdf.js` (pdfkit: header azul/rojo con empresa+NIT, KPI cards, barras serie con labels cortos $k/$M, tabla top); endpoints `/api/vendedor/reportes/excel|pdf` (esVendedor+rate20); botones blob en toolbar VendedorReportes. Verificado: xlsx 10.4KB firma PK con 6 fórmulas en XML ✓, pdf %PDF ✓, descargas Playwright con nombre correcto, 401 sin sesión.
- **(2026-08-25) Fase 1+C quick wins**: (a) **cupón búsqueda EXACTA** `WHERE TRIM(DESCRIPCION)=?` en validar+checkout (collation ai_ci ya es case-insensitive; "JADDA" parcial ya no matchea); (b) **badge chats sin leer**: `GET /api/chat/no-leidos` (por rol, excluye chats CERRADA) + punto rojo `.nav-chat-badge` en dropdown "Mis chats"/"Chats" (web+móvil) y tab Chats de VendedorNavbar (polling 20s); AdminNavbar tab Chats gana badge `chats` vía obtenerPendientes (=DEVOLUCIONES ESCALADA; devoluciones ahora cuenta SOLICITADA+MAS_PRUEBAS+ESCALADA); (c) **barra envío gratis** `BarraEnvioGratis.tsx` en MiniCartMenu+ResumenCompra — OJO: el umbral REAL es $800.000 (`ENVIO_GRATIS_DESDE` en utils/envio.js), no 200k como decían las notas viejas; CSS .barra-envio* en principal.css. Verificado API (jadda10 lowercase ok, "JADDA" 404, no-leidos=3 tras msg) + Playwright badge dropdown vendedor valor 3, sidebar badge, barra "Te faltan $730.000" fill 9%, 0 errores JS.
- **(2026-08-25) Cupones RETO con compra mínima escalada + condiciones visibles**: columna `DESCUENTOS.MONTO_MINIMO DECIMAL(12,0) NULL` (NULL=sin mínimo; JADDA10 intacto) en CREATE_TABLES_RAW+MIGRACIONES+schema.sql 51 KB; helper compartido `backend/utils/reglasCupones.js` (`montoMinimoSegunPorcentaje`: ≥10%→$200k, 7-9%→$150k, 5-6%→$100k, 3-4%→$50k — el tope coincide con envío gratis) y espejo frontend `utils/reglasCupon.ts` (+`textoCondicionesCupon` que soporta fecha ISO completa); `generarCupon` inserta el mínimo y correo/notificación dicen "· compra mínima $X"; backfill SQL a los RETO- sin usar; `cuponesController.validar` devuelve `monto_minimo`+`fecha_fin`; **checkout rechaza ANTES de la transacción** con "El cupón X requiere una compra mínima de $Y (tu carrito suma $Z)" si subtotal<min (cupón NO se consume); `crearDescuento` admin acepta MONTO_MINIMO opcional; **ResumenCompra** muestra línea "Condiciones: % · mínima $X · un solo uso · vence fecha" + aviso rojo vivo cuando el carrito no alcanza (descuento=0 y no envía cuponCodigo hasta alcanzarlo); MisRetoCard muestra "· compra mínima $X". Verificado E2E API (cupón test 10%/200k): validar expone mínimo ✓, carrito 70k→400 exacto sin consumir ✓, carrito 210k→TOTAL $197.000 (210-21+8) + USADO=1 ✓; UI Playwright condiciones+aviso ✓; datos de prueba eliminados. **(complemento) Panel "Ver cupones disponibles"** en ResumenCompra: endpoint `GET /api/cupones/disponibles` (verificarSesion) devuelve `tienda` (promos públicas vigentes, excluye RETO- y vencidos) + `personales` (RETO- del usuario vía JOIN RETOS_USUARIOS.CUPON_GENERADO, con TITULO del reto, usado/expirado); UI lista en 2 secciones, cada cupón con condiciones (textoCondicionesCupon), aviso "Te faltan $X para alcanzar el mínimo" si no llega, botón Usar que aplica directo, badges "Ya usado"/"Expirado" sin acción; aplicar desde la lista reusa el flujo del warning de mínimo. OJO doble bug de exports en cuponesController (module.exports={} pisando + referencia a identificador inexistente) → crash "argument handler must be a function"; asignar propiedades. Verificado API (admin: tienda=[JADDA10], personales=[], vencida excluida) + Playwright panel/aplicar-desde-lista, 0 errores JS. OJO: el lag del bind mount hizo que dos tests golpearan un proceso viejo/estado contaminado (carrito con restos sumaba sobre el mínimo) — reiniciar backend CON archivos ya sincronizados antes de afirmar fallos.
- **(2026-08-25) Reportes del VENDEDOR** (`/vendedor/reportes`, tab "Reportes" en su sidebar): `reportesVendedor` en vendedorController (`GET /api/vendedor/reportes?desde&hasta`, esVendedor+rate60) — ingresos/unidades/pedidos SOLO de sus productos (JOIN DETALLE_VENTAS→PRODUCTOS p.ID_VENDEDOR=?, excluye CANCELADAS; ingresos = SUM(dv.SUBTOTAL), ticket = ingresos/órdenes), serie diaria para gráfica CSS y top más vendidos con stock; página clon del AdminReportes (presets 7/30/90, KPI cards, reporte-bars, tabla ap-tabla → click edita el producto); sin descargas Excel/PDF. Verificado API (Pepe: $1.554.000, 5 órdenes, top Balón Al Rihla/Guayos, 401 sin sesión, 403 admin) + Playwright KPIs/barras/tabla/preset/tab activa, 0 errores JS.
- **(2026-08-25) Sistema de CHAT (usuario↔vendedor↔admin) + devoluciones de vendedores negociadas en chat con escalación a JADDA.** Ver entrada detallada abajo.
- **(2026-08-25) CompraExitosa con "Ver mi compra" + vendedores gestionan el estado de SUS ventas (admin solo lectura en ajenas).** Ver entrada detallada abajo.
- **(2026-08-25) Descripciones de categorías y productos mejoradas + vendedores NO pueden comprar.** Ver entrada detallada en la sección de trabajo abajo.
- **(CAMBIÓ 2026-08-24)** El asistente SÍ puede aplicar cambios de BD directamente (antes "user runs all SQL manually"); informar siempre qué ejecutó. Para SQL desde PowerShell usar archivos + `docker cp` + `mysql < archivo` (las comillas se pierden al pasar argumentos a docker.exe).
- Build must pass TypeScript and Vite build inside Docker container (`pnpm build`).
- Docker Vite dev server may show stale parse errors; `docker restart jadda_frontend` resolves them.
- Database auto-creates tables + seeds reference data on startup via `setup.js`.
- User uses MySQL Workbench; password prompts remain unresolved.
- `.gitignore` has `mysql_data/` and `backups/` in root and `.env` in backend/ + frontend/.
- Class-based FontAwesome icons (`<i className="fas fa-...">`) used across ~10 files; CSS import kept.
- `.env.example` should contain placeholders, never real credentials.
- Docs structure kept unchanged: `docs/RFs/`, `docs/HUs/`, `docs/RNFs/`. New folders added 2026-08-24: `docs/implantacion/`, `docs/manuales/`, `docs/calidad/`, `docs/aceptacion/`, `scripts/`.
- Proyecto de grado: cumplir Módulo A (despliegue/implantación) y B (calidad) — checklist de 12 ítems del usuario.

- **CHAT usuario↔vendedor↔admin + devoluciones de vendedores con escalación** (2026-08-25): (1) **Tablas nuevas** `CHAT` (TIPO SOPORTE|VENDEDOR|DEVOLUCION, ID_CLIENTE, ID_VENDEDOR, ID_DEVOLUCION, ESTADO ACTIVA|ESCALADA|CERRADA, ULTIMA_ACTIVIDAD) y `CHAT_MENSAJE` (ROL_AUTOR CLIENTE|VENDEDOR|ADMIN|SISTEMA, LEIDO) — en CREATE_TABLES_RAW + MIGRACIONES createSql, schema.sql regenerado (51 KB). (2) **chatController/chatRoutes** (`/api/chat`): `POST /iniciar` (idempotente: recupera chat ACTIVA existente; VENDEDOR exige producto con ID_VENDEDOR; DEVOLUCION valida participación), `GET /conversaciones` (role-aware: admin todas, vendedor las suyas vía VENDEDORES.ID_USUARIO, cliente por ID_CLIENTE; trae ULTIMO_MENSAJE/ROL, NO_LEIDOS por ROL_AUTOR<>mío, empresa y estado devolución), `GET /:id/mensajes` (marca leídos del otro bando), `POST /:id/mensajes` (≤2000 chars, notifica a la contraparte o a todos los admins en SOPORTE — NUNCA bloquea), `POST /:id/escalar` (solo DEVOLUCION ACTIVA, cliente o vendedor → CHAT+DEVOLUCIONES ESCALADA + mensaje SISTEMA + notif a cada admin). OJO bug clásico: `module.exports = {...}` al final PISABA los exports.x anteriores → "argument handler must be a function"; asignar propiedades. (3) **Nuevo flujo devoluciones**: `solicitarDevolucion` detecta productos con ID_VENDEDOR → crea chat DEVOLUCION por vendedor (`chatCtrl.abrirChatDevolucion`, helper exportado que ignora mensaje si ya existe) + notifica al VENDEDOR (no al admin); msg respuesta "Solicitud enviada al vendedor..."; 'ESCALADA' agregada a estados activos anti-duplicado. Admin `procesar`: 403 si producto de vendedor y estado≠ESCALADA ("se gestiona en su chat..."), acepta ESCALADA, cierra el chat con mensaje SISTEMA en decisión final. `agregarEvidencias` MAS_PRUEBAS→SOLICITADA notifica al vendedor (ruta /vendedor/devoluciones) cuando es su producto. TDZ evitado: NO usar variable del destructuring dentro del argumento de la misma llamada. (4) **Vendedor**: `devolucionesVendedor` + `procesarDevolucionVendedor` (devolver=reingreso stock+MOVIMIENTOS_STOCK, reembolsar, mas_pruebas, rechazar c/observación obligatoria; solo SOLICITADA/MAS_PRUEBAS — ESCALADAS son de JADDA); página nueva `/vendedor/devoluciones` (`VendedorDevoluciones.tsx`, tab "Devoluciones" en sidebar) con tarjetas (badge de estado, motivo/descripción/evidencias, 4 botones de decisión, "Ver la conversación"); RECHAZADA del vendedor deja el chat ABIERTO para que el cliente escale (solo APROBADA cierra). (5) **Frontend**: página `Chats.tsx` role-aware (`/chats` ProtectedRoute, `/admin/chats` AdminRoute con tab "Chats" + filtro "solo escaladas", `/vendedor/chats` VendedorRoute): lista con chips tipo/estado/no-leídos + hilo con burbujas (mio azul marino derecha), mensajes SISTEMA centrados, polling 4s hilo /12s lista, botón "⚖️ Escalar a JADDA", input bloqueado en CERRADA; css `chats.css`; accesos: dropdown navbar "Mis chats"/"Chats" (web+móvil), botón "💬 Chatear con el vendedor" en ProductDetailPage (si ID_VENDEDOR), botón chat en DevolucionEstado (ID_CHAT via misDevoluciones JOIN CHAT), timeline con paso "Decisión JADDA" en ESCALADA. `estadoCompra.ts`: ESCALADA → "En decisión de JADDA" morado + cuenta como activa. (6) **E2E API completo verificado** (venta 15, Guayos del vendedor 3): solicitar→chat 2 creado+SOLICITADA ✓, vendedor responde en chat ✓, RECHAZADA c/motivo deja chat ACTIVA ✓, admin sin escalar → 403 exacto ✓, vendedor escala → ESCALADA ✓, admin aprueba → APROBADA + chat CERRADA + stock reingresado ✓. Playwright 7/7 checks 0 errores JS (VendedorDevoluciones renderiza, tab sidebar, chats lista+hilo cerrado bloquea input, admin/chats con filtro, /chats abre hilo, badges). Datos demo dejados: solicitud #16 APROBADA + chat 2 con historial completo. Móvil SIN UI de chat (pendiente). **AJUSTES post-feedback (mismo día)**: (a) el ADMIN ya no puede abrir tickets de SOPORTE (`iniciar` → 400 "El equipo JADDA es el soporte...", botón "Nuevo soporte" oculto en su UI) — él ve y responde los tickets; (b) **filtro de groserías** `backend/utils/groserias.js` aplicado en `enviar` y en el mensaje inicial del chat de devolución: ~70 palabras ofensivas ES/CO censuradas con asteriscos del mismo largo, SOLO palabras completas vía regex `(?<![\p{L}\p{N}])palabra(?![\p{L}\p{N}])` flag 'giu' — "reputacion" NO se censura aunque contenga "puta", insensible a mayúsculas/tildes; se guarda ya censurado en BD. (c) **Hilos separados tras escalación + CTA "Llevarlo con soporte"**: columna `CHAT.PARTE` (NULL=acuerdo cliente↔vendedor, 'CLIENTE'/'VENDEDOR'=hilo privado de esa parte con JADDA; CREATE+MIGRACIONES+schema.sql 51 KB); `escalar` cierra el hilo de acuerdo con mensaje SISTEMA y crea DOS chats ESCALADA (uno con el comprador, otro con el vendedor — "mismo caso, diferentes chats para no confundirse"), notifica a admins (/admin/chats) y a cada parte en su hilo; `chatConAcceso` aisla por PARTE (vendedor → 403 en hilo del comprador y viceversa); `iniciar DEVOLUCION` resuelve el hilo del que pregunta (su PARTE primero, luego acuerdo); `cerrarChatDeDevolucion` cierra TODOS los hilos de la solicitud; admin puede REPROCESAR solicitudes de vendedor si ya tienen hilo PARTE (caso MAS_PRUEBAS→SOLICITADA post-escalación) y `agregarEvidencias` notifica a JADDA en vez del vendedor cuando PARTE existe; `misDevoluciones`/`devolucionesVendedor` resuelven el chat correcto por subconsulta ORDER BY (PARTE=mía) DESC; **DevolucionEstado**: en RECHAZADA sin escalar muestra botón morado "⚖️ Llevarlo con soporte de JADDA" (+ver conversación), si ya escaló muestra "Ver tu conversación con el soporte"; títulos en /admin/chats: "Dev #N · comprador" / "· vendedor". Verificado E2E API (venta 15→solicitud 17): rechazo→escalar genera chats 4 CERRADA/5 CLIENTE/6 VENDEDOR ✓, aislamiento 403 ✓, admin escribe en ambos ✓, decisión final cierra los 3 ✓, PARTE_CHAT=CLIENTE en detalle ✓. UI: títulos comprador/vendedor visibles, CTA oculto tras decisión JADDA, 0 errores JS. (d) **Chats de devolución FUERA de la lista general + embebidos por solicitud** (mismo día): `Chats.tsx` filtra TIPO=DEVOLUCION para no-admins (la lista general queda con Soporte+Vendedores; admin conserva los hilos Dev · comprador/vendedor); nuevo componente compartido **`ChatHilo.tsx`** (hilo con polling 4s + input, reusa clases chats.css, wrapper `.chat-inline`): **DevolucionEstado** y **VendedorDevoluciones** ahora abren EL chat de ESA devolución embebido en la página (toggle mostrar/ocultar) en vez de navegar a /chats; textarea deshabilitado cuando el chat está CERRADA; tras enviar el formulario se aterriza directo al detalle. Verificado Playwright: /vendedor/chats sin devoluciones ✓, tarjeta vendedor abre hilo embebido ✓, envío desde embebido en chat ACTIVA funciona ✓ (en CERRADA el input bloquea — comportamiento correcto), detalle cliente abre hilo JADDA embebido ✓, admin conserva sus hilos ✓. (e) **Rutas de notificaciones de chat por destinatario** (mismo día): `notificarUsuarios` ahora recibe tipoDestinatario y `rutaSegunDestinatario` resuelve admin→`/admin/chats?chat=N`, vendedor→`/vendedor/chats?chat=N`, cliente→`/chats?chat=N` — antes TODOS caían en /chats y el admin salía de su panel al click de la campanita; además Chats.tsx redirige a `/admin/chats` (conservando ?chat=) si un admin aterriza en /chats público. Verificado: rutas en BD por destinatario + redirect Playwright (URL final /admin/chats con sidebar).
- **CompraExitosa "Ver mi compra" + vendedores gestionan SUS ventas / admin solo lectura en ajenas** (2026-08-25): (1) **CompraExitosa**: junto a "Volver al inicio" ahora hay botón secundario **"Ver mi compra"** (`.btn-ver-compra` outline rojo → `/perfil/compra/{id}`, solo con sesión; contenedor `.compra-exitosa-acciones` flex). (2) **Vendedor gestiona el estado de sus ventas como admin**: `ventasVendedor` reescrito con `WHERE EXISTS` + LEFT JOIN ENVIOS (devuelve ESTADO_ENVIO/DIRECCION/CIUDAD/TELEFONO/OBSERVACIONES — OJO: `only_full_group_by` rechazó el primer intento con GROUP BY + columnas de e.*); endpoints nuevos `PUT /api/vendedor/ventas/:id/envio` y `PUT /api/vendedor/ventas/:id/estado` (esVendedor + rate 60) que validan `ventaDelVendedor` (EXISTS de un ítem suyo → 403 "Esta venta no incluye productos tuyos"), actualizan con idempotencia `sinCambios`, setean FECHA_ENTREGA en ENTREGADO y notifican vía `notificarCambioEstado`; `VendedorVentas.tsx` reescrito con el mismo stepper `ao-stepper`/ao-btn-avanzar/ao-btn-cancelar + select de estado venta + dirección del cliente (CSS `.ven-envio-gestion`/`.ven-stepper`). (3) **Admin solo lectura en ventas ajenas**: `obtenerTodasLasCompras` agrega `p.ID_VENDEDOR` al detalle y por venta calcula `ES_DE_VENDEDOR` + `VENDEDORES` (nombres de empresa); `actualizarEstadoCompra`/`actualizarEstadoEnvio` rechazan ventas con productos de vendedor (`ventaConProductosDeVendedor` → 403 "Esta venta incluye productos de un vendedor..."); `AdminOrdenes.tsx`: chip morado `.ao-venta-vendedor` (FaStoreAlt, muestra la empresa), stepper `.solo-lectura` (steps disabled sin onClick), select de estado disabled "(solo lectura)", SIN botones avanzar/cancelar envío, factura PDF sigue disponible — ventas 100% Jadda quedan 100% interactivas. (4) Verificado: node --check ×3, `tsc -b` OK, chunks nuevos AdminOrdenes-B1g0H1Cq/VendedorVentas--vZ08S_8; API: vendor avanza SU venta 15 → 200 + restauración, vendor en venta ajena 18 → 403 exacto, ADMIN en ventas 15 y mixta 12 → 403 exacto, ADMIN en venta Jadda pura 10 → 200; E2E msedge: VendedorVentas 9 tarjetas/5 steppers/avanzar funciona, AdminOrdenes chip+disabled+sin avanzar en venta de vendedor vs fila Jadda interactiva, 0 errores JS. Estados demo restaurados (15/18 PENDIENTE).
- **Principal sin repetidos entre PRODUCTOS y OFERTAS** (2026-08-25): `productosMostrados` en Principal.tsx ahora excluye los productos con `ID_DESCUENTO` (`idsEnOferta = new Set(productosOferta.map(p => p.ID))` → filter) — antes un producto con descuento que estaba entre los más populares aparecía en AMBAS secciones de la home; el fallback sin ofertas (`slice(4,8)`) también sale de la lista filtrada. Verificado E2E msedge: PRODUCTOS (Balón Al Rihla/Espinilleras/Guantes Portero/Camiseta) vs OFERTAS (Guayos/Mancuernas/Ultraboost/Casco Escalada) sin intersección, 4 badges -%, 0 errores JS.
- **Descripciones de categorías y productos mejoradas + vendedores NO pueden comprar** (2026-08-25): (1) **Categorías**: las 15 descripciones genéricas ("Productos relacionados con fútbol") reescritas con copy comercial — UPDATE a la BD viva vía archivo SQL + `docker cp` + `mysql --default-character-set=utf8mb4 <` (tildes OK), seed de CATEGORIAS en setup.js actualizado y schema.sql regenerado (50 KB). (2) **Productos**: `backend/database/contenidos-producto.js` REESCRITO completo (v4) — 44 descripciones limpias (la v3 tenía copy roto tipo "agarra la lees", "espuma extra frigorísima", "capa-läger", "cintillones") y fichas técnicas corregidas; **`original` ahora es ARRAY de textos legados** ([corto del seed, texto v3]) y `asegurarContenidosProducto` en setup.js: si la descripción actual coincide con un legado → UPDATE descripción + DELETE/INSERT de sus PRODUCTO_CARACTERISTICAS (solo en el arranque de mejora; arranques normales vuelven a ser aditivos para respetar ediciones admin); log "44 descripciones actualizadas, 256 características reemplazadas" verificado, 0 frases rotas restantes (`LIKE '%la lees%' OR '%frigor%' OR '%cintillones%' OR '%lager%'`). OJO PowerShell: al escribir un hash bcrypt entre comillas dobles `$2b$10$...` se interpola como variables y lo VACÍA — construir el SQL con `[char]39` o comillas simples. (3) **Vendedores bloqueados de compra** (decisión confirmada por el usuario): guard central en `CartContext.addToCart` web (Swal info "Cuenta de vendedor"), botones con aspecto deshabilitado pero CLICABLES (opacity .5 + cursor not-allowed + title) en `ProductCard`, `Catalogo.tsx` (click directo muestra Swal SIN abrir el modal de variantes), `ProductDetailPage` (botón principal: `disabled={!esVendedor && (...)}` + guard en handleAgregarCarrito) y `Principal.tsx` (helper `abrirAgregarCarrito` que bloquea antes de abrir modal); **backend**: `carritoController.agregarAlCarrito` y `checkoutController.procesarCompra` rechazan `req.user.ID_ROL === 6` con 403 "Los vendedores no pueden comprar en la tienda..." (doble validación API); móvil: mismo guard en `movil/context/CartContext.tsx` (Alert.alert). Verificado: node --check ×4 OK, `tsc -b` frontend OK, tsc móvil OK, rebuild frontend (chunks Catalogo-CewzrSxn/ProductDetailPage-Celvtq7N/Principal-CwEc4hfv), E2E Playwright/msedge: catálogo opacity=0.5+title+cursor not-allowed+click→Swal ✓, detalle igual ✓, admin control sin botones ✓, API vendedor carrito/agregar y checkout → 403 con mensaje exacto, admin agrega al carrito ok=True. Contraseña del vendedor demo Pepe restaurada a Prueba123 (hash regenerado por SQL). NOTA tests: `networkidle` en Playwright ahora es inestable con este app (polling del timbre mantiene la red activa) → usar `waitUntil: "domcontentloaded"` + waitForSelector.
- **Gráficas en los reportes descargables** (2026-08-24, continuación del anterior): (1) `reporteDatos.js` ahora también devuelve la **serie diaria** (día/órdenes/ingresos, excluye CANCELADAS). (2) **PDF**: dos helpers de dibujo vectorial pdfkit — `graficoBarrasVertical` (ejes, líneas guía 25/50/75%, etiquetas de valor en corto $k/$M sobre cada barra, labels de fecha en X) y `graficoRankingHorizontal` (nombre truncado + barra proporcional + valor). El reporte queda: pág.1 = KPIs + "Ingresos por día" + "Pedidos por estado" (barras coloreadas por estado: COMPLETADA verde, PENDIENTE ámbar, CANCELADA roja...), pág.2 = Top 10 más vendidos (gráfica horizontal) + tabla detalle, pág.3 = usuarios + vendedores; pie paginado en todas. (3) **Excel**: nueva hoja **"Graficas"** entre Resumen y Más Vendidos — sección Ingresos por día (Fecha/Ingresos/Órdenes + columna gráfica con fórmula `REPT("|",MAX(1,ROUND(C4/max*45,0)))` en rojo) y sección Top 15 más vendidos (gráfica `REPT("▮",...)` azul), ambas con **formato condicional dataBar** (`cfvo min/max` — OJO exceljs 4.4.0: minLength/maxLength NO es el API, lanza TypeError en DatabarXform.render) y fila Total con SUM(); nota explicando que las barras se redibujan solas al editar datos. (4) Verificado: node --check OK; excel 200 (13.7 KB, 6 hojas, descomprimido: sheet6 con REPT ×7 + `<dataBar>` presente); pdf 200 (%PDF-1.3, creció 5220→6424 B por el vector de gráficas); E2E UI re-descarga ambos formatos con nombre correcto, 0 errores JS.
- **Reportes admin: descarga de reporte en Excel (.xlsx) CON FÓRMULAS + PDF ejecutivo por rango de fechas** (2026-08-24): (1) **Backend** — `pnpm add exceljs@4.4.0` en contenedor (package.json host actualizado vía bind mount; node_modules vive en volumen backend_modules). Nuevo `backend/utils/reporteDatos.js`: fuente única de datos para ambos formatos — ventas COMPLETAS del rango (incluye CANCELADAS para los COUNTIF, con cliente/método/artículos), resumen calculado, más vendidos top 50, usuarios registrados en rango (+compras/gastado del rango) y vendedores con desempeño del período (subquery de productos para evitar fan-out del join); `resolverRango()` exporta defaults desde/hasta idénticos a reporteVentas. `backend/utils/reporteExcel.js` (5 hojas: Ventas detalle / Resumen / Mas Vendidos / Usuarios / Vendedores) — **Resumen 100% con fórmulas**: COUNTIFS+SUMIF excluyendo CANCELADA sobre la hoja Ventas, ROUND(ticket)=ingresos/órdenes, desglose por estado con COUNTIF; filas TOTAL con SUM() en las otras hojas; headers navy #002244, moneda '"$"#,##0', freeze panes. `backend/utils/reportePdf.js` (pdfkit, estilo factura): portada azul/roja, 4 KPI cards, tabla top 15 más vendidos, usuarios nuevos (máx 20 + aviso), vendedores, pie paginado. Endpoints GET `/api/admin/reportes/excel|pdf?desde&hasta` (esAdmin via router.use) → handlers `descargarReporteExcel|Pdf` en adminController con Content-Disposition `reporte-jadda_<desde>_a_<hasta>.ext`. BUG pdfkit: `.strokeAndFill()` NO existe → `.fillAndStroke('#fff','#e2e8f0')`. (2) **Frontend AdminReportes**: botones Excel/PDF en la toolbar (junto a presets 7/30/90) → fetch blob + `<a download>` + Swal éxito/error, estados disabled "Generando...". (3) **Verificación**: node --check ×5 OK, tsc -b OK, rebuild por hash nuevo (AdminReportes-O4lIrCoL); API: excel 200 (11,998 B, firma ZIP PK ✓) y pdf 200 (%PDF-1.3); **fórmulas verificadas descomprimiendo el xlsx**: sheet Resumen contiene `COUNTIFS(Ventas!E2:E12,"<>CANCELADA")`, `SUMIF(...Ventas!G2:G12)`, `ROUND(C6/C5,0)`; Mas Vendidos/Usuarios/Vendedores con SUM() totales; E2E msedge: click Excel→download `reporte-jadda_2026-07-26_a_2026-08-24.xlsx` (11.7KB) + click PDF→download correcto, 0 errores JS.
- **Tema unificado en TODAS las páginas admin** (2026-08-24): eliminadas las últimas 3 tablas Bootstrap (`table table-hover` + `table-responsive` + `table-light`) — **AdminRetos** (grupos No revisados/Ya revisados ahora envuelven `.ap-tabla` con anchos % 24/22/12/18/12/12, fila pendiente `.ad-fila-pend`, botón Ver → `.ad-btn-gestionar` navy), **AdminCategorias** (header simplificado al patrón estándar Volver/Breadcrumb/títulos + NUEVA toolbar `.ap-toolbar` con buscador client-side por nombre/descripción + count `.ap-count` + botón "Nueva categoría" `btn-save-admin`; tabla `.ap-tabla` 8/26/34/14/18 con `.ap-id`, badges y acciones `.ap-btn-accion editar/eliminar` idénticas a Productos), **AdminReportes** (top-10 más vendidos a `.ap-tabla` con `.ap-id`, imagen `.ap-img` y badges de stock `.ap-stock ok/bajo/agotado` — mismos que la tienda admin). **AdminVendedores**: fuera el wrapper `card shadow-sm` (filtros chips ahora btn-dark/outline-dark como Devoluciones + contador ap-count). Utilidades compartidas nuevas al final de adminDashboard.css: `.tb-ellip/.tb-strong/.tb-sub/.tb-sub2/.tb-fecha`. Dashboard conserva su hero propio (es la home del panel). **Verificación E2E msedge 35/35 PASS 0 errores JS**: en las 8 rutas con tabla — tabla estándar presente, cero tablas bootstrap/table-responsive, header estándar (h1+volver+breadcrumb), fondo #f1f5f9 y scroll horizontal 0px; dashboard fondo OK; categorías buscador filtra ("Sin resultados"); retos Ver abre au-modal. Productos verificado aparte (tabla aparece tras click "Gestionar productos", 10 filas, scroll 0px — su check en suite fallaba porque la página arranca en selector de vista, artefacto del test). Chunks nuevos: adminDashboard-DtynXwgm.css, AdminCategorias-BrdmVGs5, AdminReportes-BTOZldqH, AdminRetos-DZoTngom.
- **Margen superior de páginas admin eliminado** (2026-08-24): el `.admin-content` cargaba `margin-top: 56px` heredado de un "top navbar" que YA NO EXISTE (`.admin-top-navbar-tienda` solo queda en CSS, cero usos en TSX) → contenido empezaba a ~76px; y en móvil ≤992px se acumulaban 52px margin + 70px padding = 122px para despejar el toggle fijo (top 14 + alto 42). Fix: eliminado el bloque margin-top completo y móvil ahora `padding-top: 64px` único. Verificado E2E Playwright/msedge 11/11 PASS 0 errores JS: offset del primer hijo de `.admin-content` = **20px** en las 10 rutas admin (/admin, productos, ordenes, usuarios, devoluciones, vendedores, retos, categorias, reportes, configuracion) a 1440px; móvil 375px = 64px exactos con toggle visible. Rebuild verificado por hash CSS nuevo (adminDashboard-D-QbxbrW.css).
- **AdminVendedores: tabla sin scroll + "Gestionar" + rechazo con motivo obligatorio y correo; SerVendedor: éxito con botón Inicio + refresh borra datos** (2026-08-24): (1) **Tabla** reemplazada por `.av-tabla` (table-layout fixed, 6 columnas %: Empresa 25/Solicitante 22/Ubicación 17/Fecha 10/Estado 13/Acción 13, ellipsis+min-width:0, NIT quitado de la tabla — sigue en el modal, `table-responsive` eliminado = cero scroll horizontal verificado E2E delta=0px página y wrapper; <992px oculta Solicitante, <640px también Ubicación). Acción: PENDIENTE → botón navy **"Gestionar"** (`av-btn-gestionar`, abre modal con footer Aprobar/Rechazar); procesadas → icono ojo solo (`av-btn-ver`) abre modal lectura. (2) **Rechazo**: Swal textarea ahora OBLIGATORIO (`inputValidator` trim no vacío + máx 500), confirmación muestra el motivo interpolado; **backend** `procesarSolicitud` valida `RECHAZADA` sin observacion → 400 "Debes indicar el motivo del rechazo" (probado API), guarda `observacion.trim()`, notificación in-app incluye motivo, y **correo de rechazo SIEMPRE** (antes solo a solicitudes sin cuenta): destinatario = EMAIL de la cuenta USUARIOS si ID_USUARIO existe, fallback EMAIL_EMPRESA — con guarda `EMAIL_RE.test()` para no intentar envíos a emails inválidos (hallazgo test: solicitud temporal creada CON sesión admin heredó ID_USUARIO=1 cuyo EMAIL='yeison' → nodemailer "No recipients defined"; comportamiento correcto en producción). Flujo completo probado por API: crear pública 201 → rechazar sin motivo 400 → rechazar con motivo 200 ("El motivo fue enviado al solicitante por correo") → fila temporal 9 ELIMINADA por SQL (docker cp + mysql < archivo). (3) **SerVendedor.tsx**: Swal de éxito ahora con DOS botones [Ir al inicio → navegar("/"), Quedarme aquí]; banner PENDIENTE ganó botón persistente `.sv-btn-inicio` (FaHome, CSS nuevo); **prefill eliminado**: `cargarEstado` ya NO llena el form desde la solicitud guardada — tras refrescar el formulario SIEMPRE arranca vacío (verificado E2E input representante=""). (4) **Verificación**: `node --check` OK, `npx tsc -b` OK, rebuild frontend confirmado por cambio de hash de chunks (AdminVendedores-B5QlyJ0X / SerVendedor-CEwiOzWZ), suite Playwright/msedge propia 8/8 PASS 0 errores JS (scroll 0px, Gestionar/ojo según estado, validación motivo en UI, 400 API, form vacío). NOTA curl: PowerShell desnudó JSON de `-d "{\"a\":1}"` otra vez → siempre archivo+`--data-binary @file`.
- **Fix stock bajo admin + modales admin unificados al tema au-modal** (2026-08-24): (1) **Checkbox "Stock bajo" no hacía nada** — DOS bugs: backend `adminController.obtenerProductosAdmin` ponía `COALESCE(SUM(pv.STOCK),0)<=10` en el WHERE → MySQL "Invalid use of group function" (500 verificado por API); movido a HAVING (solo_jadda queda en WHERE) — API ahora 200 con 5 productos ≤10 unidades. Frontend: `filtroStockBajo` no tenía useEffect que re-consultara (el único dependía de location.search → solo filtraba viniendo del Dashboard con query param); agregado efecto dedicado con guard `primerRenderFiltro` (useRef) para no duplicar el fetch del mount. E2E: check → 10→5 filas, 5/5 badges `.ap-stock.bajo/agotado`, uncheck → vuelve a 10. (2) **Modales admin unificados al tema "Ver usuario" (`au-modal`: blanco, hero gradiente #0f172a→#1e293b, avatar rojo, stats chips, secciones)**: AdminRetos (antes dark evidencia-modal: header/meta/desc → hero+stats progreso/suma/imágenes/videos, secciones Información del envío/Material del avance, OBSERVACION como `.ad-obs-admin`, footer aprobar/rechazar conservado), AdminVendedores (custom-modal bootstrap → au-modal con secciones Solicitante/Empresa y ubicación, imports muertos FaIdCard/FaEnvelope/FaPhoneAlt/FaClock eliminados), AdminCategorias + AdminConfiguracion (formularios custom-modal-container → au-modal compacto con hero FaTags/FaLock). AdminDevoluciones: contenedor cambiado `evidencia-modal ad-modal-claro` → `au-modal` (era un híbrido que parecía la vista de usuario — causa del reporte). CSS: `.au-modal-stats` grid→flex (robusto con 2-4 items), remanentes oscuros a claro (`.evidencia-modal-media-unica` border #e2e8f0, `.evidencia-modal-adjuntos` #f8fafc/#64748b, `.evidencia-modal-btn.rechazar` texto #dc2626). EditarProductoAdmin pp-modal (vista previa réplica de tienda) dejado intacto a propósito. (3) **CAUSA RAÍZ de lo que veía el usuario**: frontend corre MODE=preview (build+serve) y Docker Desktop sincroniza los archivos editados del host CON RETRASO → el rebuild arrancó con fuentes viejas dos veces; verificado comparando chunk servido vs fuente (grep 'Material del avance' en dist vacío mientras src ya lo tenía). Protocolo confiable post-cambios: `docker restart jadda_frontend` + esperar log "built in Xs" + confirmar hash de chunk nuevo (`ls /app/dist/assets | grep AdminProductos`) antes de probar; al navegador hacerle Ctrl+F5. (4) **Verificación**: `npx tsc -b` OK, suite Playwright/msedge propia 14/14 PASS con 0 errores JS (login API→cookies compartidas; devoluciones Gestionar NO navega; retos hero gradiente medido computed-style; categorías/vendedores au-modal; stock bajo filtro ida-y-vuelta), screenshots en %TEMP%\opencode\shots\. Selector correcto del Ver de retos: `button[title="Ver avance"]` (el genérico :has-text("Ver") pega otros botones).
- **Cierre Módulos A+B (implantación + calidad) — 12/12 ítems del checklist** (2026-08-24): (1) **Scripts de backup verificados E2E**: `scripts/backup.ps1` (mysqldump DENTRO del contenedor → `docker cp` byte-perfecto sin BOM, zip SQL + zip de `frontend/public/images` — las subidas viven FUERA del volumen MySQL, rotación `-RetencionDias` 7 días, log `backups/backup.log`; corrida real: dump 111 KB/35 CREATE TABLE + imágenes 43.7 MB/163 archivos) y `scripts/restaurar-backup.ps1` (restauración probada en BD temporal `jadda_restore_test`: 35 tablas reconstruidas y BD eliminada; modo real pide confirmación SI). BUGS de PowerShell resueltos en el camino: (a) PS 5.1 ELIMINA comillas dobles internas al pasar args a docker.exe (`-e "SQL"` llega como `-e SQL`) → todo SQL entre comillas simples de sh vía `[char]39`, password `-p$MYSQL_ROOT_PASSWORD` sin comillas; (b) stderr nativo + `$ErrorActionPreference="Stop"` = NativeCommandError terminante → bajar a Continue alrededor de docker exec; (c) variables construidas ANTES de que existan sus interpolaciones ($destino) quedaban vacías; (d) `CREATE DATABASE IF NOT EXISTS "x"` falla sin ANSI_QUOTES (comillas dobles = literal); conteo de tablas con `USE db; SHOW TABLES` evita literales; (e) Out-File UTF8 añade BOM que puede romper el import → nunca pasar el dump por el pipeline de PS. `.gitignore` raíz ahora `mysql_data/` + `backups/` (Add-Content pegó líneas por falta de \n final — corregido con Set-Content). (2) **Usuario MySQL de menor privilegio**: `scripts/sql/usuarios-produccion.sql` EJECUTADO en la BD real — `jadda_app`@`%` con SELECT,INSERT,UPDATE,DELETE,CREATE,ALTER,INDEX,REFERENCES solo sobre `jadda_sports_db.*` (setup.js necesita CREATE TABLE IF NOT EXISTS + ALTER idempotentes en cada arranque; sin DROP/grants globales), password inicial `J4dd4#App2026` (rotar; para activarlo: DB_USER=jadda_app en compose). Verificado: SHOW GRANTS correcto + login jadda_app + SELECT COUNT USUARIOS=6. (3) **Mediciones RNF reales**: `scripts/mediciones-rnf.ps1` (10 iteraciones + warmup, Stopwatch) → `docs/calidad/resultados-mediciones.md`: GET / 19.7ms, /api/productos 39.6ms (P95 51.2), detalle 23ms, categorías 20.4ms, login POST 91.5ms avg (bcrypt; P95 147ms) con hallazgo positivo: iteraciones 9-10 devolvieron **429** — rate limiter anti fuerza-bruta FUNCIONA en vivo; perfil autenticado 23.7ms. (4) **docs/implantacion/**: 01-preparacion-plataforma.md (specs REALES medidas: Win11 build 26200, i5-11400H 6c/12t, 23.8GB RAM, 124.3GB libres, Docker 29.5.2/Compose v5.1.3, Node v22.23.0/pnpm 11.8.0 en contenedor, MySQL 8.0.46, puertos 3306/5000/5173 escuchando), 02-plan-migracion-respaldos.md (ruta A schema.sql/Workbench vs B setup.js idempotente, política diaria/retención 7d/verificación mensual, DR procedure, RPO 24h, schtasks ejemplo, checklist), 03-guia-despliegue.md (arquitectura ASCII, MODE=preview producción local, tabla dónde vive cada config — credenciales BD en compose NO .env raíz (no existe root/.env.example; backend/.env.example sí), pasos seguridad post-instalación, firewall LAN, HTTPS no aplica en LAN + reverse proxy si se publica, smoke tests curl, troubleshooting 6 filas, log de despliegue). (5) **docs/manuales/**: manual-instalacion.md (7 pasos, obligatoria seguridad post-instalación, actualización/desinstalación con warning down -v), manual-tecnico.md (stack, estructura, 33 tablas agrupadas, MAPA COMPLETO del API extraído del código real con middlewares [sesión/esAdmin/esVendedor/rate], seguridad implementada, convenciones, deuda técnica), manual-usuario-final.md (visitante/cliente/vendedor/admin con tablas "quiero→haz esto"). (6) **docs/calidad/**: marco-calidad.md (ISO/IEC 25010 8 características con evidencia concreta + prácticas PSP mapeadas + CMMI nivel 2 referencial), informe-evaluacion-rnf.md (tabla RNF-001..015 con estado/evidencia: 11 ✅, 3 ⚠️ parciales, 1 🔶 móvil; mediciones §1; recomendaciones MC), informe-evaluacion-calidad.md (resumen ejecutivo apto para entrega, hallazgos positivos incl. 429 demostrado, defectos corregidos muestra, salvedades §5 con mitigación MC-xx, firmas), bitacora-lecciones-aprendidas.md (16 lecciones reales con causa raíz: FileList setTimeout, claves SQL minúsculas, FOR UPDATE+LEFT JOIN, ? sin parámetro en fallback, Docker no propaga archivos nuevos, Vite stale, PS quoting/BOM/EAP, Add-Content sin \n, doble navbar elementFromPoint, z-index jerarquía, Playwright isVisible/montaje + cookies expires:0, INSERT IGNORE no pisa seeds, renombres globales, XSS Swal escapeHtml, tsc -b vs --noEmit), plan-mejora-continua.md (matriz MC-01..MC-10 correctivas/preventivas/proceso con origen/prioridad/responsable/plazo). (7) **docs/aceptacion/**: pruebas-aceptacion.md (35 casos TP-001..TP-035 agrupados A..H derivados de HUs/E2E reales, criterio 100% críticos ≥90% global), acta-entrega.md (plantilla formal: objeto, documentación entregada, exclusiones explícitas — checkout/reseñas móvil y publicación Internet, resultado UAT, capacitación, niveles de servicio, firmas), guia-capacitacion.md (3 sesiones 45/30/60 min cliente/vendedor/admin con temario minuto a minuto + checklist preparación + evaluación práctica). (8) docs/README.md indexado todo lo nuevo + nota de respaldos resueltos; AGENTS.md regla BD CAMBIADA a "asistente SÍ puede aplicar cambios de BD" + esta entrada. **Estado checklist 12 ítems**: 1✅(01) 2✅(02+scripts probados) 3✅(03) 4✅(jadda_app ejecutado+RBAC doc) 5✅(3 manuales) 6🔶(docs listos, falta ejecutar UAT+firmar con el cliente) 7✅(marco) 8✅(RNF medidos) 9✅(informe+bitácora) 10✅(MC-01..10) 11✅(49/49 RF; móvil parcial defendible ~90%) 12✅(Jira/Azure existe — agregar tareas de este cierre). PENDIENTE DEL USUARIO: correr UAT con su cliente, firmar acta, dar la capacitación.
- **Modales admin en BLANCO + devoluciones con todo + multi-evidencias + notificaciones exactas + "y otros N productos"** (2026-08-20): (1) **Modales blancos**: `.evidencia-modal` (compartido AdminDevoluciones + AdminRetos) y `.custom-modal-container`/`.modal-header-admin`/`.admin-input*`/`.admin-select`/`.admin-textarea`/`.modal-footer-admin`/`.btn-cancel`/`.custom-modal-body` en adminDashboard.css pasaron de tema oscuro a claro (#fff/#f8fafc/#e2e8f0, texto #0f172a/#334155, scrollbar claro); `btn-close-white` → `btn-close` en AdminRetos.tsx y AdminCategorias.tsx; `AdminDevoluciones.tsx` modal reescrito estilo `au-modal` (clases au-* reutilizadas): hero gradiente oscuro con avatar inicial/nombre/@usuario·correo/badges tipo+estado, **4 stats** (cantidad, total pedido `au-stat-money`, nº evidencias, antigüedad días), secciones au-seccion "Solicitud" (N° pedido/producto ×cantidad/fecha compra/solicitud/procesada), "Cliente" (nombre/correo/usuario/teléfono), "Pedido y envío" (total, método de pago, dirección completa con FaMapMarkerAlt), "Motivo y descripción" (pre-wrap), observación del admin (`.ad-obs-admin` ámbar), galería de evidencias (videos controls / fotos clic → pestaña), footer con las 4 decisiones; cierre `au-modal-x`, maxWidth 720. (2) **Backend `todas`** (devolucionController.js) enriquecido: USUARIO_LOGIN (u.USUARIO), TELEFONO, FECHA_VENTA, METODO_PAGO (LEFT JOIN METODOS_PAGO mp ON v.ID_METODO = mp.ID_METODO — la tabla VENTAS usa ID_METODO) y ENVIO_DIRECCION/CIUDAD/BARRIO/DEPARTAMENTO/CODIGO_POSTAL (**alias ENVIO_* obligatorios** — el primer intento devolvía las claves sin alias `DIRECCION_ENVIO` y el frontend las esperaba como `ENVIO_DIRECCION` → sección ausente; corregido y re-verificado). (3) **Adjuntar VARIAS evidencias en MAS_PRUEBAS**: backend ya era multi (`upload.array('evidencias', 8)`); el problema era el frontend web — subía al instante al seleccionar y no había feedback. `DevolucionEstado.tsx` ahora: preview con chips (`.de-seleccion-chip` nombre + tamaño MB), botón "Subir N archivos" / "Quitar selección", y **reset del input con `setTimeout(() => { e.target.value = "" }, 0)`** — el reset síncrono en el onChange vaciaba la FileList viva y el estado perdía los archivos (bug real encontrado en E2E: `files en input: 0`); al adjuntar con MAS_PRUEBAS vuelve a SOLICITADA + **notificación global al admin** ("📎 Nuevas evidencias adjuntadas", ruta `/admin/devoluciones`). CSS `.de-seleccion*`/`.rr-btn-secundario` en DevolucionEstado.css. (4) **Notificaciones exactas**: `procesar` (admin) notifica al usuario con ruta `/perfil/devolucion/{ID_VENTA}` (antes `/perfil/compras`) y el correo lleva botón "Ver mi solicitud" al mismo destino — verificado por API (notif 21 con ruta `/perfil/devolucion/3`). (5) **MisCompras**: `.mc-prod-nombres` muestra el primer producto + "y otros N producto(s)" (`.mc-prod-otros` gris, tooltip con todos) en vez de 2 nombres + "+N más". Verificado: `node --check`, `npx tsc -b`, `pnpm build` 7.25s, **Playwright 15/15** (multi-upload E2E con solicitud temporal ID 7 MAS_PRUEBAS → 2 archivos → chips 2 → "Subir 2 archivos" → Swal éxito → BD +2 evidencias + estado SOLICITADA + notif admin; modal devoluciones blanco con hero gradiente/5 secciones/pedido/método/dirección/4 stats; modal retos blanco con título oscuro — grupo "Ya revisados" colapsado por defecto, hay que abrirlo para ver las filas Ver; MisCompras "y otros 1 producto" con DETALLE_VENTAS temporal de 2 productos); datos de prueba limpiados (devoluciones 7/8, evidencias 2-5, notifs 19-21, fila DETALLE_VENTAS temporal, archivos `ev-*.png` en uploads). NOTA E2E: `e.target.value=""` síncrono en onChange de file inputs rompe el FileList (React 19); usar setTimeout. NOTA: las notificaciones viejas de devolución (17/18) conservan ruta `/perfil/compras` (histórico, sin ID_VENTA para migrar).
- **Fix doble navbar (bug raíz: clicks interceptados en el menú móvil)** (2026-08-20): AppLayout renderiza el Navbar global en TODAS las rutas, pero 7 páginas renderizaban el suyo encima (Principal 3 veces: loading/error/main; Catalogo, SobreNosotros, PoliticaPrivacidad, PoliticasDevolucion, PreguntasFrecuentes, TerminosCondiciones) — dos navbars fixed idénticos apilados: el segundo (después en DOM) interceptaba los clicks del primero (el panel móvil quedaba bajo el `bell-container` del navbar duplicado). Fix: eliminados `<Navbar />` e imports de esas 7 páginas (en `/resumencompra` y `/compra-exitosa` el global se oculta, así que el suyo es el único y se mantiene). Verificado: 1 solo navbar en "/", `elementFromPoint` en el click → el link INICIO.
- **Responsive web completo + fix modal devoluciones + movil/.env** (2026-08-20): (1) **Modal "Gestionar" de AdminDevoluciones con texto invisible** — `.evidencia-modal` y `.evidencia-modal-body` con `color: #e5e7eb` (antes heredaban del body claro), `strong`/`.fw-bold` `#f8fafc`, `.text-muted` `#9ca3af !important`, y body del modal con `textAlign: "left"` inline en AdminDevoluciones.tsx. (2) **Navbar móvil**: botón hamburguesa `.nav-hamburguesa` + panel desplegable `.nav-movil-panel` (INICIO/CATÁLOGO/OFERTAS/VENDER, secciones Panel Admin/Mi cuenta, INICIAR SESIÓN/REGISTRO sin sesión, CERRAR SESIÓN con ella) — `@media ≤992px` oculta `.menu-nav-list`/`.auth-buttons-flex`/`.user-info-text`; `@media ≤576px` compacta logo/nombre/búsqueda. (3) **principal.css**: h2/section/slideshow responsive (slideshow-header aspect-ratio 4/3 en móvil, flechas ocultas). (4) **catalogo.css**: banner min-height 130/100px, `.img-container-custom` 150/130px, `.filtros-container` static en móvil (antes absolute → tapaba el grid). (5) **ProductDetail.css**: `.detail-info-box` paddings, `.size-btn` 44px, `.image-zoom-img` max-height 320px, `.envios-info`; **tabs Descripción/Características/Envíos con `flex-wrap`** (los 3 `.btn rounded-0` nowrap desbordaban 9px a 375px — overflow detectado con probe de bounding boxes). (6) **adminDashboard.css**: `.evidencia-modal-footer` flex-wrap (botones 45% <768px, 100% <480px), modal max-height 94vh + body padding. (7) `movil/.env` creado: `EXPO_PUBLIC_API_URL=http://192.168.137.1:5000`, `EXPO_PUBLIC_FRONT_URL=http://192.168.137.1:5173` (gitignored, verificado con `git check-ignore`). Verificado Playwright (Chrome instalado, `channel: "chrome"` — la descarga de Chromium falla: DNS ENOTFOUND en playwright.azureedge.net y el CDN alternativo corta por timeout): **22/22 checks** — móvil 375px (hamburguesa única count=1, panel abre, INICIO, VENDER sin sesión, 0 overflow en home/catálogo/detalle/modal, banner catálogo 100px, sidebar admin oculto + toggle visible, modal texto rgb(229,231,235)/strong rgb(248,250,252)/footer wrap) y desktop 1440px (hamburguesa oculta, menú visible, modal claro + left). Modal probado con fila DEVOLUCIONES temporal (venta 3) creada y eliminada después. NOTA tests: `isVisible()` corre antes del montaje de React → esperar `.navbar`/`.admin-sidebar-toggle` con waitForSelector + timeout; sidebar oculto se verifica con `getBoundingClientRect().right <= 0` (translateX no lo hace invisible para Playwright).
- **Retos: botón "Ver motivo" al rechazar + fix z-index modales sobre navbar** (2026-08-19): (1) `retosAvances.tsx` — la evidencia RECHAZADA con `OBSERVACION` ya NO muestra la caja siempre: ahora hay botón "Ver motivo de la revisión" (id `reto-motivo-{id}`, rojo oscuro) que despliega/oculta la caja "Motivo del rechazo:" (id `reto-motivo-box-{id}`); las APROBADAS con observación siguen mostrando "Comentario del equipo:" directo; sin motivo no sale nada (helper `motivoCaja(esRechazado)` reutilizado). (2) **z-index**: `.evidencia-overlay` subió de 1400 a **10050** (adminDashboard.css) y regla global `.swal2-container { z-index: 10050 !important; }` en principal.css — los modales admin y los Swal nunca quedan debajo del navbar (público 9990 / admin 1040). Verificado con Playwright 1440px: overlay admin z=10050, swal2-container z=10050, evidencia 17 → botón → box none→block con "Motivo del rechazo: La foto no corresponde al reto…", botón cambia a "Ocultar motivo"; evidencia 18 aprobada → "Comentario del equipo: Cumple los requisitos" visible. (Diagnóstico previo: el build actual ya no tenía el bug de modal tras navbar — los z eran correctos — pero los Swal quedaban a 1060 < navbar 9990; fix robusto aplicado igual.)
- **Vendedor informal: EMPRESA y NIT ya NO son obligatorios** (2026-08-19): (1) `vendedorController.js` `solicitarVendedor` — validaciones de empresa (3-150) y NIT (5-20 dígitos) solo si el campo viene; el chequeo de NIT duplicado se salta sin NIT (NULLs no chocan con UNIQUE). (2) **Migración idempotente** en setup.js (patrón IS_NULLABLE→MODIFY): `NOMBRE_EMPRESA` y `NIT` nullable en `SOLICITUDES_VENDEDOR` y `VENDEDORES` (logs "Migración aplicada — NIT nullable en … (vendedor informal)" — las 4 aplicadas al reiniciar); `createSql` de ambas tablas actualizado para BD nuevas. (3) `SerVendedor.tsx` — campos sin `*`, placeholders "…(si vendes de forma informal déjalo vacío)", chip `.sv-opcional` ("(opcional)" gris pill) en SerVendedor.css, política "Datos veraces" reescrita ("Si vendes de forma informal (sin empresa ni NIT), puedes dejar esos campos vacíos"). (4) `AdminVendedores.tsx` — tabla y modal muestran "Vendedor informal" cuando falta empresa y "—" cuando falta NIT. Verificado por API: POST sin empresa/nit → 201 (fila de prueba eliminada de la BD después). `procesarSolicitud` sigue funcionando (slugBase ya tenía fallback 'vendedor').
- **AdminDevoluciones rediseñado: tabla sin scroll + "Gestionar" → modal** (2026-08-19): tabla reemplazada por `.ad-tabla` (table-layout fixed, 100%, wrap `overflow: hidden`, SIN table-responsive) con 6 columnas: Cliente / Pedido / Producto / Tipo / Estado / Acción — eliminadas las columnas "#" y "Cant." (la cantidad va en el producto ×N y la fecha va en el modal); la columna Acción ahora tiene UN botón **"Gestionar"** (`.ad-btn-gestionar` azul marino #002244, FaEye) que abre el modal existente `.evidencia-modal` con todo (cliente, badges tipo/estado, motivo, descripción, tu observación, evidencias, fechas y las 4 decisiones); badges de tipo `.ad-badge-tipo` (Reembolso rojo / Devolución gris), motivo con ellipsis+tooltip, filas resaltadas SOLICITADA (ámbar) / MAS_PRUEBAS (azul); CSS `.ad-*` en adminDashboard.css (responsive: columna Tipo oculta <768px). Verificado Playwright 1440px: headers sin "#", 8 botones Gestionar, tabla sin scroll (1102=1102px), página sin overflow horizontal (0px), modal abre. NOTA build: el `Add-Content` de PowerShell escribió el guion largo "—" de los comentarios CSS como 0x97 ANSI → rolldown "stream did not contain valid UTF-8"; se reemplazaron los 0x97 por "-" (archivo UTF-8 válido de nuevo).
- **AdminUsuarios: compras + retos del usuario en el modal** (2026-08-19): (1) **Backend** `obtenerUsuarioDetalle` (adminController.js) agrega `compras[]` — VENTAS por `ID_CLIENTE` con `mp.NOMBRE_METODO AS METODO_PAGO` (LEFT JOIN METODOS_PAGO — OJO: VENTAS no tiene columna METODO_PAGO, fue el 500 "Unknown column"), ESTADO_ENVIO (LEFT JOIN ENVIOS), TOTAL_ARTICULOS (COUNT subconsulta) y PRODUCTOS_NOMBRES (GROUP_CONCAT de NOMBRE, `|`), LIMIT 200 — y `retos[]` — RETOS_USUARIOS JOIN RETOS con PROGRESO/META_TIPO/META_VALOR/RECOMPENSA_PORCENTAJE/COMPLETADO/CUPON_GENERADO + EVIDENCIAS_PENDIENTES (COUNT subconsulta), LIMIT 200. (2) **Frontend** AdminUsuarios.tsx — para usuarios NO vendedores, dos grupos desplegables tras "Información personal": header clickeable `.au-grupo-titulo` con chevron ▾ que rota (Compras (N) / Retos (N), abiertos por defecto, se resetean al abrir el modal), toolbar `.au-grupo-toolbar` con buscador + select de estado (compras: completada/pendiente/cancelada/enviada·confirmada; retos: en progreso/completados), primeras **5 tarjetas + botón "Ver más (N)"/"Ver menos"** (`.au-btn-vermas`), tarjeta de compra `.au-tarjeta-compra` (Pedido # + badge `.au-badge-estado`, fecha·artículos·método·envío, productos con ellipsis+tooltip, franja total) y tarjeta de reto (título + badge Completado (`.au-badge-si`)/En progreso (`.au-badge-progreso` ámbar), prog/meta·premio%·avances en revisión·cupón, barra `.au-reto-barra` gradiente rojo con % real). CSS `.au-grupo-*`/`.au-tarjeta-compra`/`.au-reto-barra`/`.au-btn-vermas`/`.au-badge-progreso` en adminDashboard.css. Verificado por API (u9 → 7 compras todas Nequi + 1 reto "Estiramiento Diario" 1/10) y Playwright (filtro estado=Completada → 3, búsqueda "guayos" → 1, Ver más → "Ver menos" con 8 tarjetas, barra reto 63.8px ≈ 10%).
- **Retos: el material YA NO se borra al revisar + motivo opcional al rechazar visible para el usuario** (2026-08-19): pedido del usuario — (a) ver lo que envió una vez aprobado/rechazado (antes el material se borraba del disco y quedaba placeholder); (b) el admin puede dar un motivo al rechazar (opcional) y el usuario lo ve. (1) **Backend** `retoController.js`: `aprobarEvidencia` y `rechazarEvidencia` ya NO hacen `RUTA=NULL, RUTAS_EXTRA=NULL` ni llaman `borrarArchivosEvidencia` — el material se conserva en disco (eliminarEvidencia sigue borrando para avances pendientes); ambas aceptan `observacion` opcional del body (trim, máx 500, null si vacía) y la guardan en `OBSERVACION`; la notificación de rechazo incluye `Motivo: ...` cuando existe y el correo muestra caja roja con el motivo (`<strong>Motivo:</strong>`); `misEvidencias` y `adminEvidencias` ahora seleccionan `OBSERVACION`. (2) **Esquema**: columna `OBSERVACION VARCHAR(500) DEFAULT NULL` en `RETO_EVIDENCIAS` (CREATE_TABLES_RAW + createSql de MIGRACIONES + nueva entrada MIGRACIONES `ADD COLUMN` idempotente, auto-aplicada al reiniciar — log "⚙️ Setup: Migración aplicada — columna OBSERVACION agregada a RETO_EVIDENCIAS"); comentarios del bloque RUTA nullable actualizados (ya no es "material borrado tras revisión"); `schema.sql` regenerado (49296 bytes). (3) **AdminRetos.tsx**: Rechazar abre Swal con `input: "textarea"` OPCIONAL ("Puedes indicar un motivo (opcional) que el usuario verá junto a su avance", maxlength 500, sin inputValidator — vacío es válido); ambas acciones mandan JSON `{observacion}` con Content-Type; el modal admin muestra `.evidencia-modal-desc.observacion` (fondo rojo oscuro #2a1214, borde #e73737, texto #ffd5d8) con "Motivo de la revisión:" si existe, antes de la descripción del reto; texto del vacío corregido ("Este avance no tiene material adjunto / fue enviado sin fotos o videos, o su material se eliminó en una revisión anterior"). (4) **retosAvances.tsx** (modal usuario "Mis avances enviados"): placeholder cambiado a "Este avance no tiene material adjunto" (solo para evidencias viejas sin RUTA); si `OBSERVACION` existe se muestra caja bajo los previews — "Motivo del rechazo:" (rojo #3a1517/#ffd5d8) en rechazados o "Comentario del equipo:" (azul #22303f/#b6c8dd) en aprobados (posteriormente convertido en toggle, ver entrada nueva arriba). Verificado E2E por API (login admin email=`yeison` password=`Losquiero7`): evidencia 17 con material → rechazar con observacion → `ok=True`, `RUTA` CONSERVADA + `OBSERVACION` guardada (GET usuario y GET admin la devuelven); evidencia 18 → aprobar con observacion → RUTA conservada, progreso 2→3/5 (Sin cambios: evidencia 14/15 aprobadas ANTES del fix siguen con RUTA null — histórico); ambos archivos servidos 200 image/png por el proxy (localhost:5173/images/usuarios/admin_admin/retos/r10/...). `node --check` OK, `npx tsc -b` exit 0, build 7.00s. NOTA E2E: PowerShell 5.1 desnuda las comillas de `curl -d 'json'` → usar `--data-binary @archivo`. Datos demo dejados: evidencias 17 (rechazada con motivo) y 18 (aprobada con comentario) en reto_usuario 10.
- **Fix "Column 'RUTA' cannot be null" al aprobar/rechazar evidencias** (2026-08-19): el cambio previo ponía `RUTA = NULL, RUTAS_EXTRA = NULL` al aprobar/rechazar (para borrar el material del disco), pero `RETO_EVIDENCIAS.RUTA` se creó `NOT NULL` → la BD rechazaba el UPDATE (ER_BAD_NULL_ERROR en retoController.js:320/413). Fix: (1) `CREATE_TABLES_RAW` y el `createSql` de MIGRACIONES en setup.js ahora definen `RUTA VARCHAR(255) DEFAULT NULL` (BD nuevas); (2) migración idempotente en `migrarTablasExistentes` (mismo patrón IS_NULLABLE→MODIFY que SOLICITUDES_VENDEDOR.ID_USUARIO): recorre `RUTA` y `RUTAS_EXTRA` y si `IS_NULLABLE='NO'` hace `MODIFY COLUMN ... DEFAULT NULL` — auto-aplicada al reiniciar (log: "⚙️ Setup: Migración aplicada — RUTA nullable en RETO_EVIDENCIAS"). `schema.sql` regenerado (49254 bytes). Verificado E2E por API: admin crea evidencia 14 con material → `/api/retos/admin/evidencias/14/aprobar` → `ok=True` (antes 500), `RUTA` queda NULL y el archivo desaparece del disco. La evidencia 13 (la que falló al usuario) sigue pendiente y ya puede aprobarse.
- **Fix scroll horizontal en AdminOrdenes (prohibido)** (2026-08-19): la causa era `.ao-tabla { min-width: 940px }` (forzaba 940px aunque el contenedor fuera menor) + `overflow-x: auto` en `.ap-tabla-wrap`. Ahora `.ao-tabla` es 100% con `table-layout: fixed` y anchos por columna en % (Orden 16 / Cliente 22 / Artículos 9 / Total 13 / Pago 11 / Envío 15 / Acción 14 = 100%) — la tabla SIEMPRE cabe en el contenedor; texto largo (`.ao-orden-id`, `.ao-cliente-nombre`, `.ao-cliente-mail`, `.ao-metodo`) con ellipsis + `min-width: 0`, badges con `max-width:100%` + ellipsis, celdas con `overflow: hidden`; `.admin-grupo-body .ap-tabla-wrap` ahora `overflow: hidden`; media query ≤992px oculta las columnas Artículos y Pago y redistribuye anchos (20/26/18/18/18). Verificado: `npx tsc -b` exit 0, build 6.81s. NOTA: AdminProductos usa el mismo `.ap-tabla-wrap` compartido con su propio min-width? — NO: AdminProductos no tiene min-width propio (solo paginación 10/pág), el scroll que se eliminó aquí era exclusivo de órdenes.
- **Retos: descripción en modal admin + grupos colapsables "No revisados"/"Ya revisados" en admin + botón "Mis retos" arriba en la página de retos** (2026-08-19): (1) **Backend**: `adminEvidencias` (retoController.js) agrega `r.DESCRIPCION AS RETO_DESCRIPCION` al SELECT (verificado por API: login admin → GET /api/retos/admin/evidencias → 4 evidencias con RETO_DESCRIPCION "Corre o camina tu primer kilómetro del reto"). (2) **AdminRetos.tsx**: la tabla única se reemplaza por dos grupos colapsables — **"No revisados"** (ESTADO pendiente, abierto por defecto, FaClock) y **"Ya revisados"** (aprobado/rechazado, cerrado, FaCheckCircle) — cada uno con header clickeable, contador `.admin-grupo-count` (rojo) y chevron que rota; la fila (usuario/reto/progreso/fecha/estado/Ver) y los contadores de pendientes del header se mantienen. **Modal**: nueva sección `.evidencia-modal-desc` (icono FaAlignLeft rojo sobre fondo oscuro #0b1220) que muestra la **descripción del reto** encima de la galería. (3) **AdminOrdenes.tsx**: mismo patrón de acordeón — **"Sin procesar"** (envío NOT IN ENTREGADO/CANCELADO, o NULL→PENDIENTE, abierto por defecto, FaHourglassStart) y **"Procesadas"** (ENTREGADO/CANCELADO, cerrado, FaCheckCircle) con **paginación independiente por grupo** (`paginaSin`/`paginaProc`, POR_PAGINA 10, reset al cambiar filtros); la fila completa + detalle expandido se extrajeron a `renderFila(orden)` y la tabla+paginación a `renderTabla(lista, pagina, setPagina)` (sin duplicar código; eliminados `visibles`/`pagina`/`totalPaginas` globales). (4) **Retos.tsx (usuario)**: la sección "Mis retos" fue **ELIMINADA** (ya existe la página dedicada `/mis-retos`) — `MisRetoCard` y los helpers `verEvidenciasReto`/`reportarAvanceReto` ya no se importan; el botón de retorno se convierte en topbar `.retos-topbar` con **"Mis retos"** a la derecha (`.retos-btn-misretos-top` gradiente rojo, icono FaRunning, contador `.retos-btn-count` con N inscritos, flecha) navegando a `/mis-retos`; los cards inscritos conservan su botón "Ver mis retos". (5) **CSS**: `.admin-grupo*` (card blanca radius 16, header gris #f8fafc, chevron rota 180°, body padding 14) y `.evidencia-modal-desc` en adminDashboard.css; `.retos-topbar`/`.retos-btn-misretos-top`/`.retos-btn-count`/`.retos-btn-flecha` en Retos.css. Verificado: `node --check` OK, `npx tsc -b` exit 0, `docker restart jadda_backend jadda_frontend` → "✓ built in 7.40s", las 4 rutas 200 (`/admin/retos`, `/admin/ordenes`, `/retos`, `/mis-retos`), API de evidencias con descripción. NOTA tsc: hubo `toggleGrupo` duplicado en AdminOrdenes (error TS2451, eliminado el 2º) y `FaTimesCircle`/`revisadas` sin usar en AdminRetos (limpiados).
- **Admin usuarios: registro de ventas del vendedor en el modal** (2026-08-17): `obtenerUsuarioDetalle` (adminController.js) ahora devuelve `ventas[]` para rol 6 — mismas queries que `ventasVendedor` (ventas agrupadas con CLIENTE via LEFT JOIN USUARIOS + ítems con NOMBRE/IMAGEN orden 1/CANTIDAD/SUBTOTAL/variante, `ESTADO <> 'CANCELADA'`, LIMIT 20, `IN (${ph})` con placeholders manuales); `AdminUsuarios.tsx` con sección "Registro de ventas (N)" (solo vendedores): tarjeta por venta con Pedido # (`numeroPedido`), badge estado (`au-badge-estado` coloreado por ESTADO), fecha + cliente, ítems con imagen 40px/nombre/variante/×cantidad/subtotal y Total del pedido; vacío → "Sin ventas registradas"; CSS `.au-lista-ventas`/`.au-tarjeta-venta`/`.au-venta-*` en adminDashboard.css. Verificado por API: vendedor 20 → 5 ventas (pedido 17 COMPLETADA con ítem Balón Al Rihla Pro ×2 $640.000), usuario normal 9 → `ventas: []` y `vendedor: null`. `node --check` OK, `npx tsc -b` exit 0, contenedores reiniciados, proxy 200.
- **Marketplace: panel de vendedor + gestión de productos admin con aprobación** (2026-08-17): (1) **Migración** `PRODUCTOS.ESTADO_PUBLICACION VARCHAR(10) NULL` en setup.js (MIGRACIONES idempotente + CREATE_TABLES_RAW actualizado; NULL=legacy Jadda visible, PENDIENTE/RECHAZADO=oculto, APROBADO=visible). (2) **Backend vendedor**: middleware `esVendedor.js` (sesión + rol 6 + fila en VENDEDORES → `req.vendedor`); `vendedorController.js` + `vendedorRoutes.js`: `GET /api/vendedor/mi-tienda` (stats: productosPublicados/productosPendientes/unidadesVendidas/totalVentas/totalIngresos excluyendo CANCELADAS + ultimasVentas + stockBajo), CRUD `/api/vendedor/productos[/:id]` (crear → ESTADO_PUBLICACION PENDIENTE; editar vuelve a PENDIENTE para re-aprobación; eliminar solo si NO está APROBADO; valida nombre+precio+≥1 imagen), `GET /api/vendedor/ventas`, `PUT /api/vendedor/empresa` (NIT/correo/categorías bloqueados por seguridad). (3) **Backend admin**: `GET /api/admin/productos` (TODOS los productos con `COALESCE(v.NOMBRE_EMPRESA,'JADDA SPORTS') AS VENDEDOR_NOMBRE`, `ID_VENDEDOR`, `ESTADO_PUBLICACION` — el endpoint público ya filtra y NO muestra pendientes/rechazados), `POST /api/admin/productos/:id/aprobar|rechazar` (solo productos con ID_VENDEDOR; rechazo con observación obligatoria; ambas crean notificación al vendedor; `sinCambios` si ya está en ese estado), `obtenerPendientes` ahora devuelve `productos` (COUNT PENDIENTE). (4) **Filtros de visibilidad**: `obtenerProductos`, `obtenerProductoPorId` (ahora con LEFT JOIN VENDEDORES → `VENDEDOR_NOMBRE`), `obtenerRelacionados`, `obtenerRecomendados` (productoController), `historialController`, `newsletterController` y la query de carrito del checkout — todos `(ESTADO_PUBLICACION IS NULL OR ESTADO_PUBLICACION = 'APROBADO')`; `checkoutController` notifica al vendedor tras el commit (`crearNotificacion` con `numeroPedido`, try/catch nunca bloquea); `POST /api/productos/imagenes` abierto con `esAdminOVendedor` (productoRoutes). (5) **Frontend vendedor**: `AuthContext.esVendedor` (ID_ROL===6); `VendedorRoute` + rutas `/vendedor*` en App.tsx; sidebar `VendedorNavbar` (reusa `admin-sidebar*`, acento `ven-*`, activo por `esActivo` — "Mis productos" NO se ilumina en `/productos/nuevo`); páginas `VendedorDashboard` (KPIs + stock bajo + últimas ventas), `VendedorProductos` (tabla con badges Jadda/En revisión/Aprobado/Rechazado + eliminar), `VendedorProductoForm` (nuevo/editar con SubirImagenes + variantes + ficha técnica), `VendedorVentas`, `VendedorEmpresa` (editar teléfono/ubicación/dirección); `css/vendedor.css`; Navbar dropdown: con `esVendedor` muestra sección "Mi tienda" (Panel de vendedor / Mis productos) y oculta "Vender". (6) **Frontend admin**: AdminNavbar con 2 tabs "Gestionar productos" (/admin/productos, badge `productos` pendientes) y "Publicar" (/admin/publicar, `FaPlusSquare`); `AdminProductos.tsx` reescrito → usa `/api/admin/productos`, columnas Vendedor (chip morado con empresa / gris Jadda Sports) y Estado (badges), botones Aprobar (✓ verde) / Rechazar (✕ naranja con Swal de observación) SOLO en productos de vendedor PENDIENTES, filtro por estado, modal de crear RETIRADO; `AdminPublicarProducto.tsx` NUEVO (formulario completo estilo modal viejo: categorías/proveedores/descuentos/variantes/SubirImagenes/ficha técnica → POST `/api/productos` como admin, ID_VENDEDOR NULL = Jadda, visible sin aprobación). (7) **Tienda**: `ProductDetailPage` muestra "Vendido por: {empresa | JADDA SPORTS}" con badge "Vendedor" si hay ID_VENDEDOR. Verificado por API: producto 49 (vendedor 3, APROBADO) → `VENDEDOR_NOMBRE: "Deportes Andinos SAS"` en público y en admin; pendientes `{productos:0}` (demo aprobado); login admin con campo **`email`** (NO `correo` — el 400 "obligatorios" era el campo equivocado; body JSON por archivo `--data-binary @archivo` porque PowerShell 5.1 desnuda las comillas de `-d $var`); `npx tsc -b` exit 0; contenedores reiniciados, proxy 200. Demo en BD: producto 49 "Balón de prueba vendedor" (APROBADO, 8 productos legacy NULL del vendedor 3 visibles).
- **Admin usuarios: vendedores con perfil propio** (2026-08-17): el modal "Ver usuario" ahora distingue rol 6 — (1) **Backend** `obtenerUsuarioDetalle` (adminController.js): si `ID_ROL === 6` busca en `VENDEDORES` (empresa/NIT/email/categorías/ciudad/dirección/fecha) y calcula stats de vendedor: `productosPublicados` (COUNT PRODUCTOS.ID_VENDEDOR), `unidadesVendidas`/`totalVentas`/`totalIngresos` (DETALLE_VENTAS JOIN PRODUCTOS JOIN VENTAS excluyendo CANCELADAS); pone `totalCompras/totalGastado` en 0 (el vendedor no compra); responde `{ ..., vendedor }`. (2) **Esquema**: migración `PRODUCTOS.ID_VENDEDOR INT NULL + KEY idx_productos_vendedor` en setup.js (MIGRACIONES, auto-aplicada) + **migración de datos idempotente** en migrarTablasExistentes: para cada vendedor SIN productos vinculados, vincula hasta 8 productos cuyas categorías coinciden (normaliza tildes) con sus CATEGORIAS — demo: 8 artículos (Fútbol/Ropa deportiva) vinculados al vendedor 3 (Pepe/Deportes Andinos SAS). (3) **Frontend** `AdminUsuarios.tsx`: hero con stats de vendedor (Artículos FaBoxOpen, Unidades vendidas FaBoxes, Ventas FaShoppingCart, Ingresos `au-stat-money`) en vez de Compras/Favoritos/Retos/Gastado, y nueva sección "Información de la empresa" (FaStore: empresa, NIT, correo empresa, categorías, ubicación, teléfono, dirección, aprobado desde). Verificado por API: vendedor 20 → 8 artículos, 6 unidades, 5 ventas, $1.554.000, 0 compras; usuario normal 9 → stats de comprador intactas y `vendedor=null`. `node --check` OK, tsc exit 0, backend/frontend reiniciados (build 8.10s), proxy 200.
- **Límite de 20 caracteres en nombre de usuario** (2026-08-17): backend `actualizarPerfil` ahora valida `^[a-zA-Z0-9._-]{3,20}$` (400 "entre 3 y 20 caracteres"); `vendedorController.generarUsuarioUnico` trunca la base a 14 chars (vendedor_ + 5 + .NNNN = 19 ≤ 20) con fallback de 4 dígitos; frontend `PerfilEditar` input con `maxLength={20}` + slice en onChange + hint actualizado. **Defensa visual** en el modal admin: `.au-modal-usuario` con ellipsis (usuarios largos existentes en BD ya no desbordan hacia las stats).
- **Admin usuarios rediseñado + detalle de usuario + ajustes de Mis Compras/detalle** (2026-08-17): (1) **`AdminUsuarios.tsx` reescrito con clases `au-*`** (adminDashboard.css): tabla SIN scroll horizontal (`table-layout: fixed` + columnas `%`, ellipsis + tooltip — el problema era el `table-responsive` de Bootstrap que forzaba scroll lateral y cortaba el contenido; tampoco se permite scroll horizontal de la página) con SOLO 5 columnas: Usuario (@), Nombre, Correo, Rol (badge), Fecha registro + columna Acciones con botón **"Ver usuario"** (azul marino, FaEye); responsive: <992px oculta Nombre/Fecha, <640px oculta Rol; buscador y header `.au-header` con contador. (2) **Modal `au-modal` de detalle**: hero con gradiente azul marino (avatar inicial, nombre, @usuario, badges rol + CONFIRMADO/verificado, stats en 4 tarjetas con iconos: compras, gastado (`au-stat-money` con $), favoritos, retos), sección "Información personal" (`au-info-grid` con correo/telefono/documento/proveedor/fecha registro/última conexión + aviso ámbar `au-aviso-pass` si DEBE_CAMBIAR_PASSWORD), sección "Direcciones" (`au-lista-dir`/`au-tarjeta-dir` con etiqueta + principal, dirección, ciudad/depto, teléfono; vacío → "Sin direcciones guardadas") y "Métodos de pago" (`au-tarjeta-metodo` con NOMBRE_METODO, titular, banco, tipo, teléfono, principal; vacío → "Sin métodos de pago"). (3) **Backend**: `obtenerUsuarioDetalle` en adminController.js (`GET /api/admin/usuarios/:id`, esAdmin) → `{ usuario, direcciones, metodos, stats }` con subconsultas (totalCompras, totalGastado excluye CANCELADAS, favoritos, retos) — OJO: la tabla real es `USUARIOS_METODOS_PAGO` con PK `ID` (el primer intento usó `METODOS_USUARIO`/`ID_METODO_USUARIO` → 500 "Table 'METODOS_USUARIO' doesn't exist", corregido y re-verificado); ruta en adminRoutes.js. Verificado por API: login admin → GET /usuarios/9 → 200 con usuario completo + 1 dirección (Bogotá, principal=1) + 1 método (Nequi) + stats (7 compras, $652.000, 1 favorito, 1 reto); GET /usuarios/99999 → 404. `npx tsc -b` exit 0, backend/frontend reiniciados, build "✓ built in ~7s". (4) **Ajustes del reboot Mis Compras desplegados**: **factura como descarga blob** (el `window.open` del endpoint abría "una página x" — el endpoint `/api/compras/:id/factura` está OK (200 PDF verificado vía proxy con sesión); ahora axios `responseType:"blob"` + `URL.createObjectURL` + `<a download>` en MisCompras y DetalleCompra, con Swal de error); **"Volver a comprar" con confirmación** (Swal question con lista de productos ×cantidad escapada → `addToCart(id, ID_VARIANTE, cantidad)` por producto → toast "Agregado al carrito" SIN navegar; sin ID_VARIANTE → Swal informativo); **bloqueo de edición de dirección cuando el envío salió** — frontend oculta el botón si ESTADO_ENVIO ∉ (null, PENDIENTE, POR_EMPAQUETAR, EMPACADO) con aviso `.dc-dir-bloqueada` en EN_CAMINO/ENTREGADO, y backend `PUT /api/compras/:id/direccion` → 400 "No puedes editar la dirección porque el pedido ya salió del almacén" si EN_CAMINO/ENTREGADO (además del 400 existente para CANCELADA) — cubre web/móvil/API; **timeline verde completo** en ENTREGADO (`envioCompleto` → todos los pasos "hecho" con ✓, sin naranja); **Mis Compras sin card de total comprado** (pedido del usuario), centrado `max-width:1200px`, grid de 2 columnas (1 <768px), tarjetas compactadas (imagen 72px, total en la meta, botones pequeños) y nombres de producto en `.mc-prod-nombres` (hasta 2 + "+N más", tooltip). (5) **Admin: eliminar compra cancelada** — `eliminarCompra` en adminController.js (transacción, SOLO ventas CANCELADAS: 400 si otro estado; borra DEVOLUCIONES → DETALLE_VENTAS → ENVIOS → PLANES → VENTAS) + `DELETE /api/admin/compras/:id` en adminRoutes.js + botón rojo "Eliminar registro" (`.ao-btn-eliminar`, FaTrashAlt, solo si CANCELADA) con Swal de confirmación en AdminOrdenes. Verificado 400 (COMPLETADA)/404/401. Fix posterior: botón "Ver usuario" se cortaba (celda con `overflow:hidden` + columna 12%) → columna acciones 15%, botón compacto (6px 9px, 0.72rem) y `td.au-col-ver { overflow: visible }`; nombre del hero invisible (regla global de `h2` oscura sobre gradiente) → `color:#fff` explícito en `.au-modal-hero-info h2`.
- **Reboot Mis Compras web + página de detalle de compra** (2026-08-17): (1) `MisCompras.tsx` reescrito con tarjetas compactas: imagen 96px a la izquierda (badge +N si hay más artículos), a la derecha badge `estadoVisible` + meta (fecha/artículos/método pago) + total en rojo, botón **"Ver mi compra"** (azul marino `#002244`) → nueva página; abajo barra de acciones: "Pedir reembolso" (rojo, con Swal de confirmación → `/perfil/devolver/:idVenta`), "Ver estado de reembolso" si devolución activa, "Solicitar reembolso" si CANCELADA, aviso "Plazo de 3 días para reembolso vencido" si `devolucionVencida`, "Cancelar pedido" si COMPLETADA/PENDIENTE y envío no salió, "Factura" (ghost), y **"Volver a comprar"** (amarillo `#ffd166`, agrega al carrito todos los productos con ID_VARIANTE vía `CartContext.addToCart` → navega a `/carrito`); encima: **card resumen con TOTAL COMPRADO** (excluye CANCELADAS, `#ffd166` sobre gradiente azul marino) + **buscador** (por número de pedido, nombre de producto o método de pago) + **filtro por fecha** (desde/hasta) + botón Limpiar filtros; estado vacío con CTA al catálogo. (2) **Nueva página `/perfil/compra/:id`** (`DetalleCompra.tsx` + `DetalleCompra.css` clases `dc-*`, lazy ProtectedRoute en App.tsx, reutiliza CSS `rr-*`): header con volver + "Pedido {numeroPedido}" + badge estado; hero con fecha (incluye "Entregado el …" verde si FECHA_ENTREGA), TOTAL PAGADO y método de pago; **timeline de 5 pasos de envío** (rr-timeline) con texto `estadoEnvioTexto`; dirección de envío con botón **Editar dirección** (formulario inline, PUT `/api/compras/:id/direccion` — se conserva la edición que tenía el desplegable viejo); productos con imagen/nombre/chips de variante/xcantidad/subtotal y botón **Opinar** → `/producto/:id`; totales (subtotal + envío gratis/N) con franja TOTAL; pago con método/referencia/DATOS_PAGO parseados; sección reembolso según estado (ver estado / pedir con Swal / solicitar si CANCELADA / aviso vencido con fecha de entrega); acciones finales Volver + Factura PDF. (3) El desplegable (`comp-detalles`) y los modales viejos de MisCompras quedaron reemplazados por la página. Backend listo desde antes (FECHA_ENTREGA/ID_VARIANTE/REEMBOLSO_ESTADOS en comprasController + regla 3 días en devolucionController + setup.js). Verificado: `npx tsc -b` exit 0, `docker restart jadda_frontend` → "✓ built in 7.00s" + preview, proxy `/api` 200/401 correcto (401 sin sesión esperado).
- **Regla de 3 días post-entrega para devoluciones** (2026-08-17, backend): migración `ENVIOS.FECHA_ENTREGA` (DATETIME NULL) en setup.js (CREATE + MIGRACIONES idempotente, aplicada y verificada con `SHOW COLUMNS FROM ENVIOS`); `adminController.actualizarEstadoEnvio` setea `FECHA_ENTREGA=NOW()` al pasar a ENTREGADO; `solicitarDevolucion` (devolucionController) — si `ESTADO_ENVIO='ENTREGADO'` y `(FECHA_ENTREGA || FECHA_VENTA)` + 3 días < hoy → 400 "El plazo de 3 días para pedir devolución ya venció" (2 queries separadas: VENTAS con FOR UPDATE + SELECT de ENVIOS — MySQL no permite FOR UPDATE con LEFT JOIN nullable). UI: `frontend/src/utils/estadoCompra.ts` nuevo con `DIAS_DEVOLUCION=3`, `DEVOLUCION_ACTIVA=/SOLICITADA|APROBADA|MAS_PRUEBAS/`, `tieneDevolucionActiva`, `estadoVisible`, `estadoEnvioTexto`, `puedeDevolver`, `devolucionVencida`; schema.sql regenerado (49181 bytes), `node --check` OK en todos los archivos tocados.
- **Estado real en Mis Compras + página "estado de devolución"** (2026-08-17): (1) el badge de cada pedido ya NO muestra el ESTADO crudo ("COMPLETADA") — `estadoVisible(compra)` en `MisCompras.tsx` prioriza: devolución APROBADA → "Devolución aprobada" (verde), SOLICITADA/MAS_PRUEBAS → "En proceso de devolución" (azul), luego ESTADO_ENVIO → "Entregada"/"En camino"/"Empacado"/"Por empaquetar"/"Pendiente de envío"/"Envío cancelado" (con `estadoEnvioTexto` corregido: POR_EMPAQUETAR/EMPACADO ya tienen etiqueta, ENVIADO→EN_CAMINO), y por último ESTADO (Completada/Pendiente/Cancelada/Enviada/Confirmada). (2) Si hay devolución activa (`REEMBOLSO_ESTADOS` contiene SOLICITADA/APROBADA/MAS_PRUEBAS), el usuario SIEMPRE ve cómo entrar al estado: botón **"Ver estado de devolución"** en el header del pedido (sin desplegar, `.comp-ver-estado`) + botón **"Ver estado de reembolso"** al desplegar → nueva página `/perfil/devolucion/:idVenta` (`DevolucionEstado.tsx` + `DevolucionEstado.css`, lazy ProtectedRoute; reutiliza CSS rr- de ReembolsoDetalle): header con fecha/pago/total de la venta (`GET /api/compras/:id`), y por cada solicitud (`GET /api/devoluciones` filtrada por ID_VENTA) tarjeta con producto (imagen/nombre/xcantidad), badge TIPO (DEVOLUCIÓN gris / REEMBOLSO rojo), badge estado (En revisión ámbar / Pide más pruebas azul / Aprobada verde / Rechazada rojo), timeline 3 pasos (hecho/activo/rechazado), motivo + descripción + observación del equipo, galería de evidencias (imágenes clic → pestaña, videos con controls), aviso 7 días si SOLICITADA, y si MAS_PRUEBAS → botón "Adjuntar más evidencias" (input multiple → `POST /api/devoluciones/:id/evidencias` → recarga). (3) **Ya NO se puede pedir devolución/reembolso si hay solicitud activa**: el botón "Devolver" por producto y el "Devolver o pedir reembolso" (ENTREGADO) se ocultan con devolución activa, y en CANCELADAS la rama "Ver mi reembolso"/"Solicitar reembolso" pasa a "Ver estado de reembolso" → página nueva (el check de devolución activa va ANTES que el de CANCELADA y que el de ENTREGADO — OJO: hubo un bug real donde la rama ENTREGADO quedó duplicada ANTES de `tieneDevolucionActiva`, mostrando "Devolver o pedir reembolso" en pedidos con devolución activa; el orden correcto del ternario de comp-acciones es: 1) Cancelar pedido si COMPLETADA/PENDIENTE y envío no salió, 2) tieneDevolucionActiva → "Ver estado de reembolso", 3) CANCELADA → "Solicitar reembolso", 4) ENTREGADO → "Devolver o pedir reembolso", 5) null; `tieneReembolsoActivo` eliminado — la página `/perfil/reembolso/:id` queda legacy pero funcional, `obtenerReembolso` no filtra por estado de venta). Móvil: `tieneReembolso` ampliado con MAS_PRUEBAS y el botón "Devolver" se oculta si ya pidió. Nota de depuración: el login por API usa el campo **`password`** (NO `contrasena`) — con `contrasena` responde 400 "Correo y contraseña son obligatorios" (fue el tropiezo al probar con curl/fetch). Verificado por API: venta 15 admin (COMPLETADA/ENTREGADO) + solicitud 13 creada (201, NOMBRE/IMAGEN/TIPO/ESTADO correctos en GET /api/devoluciones, METODO_PAGO en GET /api/compras/15) → solicitud 13 y su notificación eliminadas (quedan 7 filas demo reales: ventas 12/7/11/17 de usuarios). `npx tsc -b` OK, `corepack pnpm exec tsc --noEmit` OK (móvil), `docker restart jadda_frontend` → build "✓ built in 8.54s" + preview OK.
- **Detalle de producto móvil "pro"** (2026-08-16): `movil/app/producto/[id].tsx` mejorado — (1) **barra fija inferior de compra** (precio grande rojo + precio original tachado + botón AÑADIR AL CARRITO siempre visible, con safe-area bottom; reemplaza al botón del contenido); (2) **chips de categoría** (rojo claro) y **marca** (azul marino) sobre el nombre; (3) **rating resumido** (5 estrellas + "4.5 · N reseñas") bajo el nombre cuando hay reseñas; (4) hint itálico "Selecciona color y talla para continuar" si faltan opciones; (5) stepper de cantidad redondeado azul marino (antes bordered blanco); (6) **beneficios en 3 columnas** con iconos en círculos rojo claro y divisores verticales; (7) descripción y características dentro de **tarjetas blancas** (fila final sin borde). Todo el resto intacto (galería zoom, selectores, aviso de reposición, reseñas). Verificado `corepack pnpm exec tsc --noEmit` (exit 0). SIN rebuild de APK (solo JS).
- **BUG RAÍZ foto desaparecía en móvil al re-loguear + defensa móvil** (2026-08-16): el login/socialLogin SIEMPRE devolvían `foto_url: null` — la tabla `USUARIOS` tiene `foto_url` y `telefono` en MINÚSCULAS (así se creó), y `SELECT *` devuelve las claves con el nombre REAL de la columna → `user.FOTO_URL` era undefined (confirmado con `SELECT *` dentro del contenedor: keys `...telefono...foto_url...`). El web nunca se enteraba porque `fetchPerfil()` usa `SELECT FOTO_URL` explícito (mysql2 devuelve la clave como se escribió en la consulta); el móvil confiaba en la respuesta del login → foto siempre null al re-loguear (subida OK porque `refreshPerfil` lee el perfil explícito). Fix: normalizar en `login` y `socialLogin` (`user.FOTO_URL = user.FOTO_URL || user.foto_url || null;` + `TELEFONO`), en las 2 estrategias de `passport.js` (fotoLocal) y en `authRoutes.js` (fallback `foto_url` en los callbacks). Verificado por API: `[LOGIN] yeison → FOTO_URL devuelta: /images/usuarios/admin_admin/perfil/perfil.png` (antes `(null)`). Además se añadió **defensa en el móvil** (igual que el web): `movil/context/AuthContext.tsx` — al arrancar restaura AsyncStorage y LUEGO llama `refreshPerfil()` (verdad del servidor; solo limpia la sesión local si el servidor responde 401/403, un error de red NO la borra), y `login()` dispara `refreshPerfil()` fire-and-forget; `movil/app/(tabs)/perfil.tsx` — fix `setPerfil(res.data.usuario)` (antes `res.data`, por lo que `perfil?.FOTO_URL` era siempre undefined y caía al contexto). Logs nuevos: `[LOGIN]`/`[SOCIAL-LOGIN]` con la FOTO_URL devuelta (para depurar en `docker logs jadda_backend`). Verificado: `node --check` OK, `docker restart jadda_backend`, `corepack pnpm exec tsc --noEmit` exit 0. SIN rebuild de APK (solo JS + backend).
- **Carrito móvil rediseñado + recomendados** (2026-08-16): `movil/app/(tabs)/carrito.tsx` reescrito — (1) **tarjetas de artículo mejoradas**: imagen 96×104 con botón ✕ flotante (círculo rojo sobre la imagen) que abre **modal moderno de confirmación** "¿Eliminar del carrito?" ("¿Seguro que deseas eliminar X?") con CANCELAR / SÍ, ELIMINAR (backdrop oscuro, icono trash en círculo rojo claro, animación fade — NO es el Alert nativo), nombre 2 líneas, chip de variante pill (fondo #f1f5f9), fila de precios `$X c/u` rojo + subtotal del artículo azul marino a la derecha, stepper redondeado azul marino y aviso "¡Solo quedan N!" (ámbar) si stock de variante ≤5; (2) **sección "TE PUEDE GUSTAR"** al pie de la lista (ListFooterComponent, también visible con carrito vacío): mezcla de **favoritos** (`GET /api/favoritos`) → **similares** (`GET /api/productos/recomendados`) → **al azar** (`/api/productos` barajado) hasta completar 10, deduplicados y excluyendo lo que ya está en el carrito; cada card es un `RecomendadoCard` (150px, imagen 110px, chip -X%, AGOTADO, botón rojo de carrito que abre **modal de selección de variante** — color/atributo, stock, cantidad, AÑADIR AL CARRITO — en lugar de agregar la variante por defecto directo) — ya NO usa `ProductoItem` (su `width: "48%"` + altura flexible se desbordaba en el carrusel horizontal y se veía bugueado; además traía su propio modal pesado); `descuentoPorcentaje` desde `GET /api/productos/descuentos` (map ID_DESCUENTO→PORCENTAJE, patrón de catalogo/index); los favoritos se enriquecen con el catálogo completo (`porId`) porque el endpoint no trae STOCK/ID_DESCUENTO/ID_VARIANTE_POR_DEFECTO. Verificado `corepack pnpm exec tsc --noEmit` (exit 0). SIN rebuild de APK (solo JS).
- **Carpetas por usuario** (2026-08-16): estructura nueva `uploads/usuarios/{USUARIO}/` con subcarpetas `perfil/` (foto de perfil) y `retos/r{ID_RETO_USUARIO}/` (evidencias) — aplica a web y móvil (mismo backend). Helper nuevo `backend/utils/carpetaUsuario.js` (`claveDeReq(req)` → USUARIO minúsculas saneado [a-z0-9._-], fallback `u{ID}`; `USUARIOS_DIR`). Cambios: `passport.deserializeUser` ahora selecciona USUARIO; `authController.subirFotoPerfil` → `uploads/usuarios/{clave}/perfil/perfil-{ts}.{ext}` (borra la anterior, nombre único por subida → el caché del teléfono refresca), URL `/images/usuarios/{clave}/perfil/...`; `retoRoutes.js` multer destination → `usuarios/{clave}/retos/r{id}`; `retoController.js` URLs/limpiarArchivos/guardarMaterial con clave + `eliminarEvidencia` soporta ambos prefijos (`/images/usuarios/` y legacy `/images/retos/`); `server.js` sirve `/images/usuarios` con express.static; `vite.config.ts` proxy `/images/usuarios` (server + preview); `docker-compose.yml` mount nuevo `./frontend/public/images/usuarios:/app/uploads/usuarios`; `frontend/public/images/usuarios/.gitkeep`. Migrados: u1 `perfil.png` → `admin_admin/perfil/`, u8 → `yeisonfernandez2022/perfil/`, u9 → `yeisiton922/perfil/`, evidencias 9/11/12 → `{clave}/retos/r{id}/` + FOTO_URL/RUTA actualizados en BD (evidencia 11 sin archivo en disco — ya estaba rota). Verificado: `node --check` OK, `docker compose up -d backend` (mount aplicado), las 5 URLs 200 (image/png, image/jpeg, video/mp4) tanto directo (localhost:5000) como vía proxy (localhost:5173).
- Fix foto de perfil móvil desaparecía al re-login social (2026-08-15): causa raíz — `authController.socialLogin` SOBRESCRIBÍA `FOTO_URL` con la foto de Google/Facebook en CADA login social (`UPDATE USUARIOS SET FOTO_URL = ?` sin condición), pisando la foto que el usuario había subido (`POST /api/auth/foto` + `PUT /api/auth/perfil`): flujo = subir foto (se ve bien) → logout → login con Google → foto reemplazada por el avatar de Google (y si Google no trae foto, queda NULL). Fix: si `FOTO_URL` ya es ruta local (`/images/...`) el login social ya NO la pisa (solo actualiza `AUTH_PROVIDER`); si es NULL o URL externa, la actualiza. Verificado: `node --check` OK, contenedor con el fix (grep esFotoLocal = 2), BD restaurada manualmente (usuario 8: `FOTO_URL = '/images/perfiles/u8/perfil.jpg'`, archivo subido por el usuario el 15/08 8:11 PM sigue en disco) — el usuario verá su foto de nuevo al entrar. Nota: el resto de la cadena de subida está OK (probado E2E por API: login admin → POST /foto → PUT /perfil → GET /perfil devuelve FOTO_URL + archivo en `frontend/public/images/perfiles/u1/perfil.png`).
- Arreglos visuales app móvil (2026-08-15): (1) **Toast "Iniciar sesión"** — nuevo `movil/context/AvisoLoginContext.tsx` (`mostrarAvisoLogin(mensaje)` auto-oculta en 4s) + `movil/components/AvisoLogin.tsx` (toast abajo `bottom: insets.bottom+90`, zIndex 1000, botón INICIAR SESIÓN → `/login`); montado en `app/_layout.tsx` (`AuthProvider > AvisoLoginProvider > CartProvider > FavoritosProvider` + `<AvisoLogin />`); `CartContext.addToCart` y `FavoritosContext.toggleFavorito` ya NO usan `Alert` sin sesión → muestran el toast. (2) **Cards de producto** (`ProductoItem.tsx`): `ID_VARIANTE_POR_DEFECTO` en la interfaz (confirmado en `obtenerProductos`), botón rojo redondo AÑADIR AL CARRITO (icono cart, `disabled` si stock≤0 o sin idVariante), acciones VER DETALLES flex con `marginTop:auto`, imagen 128px, precios/chips compactos. (3) **Categorías carrusel**: en `(tabs)/index.tsx` la sección CATEGORÍAS movida a justo después del banner como `ScrollView horizontal` de tarjetas ancho 100 (icono 46 redondo + nombre), se quitó el grid del fondo (`catWrap`→`catCarrusel`/`catCard` 100px). (4) **Header más compacto** (`Header.tsx`): `paddingTop: insets.top+6`, logo 36, brandName 15, avatar 32, loginBtn py 4/fontSize 11, menuItem py 4/fontSize 12, searchInput py 7, cart icon 20. (5) **Margen superior standalone**: botones "Volver" con `paddingTop:14` fijo quedaban bajo la barra de estado → `useSafeAreaInsets` + `[styles.back, { paddingTop: insets.top + 14 }]` en `PaginaInfo.tsx`, `ayuda-soporte.tsx`, `sobre-nosotros.tsx`, `contacto.tsx`, `pqr.tsx`, `ser-vendedor.tsx` (×2). Verificado `corepack pnpm exec tsc --noEmit` (exit 0). SIN rebuild de APK (solo JS).
- RF-023 cancelar pedido COMPLETO + liberación de stock + RN-010 real (2026-08-13): `comprasController.cancelarCompra` reescrito con **transacción**: (1) SELECT con `FOR UPDATE` de VENTAS + ENVIOS (LEFT JOIN, ESTADO_ENVIO); (2) **RN-010 real**: si `ESTADO_ENVIO` ∈ (`EN_CAMINO`, `ENTREGADO`) → 400 "El pedido ya salió del almacén y no puede cancelarse" (antes solo miraba VENTAS.ESTADO y la UI ocultaba el botón, pero la API lo permitía); (3) **liberación de stock**: lee DETALLE_VENTAS con ID_VARIANTE NOT NULL y hace `STOCK + CANTIDAD` por variante + `registrarMovimientoStock(ENTRADA)` dentro de la transacción (rollback revierte todo); (4) UPDATE estados VENTAS/ENVIOS; commit; notificación tras el commit con try/catch (nunca bloquea). El reembolso sigue siendo semi-manual (botón → DEVOLUCIONES → admin aprueba en /admin/devoluciones) — documentado como tal en RF-023.md → **Implementado** (antes "parcial": no liberaba stock — inconsistencia creada por RF-029 que ya decrementa en checkout — y RN-010 no se validaba en backend). NOTA: ventas viejas (antes del 2026-08-13) nunca decrementaron stock, así que su cancelación reingresa unidades no descontadas (aceptado, verificado en RF-023.md). Verificado E2E por API con admin: checkout 2× variante 1 (stock 6→4, mov SALIDA 2) → cancelar (stock 4→6, mov ENTRADA 2, notificación creada); checkout 1× variante 1 → admin set EN_CAMINO → cancelar → 400 RN-010 → admin restaura PENDIENTE → cancelar 200 (stock restaurado). Venta 7 (demo, con devolución aprobada) quedó intacta.
- RF-029 inventario detallado COMPLETO + fix registro sin dirección (2026-08-13): (1) `backend/utils/movimientosStock.js` (nuevo) — helper `registrarMovimientoStock({conn?, idProducto, tipo, cantidad})` inserta en MOVIMIENTOS_STOCK (tabla ya existía, SIN cambios de esquema) con TIPO_MOVIMIENTO mayúsculas y FECHA CURDATE(); nunca lanza (no rompe checkout/edición/devolución). (2) **checkoutController.js**: el comentario "Pendiente de habilitar tras pruebas" queda atrás — dentro de la transacción, por cada item con ID_VARIANTE hace `UPDATE PRODUCTO_VARIANTES SET STOCK = STOCK - ?` + registra SALIDA; el rollback revierte ambos (verificado: stock variante 16 20→17 al comprar 3). (3) **productoController.js**: `actualizarVariante` ahora lee también ID_PRODUCTO en el SELECT previo y registra ENTRADA/SALIDA con la diferencia (`Math.abs`) si el stock cambió; `agregarVariante` registra ENTRADA si nace con stock > 0. (4) **devolucionController.js**: al APROBAR se registra ENTRADA con sol.CANTIDAD dentro de la transacción. (5) **Fix bug preexistente en registro** (authController.registro): `direccion.split(',')` explotaba con 500 SI el frontend no enviaba direccion tras haber insertado el usuario — ahora la fila DIRECCIONES solo se crea si `direccion` viene (el usuario ENTRADA quedaba huérfano+500 antes). Verificado E2E por API con admin: PUT variante 1 stock 6→9 (mov ENTRADA 3) → 9→7 (SALIDA 2) → restaurado 6 (SALIDA 1); checkout de 3 uds variante 16 (mov SALIDA 3) → devolución aprobada (mov ENTRADA 3, stock vuelve a 20); RF-029.md → Implementado con la RUTA REAL corregida (`PUT /api/productos/variantes/:idVariante`, el doc decía `/api/productos/:id/variantes/:idVariante` que NO existe). Datos de prueba dejados como demo: venta 7 (admin, 3× producto 2, Nequi), devolución 3 APROBADA, usuario `movtest.rf029@gmail.com` (sin confirmar). NOTA: cancelar pedido sigue SIN reingreso de stock (decisión RF-023).
- Newsletter automática + desuscripción + envío manual admin (2026-08-13): antes el newsletter SOLO guardaba el correo en `data/newsletter.json` — nunca se enviaba nada. Ahora (1) **scheduler** en `server.js` (setInterval cada `NEWSLETTER_INTERVAL_HORAS`, default 72 h, configurable en `.env`): `enviarNewsletterAhora()` consulta **ofertas reales** de la BD (6 productos con `ID_DESCUENTO` activo, `ORDER BY RAND()`, imagen embebida con `imagenComoDataUri`, precio tachado + final `es-CO` + chip `-X%`), elige **al azar** entre 4 plantillas (💪 ofertas / 🏆 mejor página deportiva / ⚽ novedades / 🔥 ofertas) con pitch de marca ("somos la mejor página para comprar artículos deportivos"), CTA al `?descuento=true` y pie con envío gratis $200.000 + link de desuscripción; nunca lanza (errores SMTP capturados correo a correo, delay 150 ms); historial en `data/newsletter-estado.json` (último envío + total acumulado). (2) **Desuscripción**: `POST /api/newsletter/desuscribir` (body) y `GET /api/newsletter/desuscribir?email=` (página HTML de confirmación servida por el propio backend — el link va en el correo); (3) **Admin**: `GET /api/newsletter/suscritos` (lista + estado de envíos) y `POST /api/newsletter/enviar` (dispara envío manual ahora) — ambos con `esAdmin`; (4) `frontend/vite.config.ts` ganó `preview.proxy` igual al `server.proxy` (el link de desuscripción `/api/newsletter/...` funciona en modo preview `MODE=preview`); (5) `backend/.env.example` creado con placeholders (incluye `NEWSLETTER_INTERVAL_HORAS`). RF-047.md actualizado (proceso con auto-envío + 5 endpoints). Sin cambios de esquema.
- "Ser vendedor" completo + fix timbre marca-1 (2026-08-09): (1) **Fix timbre de notificaciones**: el bug "abrir el timbre marcaba todo como leído" eran DOS bugs en `notificacionController.js` — (a) `filtroUsuario` generaba el where SIN paréntesis (`ID_USUARIO IS NULL OR ID_USUARIO = ?`) y al concatenar `AND LEIDA`/`AND ID_NOTIFICACION` la precedencia rompía el filtro; ahora `(ID_USUARIO IS NULL OR ID_USUARIO = ?)`; (b) `marcarLeida` pasaba los params INVERTIDOS: SQL `UPDATE ... WHERE ID_NOTIFICACION = ? AND ${where}` pero params `[...params, req.params.id]` → ahora `[req.params.id, ...params]` (el `?` de ID_NOTIFICACION es el 1º). Verificado en BD con notifs TIMBRE-TEST: marcar la #48 (global) como admin → solo #48 LEIDA=1, #47 y #49 quedan en 0; el contador vuelve a 1 al llegar notificaciones nuevas. (2) **Ser vendedor backend**: tablas `SOLICITUDES_VENDEDOR` (ID_SOLICITUD, ID_USUARIO, EMPRESA, NIT UNIQUE, REPRESENTANTE, EMAIL_EMPRESA UNIQUE, TELEFONO, DEPARTAMENTO, CIUDAD, DIRECCION, CATEGORIAS, DESCRIPCION, ESTADO PENDIENTE|APROBADA|RECHAZADA, OBSERVACION_ADMIN, FECHA_SOLICITUD, FECHA_PROCESADA) y `VENDEDORES` (ID_VENDEDOR PK auto, ID_SOLICITUD, ID_USUARIO, EMPRESA, NIT UNIQUE, EMAIL_EMPRESA UNIQUE, FECHA_APROBACION) en setup.js + rol 6 'Vendedor' — NOTA: el runner de MIGRACIONES no puede insertar el rol (chequea que la columna exista, no los datos) → `INSERT IGNORE INTO ROLES (6, 'Vendedor', ...)` dedicado en `migrarTablasExistentes` (verificado en BD). Columnas nuevas en USUARIOS: `DEBE_CAMBIAR_PASSWORD` (TINYINT default 0). (3) **vendedorController.js + vendedorRoutes.js** (montado en server.js `/api/vendedor`): `POST /solicitud` (usuario; validación: empresa 3-100, NIT 5-20 dígitos, email empresa formato, teléfono 7-10 dígitos, departamento/ciudad/dirección obligatorios; bloquea si ya existe solicitud PENDIENTE/APROBADA → 400 "Ya tienes una solicitud en revisión"; NIT/EMAIL_EMPRESA duplicados → 409; inserta con ESTADO PENDIENTE), `GET /solicitud` (mi solicitud + `vendedor` asociado si existe), `GET /admin` (esAdmin, lista con solicitante), `POST /admin/:id/procesar` (esAdmin, transacción con FOR UPDATE: APROBADA → crea USUARIO vendedor con EMAIL=email de empresa (404 si ya existe), USUARIO único `vendedor_<slug empresa>.<4dígitos>`, ID_ROL=6, CONFIRMADO=1, DEBE_CAMBIAR_PASSWORD=1, password temporal 10 chars bcrypt; inserta VENDEDORES; respuesta NO devuelve la contraseña; credenciales por correo vía plantillaCorreo (async, no bloquea); notificación in-app al solicitante ("¡Felicidades! Tu cuenta de vendedor está activa") y al nuevo vendedor; RECHAZADA → OBSERVACION_ADMIN + ESTADO RECHAZADA + notificación; RECHAZADA permite reenvío (misma fila → PENDIENTE); doble proceso → 400 "Esta solicitud ya fue procesada"; rateLimit 30/min). (4) **auth/seguridad**: login y obtenerPerfil devuelven `DEBE_CAMBIAR_PASSWORD` (0/1); `seguridadController.cambiarPassword` ahora hace `UPDATE USUARIOS SET CONTRASENA = ?, DEBE_CAMBIAR_PASSWORD = 0`. (5) **Admin**: `adminController.obtenerPendientes` suma `vendedores` (COUNT SOLICITUDES_VENDEDOR PENDIENTE); `obtenerDashboard` agrega `vendedoresPend`; `AdminVendedores.tsx` (`/admin/vendedores`, lazy AdminRoute, tab "Vendedores" en AdminNavbar con badge de pendientes): tabla con empresa/NIT/solicitante/fecha/badge estado, filtro por estado, modal detalle (datos completos + categorías), botones Aprobar (Swal confirmación) / Rechazar (Swal con input de observación); badges `.av-badge-*` en adminDashboard.css. (6) **Frontend usuario**: Navbar dropdown → item **"Ser vendedor"** (FaStore) entre "Planes" y "Ayuda y Soporte"; `SerVendedor.tsx` + `SerVendedor.css` (`/ser-vendedor`, lazy ProtectedRoute): banner con políticas de vendedor (Colombia — incluye "tu cuenta puede ser eliminada si incumples las políticas"), formulario completo con los 9 campos, checkbox obligatorio de aceptación, card de éxito "¡Solicitud enviada! Nuestro equipo verificará tu información en un máximo de 48 horas"; si ya hay solicitud → card de estado (APROBADA muestra "Cuenta de vendedor activa" + correo de acceso; RECHAZADA muestra banner rojo con la observación + formulario para reenviar). **AJUSTE POST-E2E (2026-08-09)**: `/ser-vendedor` YA NO usa ProtectedRoute (el usuario puede ver el formulario sin login; SerVendedor ya manejaba el 401 mostrando el formulario); el navbar muestra item **"VENDER"** en el menú (`.nav-item-vendedor`, al lado de OFERTAS) SOLO cuando NO hay sesión (`{!usuarioLogueado && ...}`), y con sesión se oculta del menú y sigue en el dropdown como **"Vender"**. **SIN LOGIN COMPLETO (2026-08-09)**: el formulario de vendedor ya no exige cuenta — `SOLICITUDES_VENDEDOR.ID_USUARIO` es NULL (createSql + migración idempotente en setup.js vía IS_NULLABLE), `POST /api/vendedor/solicitud` sin `verificarSesion` (solo rateLimit; duplicados por NIT/email con `(? IS NULL OR ID_USUARIO <> ?)`); sin sesión no hay notificación in-app (solo correo): al RECHAZAR solicitudes sin usuario se envía correo de rechazo con la observación (`enviarRechazo` en vendedorController); `obtenerSolicitudes` admin con LEFT JOIN a USUARIOS → `SOLICITANTE_NOMBRE` null se muestra "Sin cuenta (formulario público)" en AdminVendedores. Verificado: POST sin sesión 201, lista admin con solicitante null, rechazo 200, duplicado NIT 409, validación 400; UI Playwright sin sesión formulario visible + envío + Swal éxito 0 errores JS; suite vendedores 23/23. **Dato de test**: el doble navbar (AppLayout renderiza Navbar global + Principal/Catalogo/etc. renderizan el suyo) hace que Playwright vea 2 navbars superpuestos → los clicks requieren `{ force: true }`; en este test el force no navegaba porque ProtectedRoute redirigía a "/" (401 sin sesión). **Cookies Playwright**: `expires: 0` en el jar → Playwright la trata como expirada (401) → inyectar `sameSite: 'Lax'` y omitir `expires` cuando vale 0 (aplicado en `cookieJar` de test_vendedores.mjs y parseJar de test_nav_vendedor.mjs). (7) **Swal cambio de contraseña forzado**: `AuthContext.tsx` — `forzarCambioPassword` (inputs Swal pass actual/nueva/confirmar, llama `/api/auth/cambiar-password`, error con Swal, si descarta → logout; éxito → refreshPerfil + toast); se dispara vía `forzarCambioPasswordRef` en `fetchPerfil` y `login` cuando `DEBE_CAMBIAR_PASSWORD`=1. Verificado: `node --check` todos los archivos tocados OK, `npx tsc -b` OK, schema.sql regenerado (49000 bytes), contenedores reiniciados. **E2E Playwright 1440px (playwright 1.48.2)**: Test A (solicitud aprobada: card visible, correo de acceso, usuario vendedor, nota de contraseña temporal flag=1 — 4/4), B (rechazada: banner + observación + formulario reenvío + política eliminación + checkbox — 5/5), C (admin: título, 2 filas, badges APROBADA/RECHAZADA, modal detalle con NIT y categorías, filtro pendientes vacío — 7/7), D (dropdown: item "Ser vendedor" arriba de "Ayuda y Soporte", navega a /ser-vendedor — 3/3), E (login vendedor con temporal ClaveTemporalX → Swal "Debes cambiar tu contraseña" → nueva MiNuevaClave123 → "Contraseña actualizada" → sesión activa — 4/4). 0 errores JS/red (solo 401 esperado al cargar sin sesión). NOTA tests: click en `.user-profile-trigger`/`.btn-login-new` requiere `{ force: true }` (un overlay del menú móvil intercepta el hit-test). **NOTA re-ejecución suite**: el Test E dejó al vendedor con password `MiNuevaClave123` y flag=0 → para re-correr hay que re-aplicar `set_pass_vendor.js` (restaura ClaveTemporalX + flag=1) y el A4/E dependen de ese estado. Usuarios demo en BD: Pepe (prueba.usuario.vendedor@test.com / Prueba123, solicitud "Deportes Andinos SAS" APROBADA, vendedor con email prueba.vendedor@test.com, NIT 901123456) y Pedro (prueba.pedro@test.com / Prueba123, solicitud "Textiles Norte Ltda" RECHAZADA "NIT no verificable en la DIAN"). Credenciales admin: yeison/Losquiero7. SIN cambios que requieran setup.js manual (migraciones idempotentes aplicadas al reiniciar jadda_backend).
- Email con productos + anti doble-clic + reembolso con botón + página de detalle + timbre admin abre (2026-08-09): (1) **Email de estado con nombres de productos**: `estadoPedido.js` consulta DETALLE_VENTAS JOIN PRODUCTOS (LIMIT 10) y agrega al email la tabla "Productos de tu pedido" (`N. nombre ×cantidad`, texto escapado con helper `escapar()` — evita inyección de HTML por nombres con `<`/`>`), más "Referencia de pago" y "Total" formateado es-CO. Verificado capturando el mail (patched `mailer.sendMail` en contenedor): TO yeisiton922@gmail.com, HTML con "1. Malla porta balones ×1, 2. Espinilleras ×1, 3. Tenis Lebron Witness ×1" + referencia SIM_1786230238198_9 + $588.000. (2) **Idempotencia anti emails duplicados**: `adminController.actualizarEstadoCompra` y `actualizarEstadoEnvio` leen el estado ACTUAL primero; si es igual al pedido → responden `{ ok: true, sinCambios: true }` SIN UPDATE y SIN `notificarCambioEstado` (ya no se envían N emails al repetir "Por empaquetar"). En `AdminOrdenes.tsx` nuevo estado `procesandoEnvio` (number|null): guard en `cambiarEstadoEnvio` (`if (procesandoEnvio !== null) return`), set/clear en try/finally, stepper/avanzar/cancelar-envío con `disabled` mientras hay petición (`.ao-btn-avanzar/.ao-btn-cancelar/.ao-step` disabled con opacity 0.55 + cursor not-allowed en adminDashboard.css), y el Swal success se omite si `data.sinCambios`. Verificado por API: PUT POR_EMPAQUETAR → 200 + 1 notificación; repetido → `sinCambios:true` y 0 notificaciones nuevas. (3) **Reembolso por BOTÓN (no prompt automático)**: tras cancelar un pedido YA NO se pregunta automáticamente por reembolso — en el detalle del pedido CANCELADO aparece botón **"Solicitar reembolso"** (FaWallet, `.comp-acciones`) → Swal aviso "se reembolsará en un máximo de 7 días al método de pago con el que compraste" + monto → `POST /api/compras/:id/reembolso` (transacción con FOR UPDATE, solo ventas propias CANCELADAS, 400 "Ya solicitaste un reembolso" si existe DEVOLUCIONES SOLICITADA/APROBADA, inserta 1 fila DEVOLUCIONES por producto con MOTIVO 'Reembolso por cancelación del pedido' y ESTADO SOLICITADA) → Swal "REEMBOLSO SOLICITADO" con botones "Ver mi reembolso"/"Cerrar". El botón del pedido cambia a **"Ver mi reembolso"** (cuando `REEMBOLSO_ESTADOS` de `obtenerCompras` contiene SOLICITADA/APROBADA — subconsulta GROUP_CONCAT de DEVOLUCIONES agregada al SELECT). (4) **Página `/perfil/reembolso/:id`** (`ReembolsoDetalle.tsx` + `ReembolsoDetalle.css`, clases `rr-*`, ruta lazy en App.tsx): consume `GET /api/compras/:id/reembolso` (nuevo en comprasController.obtenerReembolso + comprasRoutes: datos venta + reembolsos con NOMBRE/IMAGEN/PRECIO_UNITARIO vía JOIN a DETALLE_VENTAS y `totalReembolso` calculado) — badge de estado EN REVISIÓN/APROBADO/RECHAZADO, monto a devolver, **aviso 7 días con fecha estimada** (FECHA_CREACION + 7d) si SOLICITADA, timeline 3 pasos (Solicitud recibida → En revisión → Reembolsado), información del pedido (método/referencia), productos del reembolso con subtotales, franja "Total a reembolsar" y botón Factura PDF. (5) **Admin**: AdminDevoluciones renombrado "Devoluciones y reembolsos" con **filtro por tipo** (chips Todos/Reembolsos/Devoluciones), badge rojo REEMBOLSO (FaWallet) o gris DEVOLUCIÓN en la columna Tipo/Motivo, y el Swal de aprobar dice "el dinero se reembolsará al cliente en un máximo de 7 días" para reembolsos. (6) **Timbre admin no abría**: `BellNotificaciones` en el pie del sidebar (`.admin-sidebar-actions .bell-container`, izquierda x=14) — el `.bell-panel` heredaba `right: 0` y abría hacia abajo: quedaba fuera de pantalla (x=-316). Fix en Navbar.css: `.bell-container.oscuro .bell-panel` abre HACIA ARRIBA (`top: auto; bottom: calc(100% + 14px)`) y anclado a la izquierda (`left: 0; right: auto`) → panel 370px dentro del viewport; se requirió `docker restart jadda_frontend` (CSS stale de Vite). Verificado con Playwright 1440px, 0 errores JS: panel dentro del viewport, 17 items, click en item → navega a /perfil/compras; flujo UI completo MisCompras: cancelar → solo "PEDIDO CANCELADO" (SIN prompt), botón "Solicitar reembolso" → aviso 7 días → "REEMBOLSO SOLICITADO" + "Ver mi reembolso" → navega a /perfil/reembolso/15 (EN REVISIÓN, $198.000, fecha estimada domingo 16 ago 2026, 3 pasos, 1 producto) → botón cambia a "Ver mi reembolso" → admin filtro Reembolsos (4) con badge REEMBOLSO y fila #15. `npx tsc -b` OK. SIN cambios de esquema (no requiere setup.js ni schema.sql). Nota: el usuario dejó su propio demo en BD (venta 12 CANCELADA con reembolso SOLICITADA x3 productos — visible en su nueva página /perfil/reembolso/12 y en AdminDevoluciones); la devolución de prueba de venta 15 se eliminó y su estado restaurado (COMPLETADA/PENDIENTE).
- Fix notificación admin + email "No recipients" + corte tabla órdenes (2026-08-09): (1) **Email**: el error `EENVELOPE: No recipients defined` al cambiar estado de envío ocurría porque el EMAIL del admin en BD es `yeison` (no es un correo válido) — `estadoPedido.js` ahora valida `^[^\s@]+@[^\s@]+\.[^\s@]+$` antes de sendMail (si es inválido, salta el correo sin error); los correos reales de registro siguen enviándose. (2) **Notificación "que falta"**: `notificarCambioEstado` SÍ creaba la notificación, pero con `idUsuario` = el cliente de la venta (admin, ID 1) y el timbre del admin (`filtroUsuario` en notificacionController) solo mostraba `ID_USUARIO IS NULL` — ahora el admin ve **globales + las suyas** (`ID_USUARIO IS NULL OR ID_USUARIO = ?`, mismo params en las 4 rutas: misNotificaciones/noLeidas/marcarLeida/marcarTodasLeidas). (3) **Corte de la tabla de órdenes**: el botón "Gestionar" (y el detalle expandido) quedaban cortados a 1440px porque `.ap-tabla-wrap` tenía `overflow: hidden` — ahora `overflow-x: auto` (scroll si hace falta, sin cortar; también beneficia AdminProductos) + `.ao-tabla` con `min-width: 940px` y padding de celdas `12px 12px` → a 1440px la tabla entera cabe sin scroll (verificado: wrap 1104px, scroll horizontal 0px, botón Gestionar completo, detalle 2 columnas visible). Verificado con Playwright 0 errores JS: PUT envio 15 → EN_CAMINO 200 → timbre admin muestra "Tu pedido #15 está en camino…" → restaurado PENDIENTE; logs sin EENVELOPE.
- AdminOrdenes profesional con envíos y entregas (2026-08-09): (1) **Backend** `obtenerTodasLasCompras` (adminController.js) ahora trae del ENVIOS completo: BARRIO, CODIGO_POSTAL, OBSERVACIONES, TELEFONO_CONTACTO, COSTO_ENVIO (numeric/null), FECHA_ENVIO + TELEFONO del cliente; DETALLE_VENTAS con LEFT JOIN a PRODUCTO_VARIANTES (COLOR/NOMBRE_ATRIBUTO/ATRIBUTO) y agregados `TOTAL_ARTICULOS`/`TOTAL_UNIDADES`. (2) **AdminOrdenes.tsx reescrito** con clases `ao-*` (CSS nuevo al final de adminDashboard.css): header profesional; **5 KPI cards clickeables** (`.ao-kpi` con iconos de colores: Total, Pendientes, En camino, Entregadas, Canceladas → click filtra); toolbar reutilizando `ap-search/ap-filtro/ap-count` con buscador (#/cliente/correo), filtro estado venta, estado envío y fecha (7/30 días); tabla `.ao-tabla` sin Bootstrap: orden con fecha (FaCalendarAlt), cliente con mail, artículos/unidades, total + método, badges de estado `.ao-bv-*` y envío `.ao-be-*` con FaTruck; botón "Gestionar" expande **detalle en 2 columnas**: IZQ = artículos con imagen 52px, chips de variante (FaPalette/FaTag), xCantidad + subtotal, y desglose totales (subtotal/envío/total con fila final) — DER = "Envío y entrega": **timeline stepper de 5 pasos** (Pendiente→Por empaquetar→Empacado→En camino→Entregado, `.ao-step` con círculos hecho-verde/activo-rojo, clickeable para saltar, línea conectora), banner rojo si CANCELADO, botón "Avanzar: marcar como X" (`.ao-btn-avanzar`), tarjeta de info de envío con íconos (dirección+barrio, ciudad/depto/CP, teléfono, observaciones, costo envío gratis/$X, fecha envío), y acciones: select de estado de la orden + Factura PDF + "Cancelar envío" (con Swal de confirmación); paginación reutiliza `.ap-paginacion` (10/pág); cancelar envío ahora confirma con Swal. Verificado con Playwright 1440px, 0 errores JS: 5 KPIs (9 órdenes), búsqueda "admin" → 1 fila, filtro EN_CAMINO → 0, detalle con 5 pasos/activo Pendiente/1 producto/2 chips variantes/botón avanzar/info completa ($198.000 + $9.000 = $207.000); flujo real: click Avanzar → PASO activo "Por empaquetar" + API confirma POR_EMPAQUETAR → click paso "Entregado" → salta directo + oculta avanzar → restaurado a PENDIENTE por API.
- Breadcrumb + confirmación al quitar imagen + guardia de cambios sin guardar (2026-08-09): (1) **Breadcrumb** en EditarProductoAdmin pasa de "Dashboard › Editar Producto #29" a **"Dashboard › Productos › Producto %nombre%"** (link "Productos" → `/admin/productos`; último segmento = estado `nombre`, fallback `Producto #id`). (2) **Confirmación al quitar imagen**: el ✕ de `SubirImagenes.tsx` ahora abre Swal "¿Quitar esta imagen?" (question, confirmButtonColor #d33, reverseButtons; aplica también al modal crear de AdminProductos, compartido). (3) **Guardia de cambios sin guardar**: nuevo `frontend/src/utils/navigationGuard.ts` (guardia a nivel de módulo: `setGuardiaNavegacion/puedeNavegar/navegarConGuardia`); `AdminNavbar.tsx` enruta TODAS sus navegaciones por `navegarConGuardia` (sidebar tabs, logo, botón tienda) y `cerrarSesion` chequea `puedeNavegar` antes de logout; `EditarProductoAdmin.tsx` — **dirty tracking por snapshot**: al cargar guarda `JSON.stringify` de los 9 campos (nombre, marca, precio, ids, descripcion, imagenes, variantes, caracteristicas) en `snapshotRef` y un useEffect compara y setea `dirty`; con dirty=true registra la guardia (Swal "Cambios sin guardar" → "Salir sin guardar"/"Seguir editando"), `beforeunload` (dialogo del navegador) y bloqueo de **back del navegador** (popstate → pushState + Swal, `permitirSalidaRef` para permitir salida real tras confirmar); "Cancelar" de edición también confirma si hay dirty; tras guardar o cancelar, `cargarProducto` actualiza el snapshot → dirty vuelve a false. Verificado con Playwright 1440px, 0 errores JS: breadcrumb "Dashboard › Productos › Guayos profesionales" + link visible; editar nombre → click sidebar → Swal → "Seguir editando" queda en la página → 2º click → "Salir sin guardar" navega a Órdenes; quitar imagen → Swal → Cancelar conserva 3 → Quitar deja 2; reload con cambios → dialog `beforeunload`; cancelar edición con cambios → Swal → salir → nombre recargado "Guayos profesionales"; carga limpia sin dirty.
- Orden tienda por popularidad + admin por ID + carpetas de subida (2026-08-09): (1) `obtenerProductos` ahora ordena `ORDER BY visitas DESC, ID ASC` (visitas = COUNT de HISTORIAL por producto) — la TIENDA muestra primero lo más visitado; (2) `AdminProductos` por defecto ordena por **ID ascendente** con nueva columna `#` ordenable (`.ap-id`), las filas se ven 1..44 en orden; (3) `POST /api/productos/imagenes` acepta `idProducto` opcional → guarda en `uploads/Producto_NN/img_{siguiente}.{ext}` (continúa numeración del max existente; URL `/images/productos/Producto_NN/img_N.ext`); sin idProducto (modal crear) sigue en `subidas/`; `SubirImagenes` acepta prop `idProducto` y EditarProductoAdmin la pasa; (4) `subirFotoPerfil` ahora guarda en **carpeta por usuario** `uploads/perfiles/u{ID}/perfil.{ext}`: elimina la foto anterior de esa carpeta (regex `perfil.\w+`) y la reemplaza — siempre 1 archivo; las fotos planas viejas quedan intactas; sigue sirviéndose por express.static del backend (recursivo, sin bug de Docker). Cupones confirmado: SOLO `RETO-` se marca USADO=1 en checkout (checkoutController.js:120-121,162); los descuentos de producto nunca se destruyen. "Ver página" 404 resuelto (era la versión con `/api/producto/` roto): verificado con Playwright popup renderiza `Guayos profesionales` sin errores. Verificado: upload API → archivo en Producto_01/img_4.png servido 200 (limpiado), foto → u1/perfil.png reemplazada (1 archivo), API tienda ordena 1,2,3,4,5 (visitas 2,1,1,1,1), Playwright admin # 1..10 asc + sort precio OK, 0 errores JS.
- READMEs profesionales + fix ruta Ver página (2026-08-09): los 5 READMEs reescritos con badges shields.io, tablas actualizadas y datos reales del stack: **raíz** (resumen 3 capas, arquitectura, inicio rápido Docker/sin Docker, credenciales admin seed, features usuario/admin al día, docs con 49 RF/39 HU/15 RNF, equipo); **backend** (stack real con multer/pdfkit, rutas corregidas: eliminado `GET /google-client-id` inexistente, agregados `social-login`, `/foto`, `cambiar-email`, `confirmar-cambio-email`, `verificar-password`, `DELETE /retos/evidencias/:id`, `/admin/pendientes`, bases `/api/envio|newsletter|contacto|notificaciones`, env vars `FRONTEND_URL`/`ADMIN_EMAIL`/`ADMIN_PASSWORD`, notas de BD 33 tablas + migraciones); **frontend** (estructura con todos los admin pages/componentes nuevos, rutas con `/producto/:id`, `/admin/editar/:id`, `/perfil/metodos-pago`, NotFound catch-all, nota de mounts y proxy); **movil** (profesional con status table ✅/🚧 y nota Expo SDK 54); **docs** (badges 49/39/15 + verificación). **BUG FIX**: el replace de `/productos/${id}`→`/producto/${id}` en EditarProductoAdmin rompió los fetch del API (`/api/producto/` → 404) — corregido a `/api/productos/${id}`; los 3 `window.open` quedaron en `/producto/${id}` (ruta pública real, verificada con Playwright: popup `http://localhost:5173/producto/1`, 0 errores JS). Probe completo de edición re-corrido OK (galería, modal 15 chips/7 filas, reorden, 0 errores).
- Editar producto admin con galería ordenable + vista previa (2026-08-09): `SubirImagenes.tsx` (compartido con AdminProductos) ahora permite **ordenar miniaturas con flechas ◀▶** (`.si-*` en adminDashboard.css): badge `Portada` rojo en la 1ª (el resto numeradas 2..N), botón ✕, hint "Usa las flechas para ordenar". `EditarProductoAdmin.tsx` (+ CSS `pp-*` en editarProducto.css): componente `GaleriaAdmin` (principal aspect-ratio 1/1 + tira de thumbs con badge Portada/número, activa con borde rojo) usado en el card "Vista previa" del sidebar y en "Imágenes del Producto" (thumbs SIEMPRE visibles, en modo ver y editar — antes solo un texto "N imagen(es)"); **modal "Vista previa del producto"** (`.pp-backdrop/.pp-modal`) que replica la página pública: galería clickeable, chip categoría, nombre, marca, precio con viejo tachado + chip -X% (precio original = precio/(1-pct/100)), badge stock ok/bajo/agotado, chips de variantes con FaPalette/FaTag + "N uds" (agotadas en rojo), descripción y ficha técnica en filas zebradas; botones "Vista previa" y "Ver página" (window.open `/productos/:id`) en save bar (ambos modos) y sidebar; guardado ya persiste el ORDEN (backend `actualizarProducto` borra PRODUCTO_IMAGENES y reinserta con ORDEN 1..N según el array). Verificado con Playwright 1440px: galerías renderizan, click thumb cambia principal, modal con 15 chips variante/7 filas ficha, reorden con flechas funciona y se cancela sin guardar, modal crear de AdminProductos abre OK, 0 errores JS. NOTA ruta: `/admin/editar/:id` (no `/admin/productos/:id`).
- AdminProductos rediseñado (2026-08-09): `AdminProductos.tsx` reescrito con clases `ap-*` (CSS en adminDashboard.css): toolbar con buscador (`.ap-search` con icono FaSearch), filtro por categoría (`.ap-filtro`) y contador de productos (`.ap-count`); tabla `.ap-tabla` con columnas imagen 52px, nombre+marca, categoría como chip `.ap-chip` con color derivado por hash del nombre, precio `.ap-precio`, stock con badges `.ap-stock ok/bajo/agotado` (≤10 "¡Solo quedan N!", 0 "Agotado") y acciones `.ap-btn-accion editar/eliminar`; **columnas Nombre/Precio/Stock ordenables** (`.ap-sortable` + FaSort/FaSortUp/FaSortDown, estado `orden:{campo,dir}`); paginación `.ap-paginacion` (Anterior/páginas/Siguiente, `.ap-page-num.activa` con gradiente rojo); estado vacío `.ap-vacio`; SIN scroll interno (paginación 10/pág); modal de creación/edición intacto. Verificado con Playwright 1440px: 10 filas, orden precio asc $25.000 → desc $1.800.000, búsqueda "balon" → 3 filas, filtro Cardio → solo chips Cardio, última página 5 activa, tabla sin scroll, 0 errores JS.
- Admin rediseñado (2026-08-09): (1) **AdminNavbar → sidebar lateral oscuro** (`AdminNavbar.tsx` reescrito, clases `admin-sidebar*`): fijo 252px, gradiente #0f172a→#1e293b, logo JS + "JADDA SPORTS / Panel de control", 8 links con iconos react-icons (no emoji) y **badges de pendientes** (evidencias retos + devoluciones) traídos de `GET /api/admin/pendientes` (nuevo en adminController.obtenerPendientes + adminRoutes) al montar y al cambiar de ruta; pie con avatar/letra, usuario, BellNotificaciones oscuro, botones tienda/logout. Responsive: <992px el sidebar se oculta (translateX -105%) y aparece botón hamburguesa `.admin-sidebar-toggle` + backdrop que abren el drawer (`.admin-sidebar-open`). (2) **AdminDashboard reescrito** (`AdminDashboard.tsx` + clases `adm-*`): header con saludo y fecha, botones Reportes/Ver tienda; 4 KPI cards con gradientes (Ingresos 30d, Pedidos 30d, Clientes, Productos) clickeables; tarjeta HOY con ingresos/pedidos del día + **% vs ayer** (↑verde/↓rojo/–) + ticket promedio 30d + unidades 30d; tarjeta "Por revisar" (evidencias retos, devoluciones, stock bajo, avisos de reposición con contadores); **gráfica de barras CSS** de ventas 30 días (tooltip con día/ingresos/pedidos); top 5 más vendidos (pos, imagen, unidades, barra proporcional); últimas órdenes con badges de estado `.adm-estado-*`; usuarios recientes con avatar; resumen global (ingresos confirmados, órdenes, usuarios, productos). (3) **Backend `obtenerDashboard` ampliado**: stats globales + 30d (ordenes/ingresos/ticket/unidades), comparativa hoy vs ayer, serie diaria 30d, top 5 más vendidos, 6 órdenes recientes, 5 usuarios recientes, pendientes (evidencias/devoluciones/stock bajo/avisos). (4) **adminDashboard.css reescrito** manteniendo compatibilidad con clases viejas (custom-modal, edit-*, evidencia-*, reporte-*, admin-input/label/select): fondo #f1f5f9, `.admin-content` con margin-left 252px (0 en móvil, padding 80px top), `.admin-footer` restyling con mismo margin. Verificado E2E: dashboard con 4 KPI ($2.843.000), 2 barras reales, top 5, 6 órdenes, badges sidebar, navegación KPI→reportes y sidebar→productos con content left=252, móvil drawer abre, 0 errores JS. **Ajustes post-feedback (2026-08-09)**: sidebar SIN scroll (quitar `overflow-y:auto` de `.admin-sidebar-nav`), tarjeta HOY de azul oscuro → **fondo blanco** (`.adm-hoy-card` #fff, métricas #f8fafc, texto #0f172a), y en AdminProductos se eliminó el `maxHeight/overflowY` de la tabla (`style` inline de `.table-responsive`) — la paginación de 10 queda sin scroll interno. Verificado con Playwright: sidebar no scrollable, hoy-card rgb(255,255,255), tabla sin maxHeight y sin scroll, 10 filas + paginación, 0 errores JS.
- Navbar brand con nombre (2026-08-09): "JADDA SPORTS" junto al logo — `Navbar.tsx` agrega `<span class="navbar-brand-name">JADDA <span class="navbar-brand-name-sports">SPORTS</span></span>` dentro del Link brand; `Navbar.css`: `.navbar { padding: 0 16px }` (antes 40px — logo más a la izquierda), `.navbar-brand` flex gap 10px, nombre blanco 1.05rem/800/letter-spacing 1.5px con SPORTS dorado #ffd166, white-space nowrap. Verificado con Playwright 1440px: logo x=16 (antes 40), nombre x=89 (justo a la derecha, gap 10px), menú INICIO x=296 sin solape.
- Evidencias visibles y eliminables por el usuario (2026-08-09): el aviso "N avances en revisión" (amarillo) y el de rechazados (rojo) en Mis Retos son CLICKEABLES → abren modal Swal "Mis avances enviados" (fondo #1a1a1a) con lista de evidencias (previews img/video 120x90 con preload=metadata, chip de estado EN REVISIÓN/APROBADO/RECHAZADO, cantidad + fecha) y botón "Eliminar avance" SOLO en las pendientes (Swal de confirmación con reverseButtons; al confirmar cierra los dos modales, recarga y muestra toast). En retos COMPLETADOS hay link "Ver avances enviados". Backend: `DELETE /api/retos/evidencias/:id_evidencia` (verificarSesion) en retoController.eliminarEvidencia — valida que sea del usuario y ESTADO='pendiente' (aprobada/rechazada → 400 "Solo puedes eliminar avances que siguen en revisión", ajena/inexistente → 404), borra del disco RUTA + RUTAS_EXTRA (unlink con try/catch, `uploads/retos/`) y la fila. E2E: evidencia pendiente creada → archivo existe en disco → DELETE 200 → archivo desaparece → aprobada → 400 → inexistente → 404; Playwright: alert clickeable → modal con 2 previews + chip APROBADO + botón eliminar → confirmación → CANCELAR no borra (verificado en BD). Datos demo dejados a propósito para que el usuario pruebe: reto_usuario 10 (admin, reto 1 "Semana Activa"), evidencia 12 pendiente con imagen, venta 15 (admin, 1 producto).
- MisCompras en 2 columnas (2026-08-09): el detalle expandido ya no apila todo hacia abajo — `.comp-detalles` es grid `minmax(0,1.55fr) minmax(0,1fr)` (breakpoint 992px → 1fr): columna IZQUIERDA = productos + total final; columna DERECHA (fondo #f8fafc, borde, radius 14px) = badge de envío, dirección, pago y `.comp-acciones` (Cancelar + Factura PDF juntos). Resultado medido con Playwright en viewport 1440x900: detalle 604px de alto (antes ~1100px), 0 errores JS.
- Retos + Favoritos UX (2026-08-09): (1) `Favoritos.css` compacto — grid `minmax(155px,1fr)`, imagen 125px, paddings/fuentes menores (móvil 130px/105px). (2) Porcentajes de retos bajados en `setup.js` (los 2 bloques seed) con tope 10%: ID1 5, ID2 8, ID3 10, ID4 6, ID5 3, ID6 4, ID7 5, ID8 10, ID9 5, ID10 5, ID11 8, ID12 5, ID13 6, ID14 4, ID15 6, ID16 8 — schema.sql regenerado; **BD EXISTENTE pendiente de actualizar en Workbench** (INSERT IGNORE no pisa; SQL listo abajo). (3) Reportar avance REDISEÑADO + videos grandes: estrategia = **multipart con multer 2.2.0 (streaming a disco, sin base64 ni memoria)** — `docker exec jadda_backend pnpm add multer`; `retoRoutes.js` sube `materiales[]` a `uploads/retos/r{id}/` (mkdirSync en destination, fileFilter jpg/png/webp/gif/mp4/webm/**mov**, límite 100 MB/archivo, máx 10) con mensaje 400 por límite; `retoController.reportarProgreso` acepta la rama multipart (TIPO según mimetype, RUTA=1º, RUTAS_EXTRA=resto) y mantiene la rama base64 legacy; helper `limpiarArchivos()` borra subidas si falla la validación (restante≤0, >10, catch general). Frontend `Retos.tsx`: modal nuevo — stepper −/+ para cantidad, **dropzone click + drag&drop**, previews con badge FOTO/VIDEO y chip de tamaño, acepta .mov, subida con **barra de progreso real** (XHR `upload.onprogress` → % en Swal "Subiendo evidencia…", no cierra), mensaje de revisión ≤24h; `MAX_MB_POR_ARCHIVO` 10 → 100. E2E: curl multipart imagen png + video mp4 → 2 evidencias pendientes OK (archivos en disco); Playwright flujo UI completo (dropzone/stepper/preview → + → enviar → barra → "Avance en revisión", 0 errores JS); datos de prueba limpiados (evidencias 5-8, retos_usuarios 8-9, usuario 13, notificaciones, carpetas r8/r9).
- Direcciones + métodos de pago UX (2026-08-09): `DireccionesPerfil.tsx` — tarjeta de dirección con más info en filas con íconos (etiqueta con FaHome, dirección destacada con FaMapMarkerAlt, ciudad/depto/barrio con FaCity, teléfono FaPhone, CP FaEnvelope como `<small>` → líneas `.dir-linea` con íconos rojos). `PerfilMetodosPago.tsx` — el `window.confirm` de eliminar (alert básico del navegador) reemplazado por SweetAlert2 dark (fondo #1a1a1a, confirmButtonColor #e63946, reverseButtons, texto con titular si existe). Verificado con Playwright: dirección muestra todas las líneas, eliminar abre Swal y cancelar no borra, 0 errores JS; datos de prueba (dirección 11, método 8) eliminados vía API.
- Mis Compras con variantes y rediseño profesional (2026-08-09): DETALLE_VENTAS YA NO guardaba la variante — se agregó columna `ID_VARIANTE` (NULL, FK a PRODUCTO_VARIANTES ON DELETE SET NULL) en CREATE_TABLES_RAW + MIGRACIONES idempotente en setup.js (aplicada con `docker restart jadda_backend`); `checkoutController` ahora graba `item.ID_VARIANTE` en el INSERT de DETALLE_VENTAS; `obtenerCompras` y `obtenerCompraPorId` (comprasController) hacen LEFT JOIN a PRODUCTO_VARIANTES y devuelven `COLOR`/`NOMBRE_ATRIBUTO`/`ATRIBUTO` por producto (ventas viejas → NULL, sin chips). Frontend `MisCompras.tsx`: fila de producto rediseñada (grid 4 columnas) — miniatura 64px en wrap, nombre con ellipsis, chips de variante (`<FaPalette/> COLOR` y `<FaTag/> NOMBRE_ATRIBUTO: ATRIBUTO`), precio unitario "X c/u", badge xCANTIDAD, subtotal + botón Devolver a la derecha, y pie `.comp-total-final` (franja azul marino `#0f172a→#1e293b` con total en negrita); header de productos con contador "N artículos"; media query móvil (grid 2 filas, total al final). schema.sql regenerado. E2E completo: compra real vía API (login admin → carrito con id_variante 2 → checkout → GET /api/compras devuelve COLOR "Blanco"/Talla "40") + Playwright (login admin → /perfil/compras → chips y diseño OK, 0 errores JS); datos de prueba (venta 14 + ENVIOS + DETALLE + PLANES) eliminados; venta 13 (usuario 9) quedó con variantes asignadas vía UPDATE como dato demo.
- Última conexión en /perfil/seguridad (2026-08-09): columnas `ULTIMA_CONEXION`/`ULTIMA_IP`/`ULTIMA_UBICACION` en USUARIOS (CREATE_TABLES_RAW + MIGRACIONES en setup.js, aplicadas al reiniciar backend); `registrarConexion()` en authController actualiza fecha+IP y geolocaliza best-effort con ip-api.com (timeout 2.5s, salta IPs privadas 10./192.168./172.16-31/127/::1) — NUNCA bloquea el login; se dispara en `login` y `socialLogin`; `obtenerPerfil` expone los 3 campos. `Seguridad.tsx` muestra "Última conexión: <fecha hora> · <ciudad, región, país>" (si no hay, "Aún no hay registros de conexión") con estilo .ultima-conexion en Seguridad.css. E2E: POST login → GET perfil → ULTIMA_CONEXION/ULTIMA_IP llenos (IP Docker 172.18.0.1 → ubicación null correcto). schema.sql regenerado.
- PerfilEditar seguro (2026-08-09): el correo YA NO se guarda con `PUT /perfil` directo — flujo `cambiar-email` (correo nuevo + contraseña actual validada con bcrypt → guarda `EMAIL_PENDIENTE` (columna nueva vía MIGRACIONES en setup.js, createSql+alterSql) y envía código de 6 dígitos al correo NUEVO) → `confirmar-cambio-email` (valida TOKEN/TOKEN_EXPIRA/EMAIL_PENDIENTE → recién ahí UPDATE EMAIL). Teléfono pide `verificar-password` antes de guardar. Nombre/usuario siguen directo. Rutas: `POST /api/auth/cambiar-email`, `POST /api/auth/confirmar-cambio-email`, `POST /api/auth/verificar-password` (todas con verificarSesion + rateLimit). Email removido de `actualizarPerfil`. E2E probado: pass mala → 400, duplicado → 409, código malo → 400, código bueno → 200 + login con correo nuevo OK. Usuario de prueba `prueba.cambioemail.nuevo@gmail.com` (ID 12) + `prueba.username.4812/7391@gmail.com` (IDs 10-11, + DIRECCIONES) pendientes de borrar en Workbench. schema.sql regenerado (EMAIL_PENDIENTE en CREATE_TABLES_RAW).
- PerfilEditar rediseñado (2026-08-09): solo info personal en filas desplegables — click en la fila despliega el editor (Nombres → nombre+apellido; Correo; Teléfono; Nombre de usuario al final); foto de perfil REMOVIDA de esta página (vive en Perfil.tsx); tipo/número de documento fuera; sección "Información de tu cuenta" queda con Miembro desde + Cambiar contraseña. Backend: `registro` asigna `USUARIO` aleatorio único `base.XXXX` (helper `generarUsuarioUnico` — normaliza nombre sin tildes, 6 reintentos, fallback timestamp; usado también en socialLogin); `actualizarPerfil` ahora acepta EMAIL (400 si formato inválido, 409 si ya existe otro usuario con ese correo), valida usuario (regex `^[a-zA-Z0-9._-]{3,30}$`, 409 duplicado) y teléfono (7-10 dígitos, 400). Probado E2E: 2 registros con mismo nombre OK (usernames únicos), PUT usuario válido/cambio/revert OK, espacio → 400, email inválido → 400, email duplicado → 409. Pendiente de borrar en Workbench: usuarios `prueba.username.4812@gmail.com` y `prueba.username.7391@gmail.com` (+ DIRECCIONES).
- Slot del slider de precio del catálogo (2026-08-09): el BUG "No encontramos productos en la categoría Cardio" era el filtro de precio (`PRECIO <= precioMaximo` con tope fijo $1M) — la Elíptica ($1.8M) y el Reloj ($1.2M) de Cardio superaban el tope y se descartaban silenciosamente. Fix en `Catalogo.tsx`: tope dinámico `precioTope` = mayor precio real redondeado a $50.000 (mín $1M) calculado al cargar los productos; `sliderTocado` evita pisar la selección del usuario; LIMPIAR FILTROS restablece al tope real. Se mantienen: filtro por ID_CATEGORIA robusto, búsqueda por categoría en backend y mensaje vacío contextual. Diagnóstico DIAG-FETCH/DIAG-FILTRO removido del código.
- ResumenCompra: nested fragment `<>...</>` → `<div>` fix for OXC parser error.
- Catalogo variant selector modal: opens on cart icon click, fetches `GET /api/productos/:id/variantes`, color + attribute buttons filtered by color, stock display, quantity selector, confirm button, 80x80 thumbnail.
- Search improved: prefix matching `LIKE 'word%'` (was `%word%`), all query words must match.
- Related products improved: `ORDER BY RAND()`, fallback to other categories if fewer than 4.
- ProductDetailPage: `addToCart` now passes `ID_VARIANTE` (not `cantidad`); selecting attribute first then color no longer resets attribute.
- Review form in ProductDetailPage: star hover effect, comment textarea, logged-in gate, auto-name from AuthContext, avatar circle.
- Review form in CompraExitosa: product selector buttons, star hover, comment, submit, confirmation.
- Footer compact: reduced padding/margins, `margin-top: auto` via AppLayout flex column.
- FloatingCart draggable: mouse/touch drag, viewport clamped, navbar boundary, click vs drag detection.
- MiniCartMenu: position synced via `cartButtonX/Y` in CartContext, closes on click-outside.
- `backend/database/setup.js` created: 22 `CREATE TABLE IF NOT EXISTS` + `INSERT IGNORE` for all reference data. Retries MySQL connection up to 10 times.
- `server.js` modified: added `require('./database/setup')` at line 9.
- `docker-compose.yml` modified: bind mount `./mysql_data` → named volume `mysql_data:`.
- READMEs created/updated: root, backend, frontend, movil.
- Mobile app reviewed: Inicio, Catalogo, Detalle Producto, Login, Registro functional. AuthContext ya persiste con AsyncStorage (`@jadda_usuario`).
- Catalogo two carts bug fixed: removed duplicate FloatingCart/MiniCartMenu.
- Categorias slowness fixed: bg placeholder, fadeIn animation, reduced margin-top.
- Docs analyzed: 39 HUs, 39 RFs, 7 RNFs. Stack mismatches found.
- RNF-001.md: Module 1 only (Security) — separated from monolithic original.
- RNF-002.md through RNF-006.md: extracted from original RNF-001.md modules 2–6, each with proper format.
- RNF-007.md: extracted Module 7 (Documentation/Maintainability) from original.
- RNF-008.md through RNF-011.md: 4 new modules — DevOps, Mobile App, Internationalization, Error Handling.
- `docs/plan de trabajo.md` created.
- `docs/Restricciones del Proyecto.md` created.
- RF-001 through RF-012: stack/endpoints corrected (JWT → express-session, `/api/v1/...` → `/api/...`, table names to real DB schema).
- RF-013 through RF-029, RF-036, RF-039: status/endpoints aligned to real implementation.
- HUs updated to match corrected RFs: statuses, acceptance criteria, endpoints, table names aligned.
- HU-002 fixed: was incorrectly containing RF-002 content; replaced with proper HU.
- HU-005 fixed: corrupted acceptance criteria content restored.
- HU-003 through HU-012, HU-013 through HU-029, HU-036, HU-039: statuses updated (Implementada), acceptance criteria rewritten.
- Diagrams created: `docs/diagrams/arquitectura.md`, `docs/diagrams/base-de-datos.md`, `docs/diagrams/flujos.md`.
- `docs/README.md` created: index with links to all docs, state table, stack.
- `docs/plan de trabajo.md` fixed: auth stack corrected (JWT → express-session).
- `backend/README.md`: auth stack corrected (JWT removed), route table expanded to 40+ real endpoints, env vars aligned, Facebook route removed.
- `frontend/README.md`: auth stack corrected (jwt-decode → express-session cookie-based).
- `movil/README.md`: API URL reference updated to `EXPO_PUBLIC_API_URL`, status table corrections (added verificar-codigo as Funcional).
- HU-001.md: verification flow corrected — 24h link → 15min 6-digit code, removed "NN Auth" reference.
- RF-001.md: verification flow corrected — 24h → 15min, endpoint `/api/auth/verificar-codigo` → `/api/auth/confirmar`, added `/api/auth/reenviar-codigo`.
- `docs/README.md`: RNF count updated 7 → 11.
- AGENTS.md: comprehensive update with all changes from this session.
- AdminProductos: búsqueda (nombre/marca/categoría), paginación 10/pág, orden aleatorio, sticky header, scroll acotado.
- NotFound page (`frontend/src/pages/NotFound.tsx`): 404 con botón "Volver al inicio"; catch-all en App.tsx apunta a ella (antes Principal).
- ErrorBoundary (`frontend/src/components/ErrorBoundary.tsx`) + ErrorFallback (`frontend/src/pages/ErrorFallback.tsx`): página "¡Oops!" con botón Reintentar; envuelve todas las rutas.
- LoadingPage (`frontend/src/components/LoadingPage.tsx`): pantalla de carga reutilizable; usada como fallback de Suspense y en Catálogo (pantalla completa hasta que carguen productos).
- Catalogo: estado de error con botón "Reintentar" si falla el fetch de productos.
- `backend/database/setup.js`: `seedAdminUser()` crea cuenta admin en CADA arranque (incluso si las tablas ya existen). Credenciales: `ADMIN_EMAIL`/`ADMIN_PASSWORD` en .env; default `yeison` / `Losquiero7`, ID_ROL=1, CONFIRMADO=1.
- `backend/controllers/checkoutController.js`: total calculado 100% en servidor (ignora `totalFinal`/`descuentoAplicado` del cliente); valida cupón contra DESCUENTOS; transacción completa (BEGIN/COMMIT/ROLLBACK); rollback en catch; conexión liberada en finally. Decisión del usuario: decremento de stock + MOVIMIENTOS_STOCK REMOVIDOS temporalmente hasta que se hagan pruebas.
- `backend/middlewares/rateLimiter.js` (nuevo): limitador en memoria (ventana deslizante por IP, sin dependencias). Aplicado a login (10), registro (5), confirmar (10), recuperar-password (5), verificar-codigo (10), update-password (10), reenviar-codigo (5), contacto (5).
- `backend/controllers/authController.js`: registro valida email/nombre/password (min 8) antes de BD; login devuelve 400 si faltan campos y usa mensaje unificado "Correo o contraseña incorrectos" (anti-enumeración).
- Protección de rol admin COMPLETA: middleware `esAdmin` (sesión + ID_ROL === 1) aplicado en `adminRoutes.js` (router.use), CRUD de productos (`productoRoutes.js`) y evidencias de retos (`retoRoutes.js`); `deserializeUser` en passport.js SÍ selecciona `ID_ROL`.
- Batch UX (2026-08-02): checkout valida stock SIN decrementar (400 con mensaje por producto); envío real por departamento; rating promedio + badges de stock en cards; cancelar pedido; dirección de pedido en servidor; CompraExitosa recupera venta al refrescar; guía de tallas; recientemente vistos; newsletter; plan con días persistidos; fixes Navbar/AuthModal.
- Migración imágenes locales (2026-08-02): 44 carpetas `Producto_01..44` (3 archivos `img_1/2/3` c/u) copiadas de `Downloads\imagenes_productos` → `frontend/public/images/productos/`; URLs en BD pasan de externas a `/images/productos/Producto_NN/img_K.ext`. Producto 45 ('Balón basket oficial', duplicado del 6) ELIMINADO de seeds y de BD viva.
- `backend/database/schema.sql` (UNICO archivo SQL): esquema completo (33 tablas) + seeds con 132 rutas locales (sin producto 45) — importable en Workbench para BD NUEVAS; regenerado con `node backend/database/exportarSchema.js`. Para BD EXISTENTES no se importa: al reiniciar el backend, `setup.js` aplica las migraciones idempotentes (columnas/tablas/índices nuevos).
- `backend/database/setup.js` + `schema.sql`: bloque PRODUCTO_IMAGENES regenerado con 132 rutas locales; filas del producto 45 removidas de PRODUCTOS y PRODUCTO_VARIANTES.
- Métodos de pago en perfil COMPLETO (2026-08-06): página `frontend/src/pages/PerfilMetodosPago.tsx` (`/perfil/metodos-pago`, tarjeta "Métodos de pago" en Perfil.tsx, ruta lazy en App.tsx) con CRUD completo + marcar principal; `backend/controllers/metodoPagoController.js` `guardarMetodo` valida ID_METODO contra METODOS_PAGO (400 si no existe) y auto-asigna ES_PRINCIPAL=1 si es el primer método; `ResumenCompra.tsx` checkbox "Guardar este método" ahora por defecto TRUE (al pagar queda guardado); CSS en `frontend/src/css/MetodosPagoPerfil.css`. Sincronización bidireccional: métodos guardados en checkout → aparecen en perfil y viceversa (ResumenCompra ya precarga el principal). Probado E2E: 1er método → esPrincipal 1, 2do → 0, id_metodo inválido → 400, PUT principal cambia orden, DELETE limpia.
- Subida de imágenes desde el PC en panel admin: `POST /api/productos/imagenes` (base64 JSON, `express.json({ limit: '25mb' })` global en server.js + límite 25mb en la ruta). Guarda en `backend/uploads/subidas/` que es bind-mount de `frontend/public/images/productos` (docker-compose: `./frontend/public/images/productos:/app/uploads`) → URL `/images/productos/subidas/<ts>-<rand>.<ext>`.
- `crearProducto` y `actualizarProducto` ahora aceptan `IMAGENES` (array de URLs, ORDEN secuencial 1..N); `URL_IMAGEN` individual sigue funcionando como fallback.
- `frontend/src/admin/SubirImagenes.tsx` (nuevo): componente reutilizable — botón "Subir imágenes del computador" (multi-archivo, accept jpg/png/webp/gif), previews con botón ✕, input "o pega una URL". Usado en AdminProductos (modal agregar) y EditarProductoAdmin (sidebar, modo edición); reemplaza el `<input type="url">`.
- Correo de checkout: helper `imagenParaCorreo()` antepone `FRONTEND_URL` (env, default `http://localhost:5173`) a rutas relativas para que las imágenes locales funcionen en el email.
- Móvil: `movil/constants/api.ts` exporta `resolverImagen(url)` (prefija `EXPO_PUBLIC_FRONT_URL`, default `http://10.2.178.124:5173`); aplicado en `movil/app/producto/[id].tsx` (imagen principal + miniaturas).
- Perfil + foto de perfil (2026-08-02): `POST /api/auth/foto` (base64 ≤10mb → `backend/uploads/perfiles/u{ID}/` → `/images/perfiles/u{id}/...`, mount nuevo en docker-compose); `Perfil.tsx` rediseñado (hero pegado al navbar, avatar con borde rojo + menú Ver foto/cargar desde navegador, modal fullscreen, dashboard de 7 tarjetas); `PerfilEditar.tsx` con botón "Subir foto desde el navegador" y sección "Información de tu cuenta" (correo, miembro desde con día, documento, botón cambiar contraseña).
- Migración automática de BD: `migrarTablasExistentes()` en setup.js aplica ALTERs idempotentes al arrancar cuando las tablas ya existen (lista `MIGRACIONES`; hoy: `ENVIOS.COSTO_ENVIO`, `DESCUENTOS.USADO`, `RETO_EVIDENCIAS`); schema.sql actualizado (la nota ya no pide ALTER manual).
- Lote UX grande (2026-08-02): navbar "Ver perfil" (antes "Miembro JADDA"); páginas de perfil pegadas al navbar (`padding-top: 0` — el offset lo da `body { padding-top: 65px }` global); foto por carpeta `u{ID_USUARIO}`; input file fuera del menú del avatar + `setTimeout 50ms`; PerfilEditar sin input URL (se mantiene FOTO_URL como almacenamiento); fecha con día; Seguridad/MisCompras/Direcciones/Favoritos pegadas; lápiz en DireccionesPerfil; favoritos con deshacer (barra 5s + POST restaurador); FloatingCart estático (sin drag, se eliminó estado de posición del contexto); MiniCartMenu calcula posición sobre el botón fijo.
- Retos completos (2026-08-02): retoController reescrito (evidencias + aprobación + cupón); retoRoutes con 9 rutas; checkout valida `DESCUENTOS.USADO` (cupones RETO- de un solo uso: no aplica si ya usado, marca USADO=1 en la transacción); docker-compose mount `./frontend/public/images/retos:/app/uploads/retos`; `Retos.tsx` con botón atrás + adjuntar imagen/video (base64 ≤10mb) + mensaje "nuestros asesores... 24 horas" + badge de avances en revisión; `AdminRetos.tsx` (`/admin/retos` + tab en AdminNavbar) con tabla, modal de vista (img/video) y Aprobar/Rechazar.
- Verificaciones: `node --check` OK (retoController, retoRoutes, checkoutController, setup.js) + `npx tsc -b` OK (frontend, tras quitar `pos` muerto en FloatingCart y `guardarFoto` muerta en PerfilEditar).
- RF-035 Aviso de stock COMPLETO (2026-08-06): tabla `AVISOS_STOCK` (ID_VARIANTE+ID_USUARIO UNIQUE, ENVIADO) auto-creada vía `MIGRACIONES.createSql` en setup.js (patrón NOTIFICACIONES, aplicada con `docker restart jadda_backend`); `backend/utils/correo.js` (nuevo) con `imagenParaCorreo()` extraído de checkoutController; `productoController.js`: `suscribirAvisoStock`/`estadoSuscripcionAviso`/`cancelarAvisoStock` + helper `notificarReposicion()` (email + notificación in-app + ENVIADO=1, nunca bloquea al admin); hooks de reposición en `actualizarVariante` (stock 0→>0), `agregarVariante` (stock>0) y `actualizarProducto` (reemplazo por posición: captura agotadas con avisos antes del DELETE); 3 rutas nuevas en `productoRoutes.js` (`POST/GET/DELETE /api/productos/variantes/:id/suscribir|suscripcion`); `ProductDetailPage.tsx`: botón "🔔 Avísame cuando vuelva a estar disponible" cuando la variante seleccionada está agotada (login obligatorio → Swal), estado "Te avisaremos — quitar aviso". Probado end-to-end con curl (login admin → suscribir → estado → admin sube stock 0→7 → notificación in-app creada + ENVIADO=1; email falla por SMTP no alcanzable pero capturado).
- RF-037 Compartir en redes COMPLETO (2026-08-06): `ProductDetailPage.tsx` inyecta meta tags OG dinámicos al cargar (`og:title`, `og:description`, `og:image`, `og:url`, `og:price:amount` COP, `twitter:card`) con cleanup al desmontar; botón "Compartir" ahora es dropdown con WhatsApp/Facebook/X (share URLs con window.open) + "Copiar enlace". Nota: en SPA los crawlers no ejecutan JS → preview no garantizada en todos los scrapers.
- Docs alineados (2026-08-06): RF-020/023/030/031 → Implementado (endpoints reales `/api/admin/compras`); RF-035/037 → Implementado; HUs HU-020/023/030/031/035/037 → Implementada; `docs/README.md` + `plan de trabajo.md` actualizados (aviso stock y compartir agregados); AGENTS.md corregido (esAdmin aplicado, ID_ROL en deserializeUser, defaults admin `yeison/Losquiero7`, móvil con AsyncStorage).
- RF-021 Factura PDF COMPLETO (2026-08-06): dependencia `pdfkit` agregada al backend (`docker exec jadda_backend pnpm add pdfkit`); `backend/utils/facturaPdf.js` (nuevo) genera el PDF con pdfkit (dimensiones A4 fijas — `doc.page` no está disponible al inicio del Promise; encabezado JADDA SPORTS, facturado a, referencia/método de pago, tabla de productos, subtotal/descuento/envío/total, datos de envío, pie con `ALTO_PAGINA`); `GET /api/compras/:id/factura` (usuario dueño) y `GET /api/admin/compras/:id/factura` (`esAdmin`) devuelven `application/pdf` + `Content-Disposition: attachment; filename="factura-<id>.pdf"`; el email de confirmación del checkout ahora adjunta el PDF (attachments de nodemailer, generado con try/catch para no romper la compra). UI: botón "Descargar factura PDF" (FaFilePdf) en MisCompras y "Factura PDF" en AdminOrdenes (window.open del endpoint). Probado end-to-end: registro + confirmación + login + checkout → PDF 200 con header `%PDF-1.3` (usuario y admin).
- RF-025 Notificaciones de estado COMPLETO (2026-08-06): `backend/utils/estadoPedido.js` (nuevo) con `notificarCambioEstado(idVenta, tipo, estadoNuevo)` — consulta venta+cliente, crea notificación in-app (NOTIFICACIONES, tipo `pedido`, ruta `/perfil/compras`) y envía email con mensajes por estado (TEXTO_ENVIO/TEXTO_ESTADO); disparado en `actualizarEstadoCompra` (VENTAS.ESTADO), `actualizarEstadoEnvio` (ENVIOS.ESTADO_ENVIO) y `cancelarCompra` (usuario). El envío nunca bloquea (errores SMTP capturados). Probado end-to-end: admin cambió envío a EN_CAMINO + venta a CONFIRMADA + cancelación → 3 notificaciones creadas para el cliente con título/mensaje/emoji correctos (verificado vía `GET /api/notificaciones`).
- RF-032 + RF-034 Reportes y más vendidos COMPLETOS (2026-08-06): `adminController.reporteVentas` (`GET /api/admin/reportes/ventas?desde&hasta`, defaults últimos 30 días) → `{totalOrdenes, totalIngresos, ticketPromedio, totalUnidades, serie[]}` (excluye CANCELADAS, AVG ticket, serie diaria para el gráfico); `adminController.masVendidos` (`GET /api/admin/analytics/mas-vendidos?desde&hasta&limite`) → ranking por unidades con ingresos y stock (ojo: usar subconsultas para IMAGEN/stock, NO JOIN a PRODUCTO_VARIANTES — multiplicaba unidades); página `frontend/src/admin/AdminReportes.tsx` (`/admin/reportes`, tab 📈 en AdminNavbar): inputs de rango + presets 7/30/90 días, 4 tarjetas KPI, gráfico de barras CSS (sin librería de charts), tabla top 10 con stock badge; ruta en App.tsx. Probado end-to-end: 403 para no-admin, cancelar venta la excluye del reporte, fechas inválidas → defaults. Datos de prueba limpiados.
- RF-027 CRUD categorías COMPLETO (2026-08-06): `crearCategoria`/`actualizarCategoria`/`eliminarCategoria` en productoController (POST/PUT/DELETE `/api/productos/categorias` con `esAdmin`; nombre único 3-100 chars; DELETE bloqueado si la categoría tiene productos → 400 con conteo); `obtenerCategorias` ahora devuelve DESCRIPCION + TOTAL_PRODUCTOS; página `frontend/src/admin/AdminCategorias.tsx` (`/admin/categorias`, tab 🗂️ en AdminNavbar) con tabla + modal crear/editar (maxLength 100) + Swal de confirmación; ruta en App.tsx. Probado end-to-end: crear 201, duplicado/nombre corto 400, renombrar, eliminar 200, categoría con productos 400, DELETE repetido 404, no-admin 403.
- RF-033 Devoluciones COMPLETO (2026-08-06): tabla `DEVOLUCIONES` (ID_USUARIO, ID_VENTA, ID_PRODUCTO, CANTIDAD, MOTIVO, ESTADO SOLICITADA|APROBADA|RECHAZADA, FECHA_CREACION, FECHA_PROCESADA) auto-creada vía `MIGRACIONES.createSql` en setup.js (con `docker restart jadda_backend`); `backend/controllers/devolucionController.js` + `backend/routes/devolucionRoutes.js` (montadas en `/api/devoluciones`): `POST /` (usuario, solo ventas COMPLETADAS propias, cantidad ≤ comprada − ya solicitada/aprobada, motivo ≤ 500), `GET /` (mis solicitudes), `GET /admin` (admin, con cliente/producto), `POST /admin/:id/procesar` (APROBADA → transacción con FOR UPDATE: reingresa CANTIDAD al stock de la variante por defecto `MIN(ID_VARIANTE)` + notificación in-app tipo `devolucion` ruta `/perfil/compras`; RECHAZADA → solo estado + notificación; doble proceso → 400). UI: botón "Devolver" (FaUndoAlt) por producto en MisCompras (solo COMPLETADA) con Swal cantidad/motivo; `frontend/src/admin/AdminDevoluciones.tsx` (`/admin/devoluciones`, tab ↩️ en AdminNavbar) con tabla, badge de estado, modal de detalle y Aprobar/Rechazar. Probado end-to-end: 201/400 (cantidad excedida, venta ajena 404), aprobada → stock 50→51 + notificación, rechazada sin reingreso, stock restaurado y datos de prueba limpiados.
- RF-038 Recomendaciones COMPLETO (2026-08-06): `obtenerRecomendados` en productoController (`GET /api/productos/recomendados`, sesión): productos de categorías compradas (ventas no CANCELADAS, excluye productos ya comprados), ordenados por frecuencia de compra de la categoría, LIMIT 8, con forma igual a `obtenerProductos` (STOCK, RATING, ID_VARIANTE_POR_DEFECTO); fallback a más vendidos (JOIN subconsulta pop) cuando no hay datos; responde `{origen: 'categorias'|'populares', productos[]}`. Sección "✨ RECOMENDADOS PARA TI" en Principal (solo con sesión, fetch on `usuarioLogueado`). OJO BUG: la consulta fallback quedó sin el array de parámetros `db.query(sql, [idUsuario])` → `?` literal → ERROR 1064 "near '?'"; la consulta de categorías sí lo tenía y pasaba — verificar SIEMPRE que cada `?` tenga su valor en TODAS las ramas. Probado end-to-end: sin compras → origen 'populares'; con compras → origen 'categorias' excluyendo lo comprado; 401 sin sesión.
- Auditoría de seguridad/robustez RF-027/033/038 COMPLETA (2026-08-06): (B1) `actualizarCategoria` ya no devuelve 404 en updates sin cambios (mysql2 `affectedRows` = filas cambiadas; ahora verifica existencia con SELECT previo); (B2) validación de tipos en `description`/`motivo` (`String(x).trim()` — antes `123`.trim()` lanzaba 500); (B3) índice único `uq_categoria_nombre` en `CATEGORIAS.NOMBRE_CATEGORIA` (en CREATE_TABLES_RAW para BD nuevas + migración idempotente vía INFORMATION_SCHEMA.STATISTICS para BD existentes) + catch de `ER_DUP_ENTRY` → 400 en crear/actualizar; (B4) `backend/database/schema.sql` REGENERADO con `backend/database/exportarSchema.js` (script que extrae CREATE_TABLES_RAW + DEVOLUCIONES + SEED_DATA de setup.js — fuente única, 33 tablas; ejecutar `node backend/database/exportarSchema.js` tras cada cambio de esquema); (V1) `frontend/src/utils/escapeHtml.ts` nuevo — usado en Swal `html`/`title` de MisCompras (devolución) y AdminCategorias (los campos title/html de SweetAlert2 se interpretan como HTML → XSS almacenado vía nombres de producto/categoría); (V2) rate limiting en rutas nuevas: devoluciones POST (10/15min), admin list (60), procesar (30), categorías CRUD (30), recomendados (30); (V4) recomendados ahora excluyen productos agotados (`HAVING SUM(pv.STOCK) > 0` en ambas ramas). Verificado end-to-end: PUT no-op → 200, PUT id inexistente → 404, description numérica → 201 ("123"), duplicado → 400, 11º POST devolución → 429, producto con stock 0 desaparece de recomendados y vuelve al restaurar. `docs/diagrams/base-de-datos.md` nota de 24 → 33 tablas.

### In Progress
- (none)
- HUs completadas 39 → 49: creadas HU-040..HU-049 (retos, plan de entrenamiento, métodos de pago, notificaciones in-app, login social, foto de perfil, subida de imágenes admin, newsletter, contacto, historial de navegación), cada una con CA numerados por formato `CA-NNN.M` y `RF asociados`.
- RFs completados: RF-030 (faltaban Entradas/Proceso/Salidas), RF-035 (Entradas/Salidas), RF-037 (Entradas/Salidas/Endpoints + nota client-side), RF-032/034 (Proceso), RF-020 (Endpoints asociados).
- `docs/README.md`: badge HUs 49, índice HU-001..HU-049, nota de verificación corregida (RF-023/RF-029 ya NO son parciales — 47 Implementado 100%).
- `docs/plan de trabajo.md`: módulos nuevos agregados a la tabla (retos, planes, métodos de pago, notificaciones, login social, foto, subida admin, newsletter, contacto, historial, gestión categorías) y a Fase 4.

### Blocked
- (none)

## Key Decisions
- Search uses prefix matching (`LIKE 'word%'`) so "za" only matches products starting with "za".
- FloatingCart draggable: touchAction: none + passive events; MiniCartMenu follows via CartContext shared position.
- Reviews only for logged-in users; name auto-fetched from AuthContext; avatar uses first letter.
- Database setup: `CREATE TABLE IF NOT EXISTS` + `INSERT IGNORE` safe for every restart.
- Docker volume: named volume for portability (no `mysql_data/` dir needed on new clones).
- `.env.example` contains only placeholders; real credentials shared outside git.
- Docs corrections preserve existing structure and format.

## Next Steps
0. (User) Probar en el navegador lo nuevo: (a) admin → /admin/devoluciones → "Gestionar": modal BLANCO con hero oscuro, 4 stats, secciones Solicitud/Cliente/Pedido y envío (método de pago + dirección)/Motivo/Evidencias y footer con 4 decisiones; /admin/retos → abrir "Ya revisados" → "Ver": modal blanco; /admin/categorias modal claro; (b) en /perfil/devolucion/:idVenta con solicitud en "Pide más pruebas": seleccionar VARIOS archivos → chips con nombre+MB → "Subir N archivos" → al volver queda "En revisión" y el admin recibe notificación "📎 Nuevas evidencias adjuntadas" → /admin/devoluciones; (c) notificación al aprobar/rechazar/más pruebas lleva a /perfil/devolucion/{pedido} (antes /perfil/compras); (d) MisCompras: pedido con varios productos muestra "X y otros N producto(s)".
1. (User) Probar en el navegador: (a) retos — como admin en /admin/retos Rechazar con motivo (opcional) → en /mis-retos abrir "Mis avances enviados": botón "Ver motivo de la revisión" despliega la caja roja y el material SIGUE visible; aprobar otro avance con comentario → caja azul "Comentario del equipo" directa; evidencias viejas (14/15) sin material (histórico); (b) admin usuarios — modal "Ver usuario" con Compras (7) y Retos (1) desplegables, filtro por estado y búsqueda, "Ver más"/"Ver menos" (u9 yeisiton922 tiene 7 compras Nequi + reto Estiramiento 1/10); (c) admin devoluciones — tabla sin scroll ni columna "#", botón "Gestionar" abre el modal con todo; (d) ser vendedor — enviar solicitud SIN empresa/NIT → 201, y en /admin/vendedores aparece "Vendedor informal"/"—".
1. (User) Probar en el navegador "Ser vendedor": (a) dropdown del navbar → "Ser vendedor" → formulario con políticas y los 9 campos; (b) enviar solicitud → card de éxito 48h; (c) como admin (yeison/Losquiero7) → /admin/vendedores → Aprobar (llega correo con credenciales temporales) o Rechazar (con observación); (d) el vendedor al iniciar sesión con la temporal DEBE cambiar la contraseña (Swal) antes de usar la cuenta. Demo en BD: Pepe ya aprobado (vendedor prueba.vendedor@test.com), Pedro rechazado → probar el reenvío desde su cuenta.
2. (User) Probar en el navegador el timbre: abrir el panel NO marca todo como leído; marcar UN item solo ese; con notificaciones nuevas el contador vuelve a 1.
3. (User) Workbench: aplicar los nuevos porcentajes de retos (INSERT IGNORE no pisa la BD existente):
   `UPDATE RETOS SET RECOMPENSA_PORCENTAJE = CASE ID_RETO WHEN 1 THEN 5 WHEN 2 THEN 8 WHEN 3 THEN 10 WHEN 4 THEN 6 WHEN 5 THEN 3 WHEN 6 THEN 4 WHEN 7 THEN 5 WHEN 8 THEN 10 WHEN 9 THEN 5 WHEN 10 THEN 5 WHEN 11 THEN 8 WHEN 12 THEN 5 WHEN 13 THEN 6 WHEN 14 THEN 4 WHEN 15 THEN 6 WHEN 16 THEN 8 END WHERE ID_RETO BETWEEN 1 AND 16;`
4. (User) Probar en el navegador el reporte de avance con un video grande (>10 MB, incluso .mov de iPhone): dropzone arrastra el archivo, barra de progreso real, evidencia en /admin/retos.
5. (User) Probar favoritos (tarjetas compactas) y que los % de retos se vean nuevos en la portada de retos.
6. (User) Restart containers: `docker compose up -d` (aplica los mounts nuevos `./frontend/public/images/perfiles` y `./frontend/public/images/retos`) + `docker restart jadda_backend` (aplica `migrarTablasExistentes` → `ENVIOS.COSTO_ENVIO`, `DESCUENTOS.USADO`, tablas `RETO_EVIDENCIAS`/`NOTIFICACIONES`/`AVISOS_STOCK`/`DEVOLUCIONES` + retos 5-16, rutas nuevas); `docker restart jadda_frontend` si hay parse errors stale.
7. (User) Terminar app móvil: checkout y reseñas pendientes (carrito real, favoritos, catálogo con imágenes OK); probar en el teléfono el carrito rediseñado + sección "TE PUEDE GUSTAR" (favoritos → similares → al azar).
8. (User) Create `backend/.env.example` with placeholder values for git sharing (add ADMIN_EMAIL/ADMIN_PASSWORD, FRONTEND_URL).
9. (User) Remove legacy `jsonwebtoken` dependency, unused `verificarToken` middleware and dead `resenaController.js`.

## Critical Context
- `MYSQL_ROOT_PASSWORD: tu_password_secreto` in `docker-compose.yml` and `.env` — must match between backend and database container.
- `docker compose down -v` deletes the named `mysql_data` volume; on next `up`, setup.js recreates tables + seeds.
- Docker Vite dev server may show stale parse errors; `docker restart jadda_frontend` resolves.
- **Verificación frontend**: usar `npx tsc -b` (NO `tsc --noEmit` — el tsconfig raíz solo tiene referencias y no revisa nada). El Docker corre `pnpm build` = `tsc -b && vite build`.
- `addToCart(idProducto, idVariante, cantidad)` — second param is always a variant ID, never a quantity.
- FloatingCart and MiniCartMenu are rendered globally by `App.tsx`; individual pages should NOT import them.
- 49 RFs con estado verificado (2026-08-08): RF-001..RF-049; los RF-040..049 (retos, planes, métodos de pago, notificaciones, login social, foto de perfil, subir imágenes, newsletter, contacto, historial) y los RNF-012..015 (no-bloqueo correo, subidas, BD portátil, RBAC) documentados como nuevos. RF-023/029 → "Implementado parcialmente"; 39 HUs actualizadas (Implementadas). Sin pendientes de RF.
- Real stack: Express sessions + Passport.js (not JWT), `USUARIOS` table, `CONFIRMADO` field, `POST /api/auth/login`, Spanish error messages.
- Admin seed: `seedAdminUser()` corre en cada arranque (setup.js); si borras al admin en Workbench, se recrea al reiniciar el backend.
- Checkout seguro: el servidor recalcula subtotal/descuento/total; el decremento de stock por variante queda pendiente de habilitar tras pruebas (ver checkoutController.js).
- Checkout 2026-08-02: VALIDA stock por variante SIN decrementar (400 con mensaje por producto); costo envío calculado en servidor (`utils/envio.js`, gratis ≥ $200.000) y sumado al total; `ENVIOS.COSTO_ENVIO` se migra AUTOMÁTICAMENTE vía `migrarTablasExistentes()` en setup.js (ALTER idempotente al arrancar, verifica INFORMATION_SCHEMA) — ya no es necesario correr el ALTER a mano en Workbench.
- Foto de perfil: `POST /api/auth/foto` (base64, límite 10mb) guarda en `backend/uploads/perfiles/u{ID_USUARIO}/` → mount Docker `./frontend/public/images/perfiles:/app/uploads/perfiles` → URL `/images/perfiles/u{id}/...`; después se guarda la URL con `PUT /api/auth/perfil` (`foto_url`) y `refreshPerfil()` del AuthContext. Funciona SOLO en Docker (como la subida de imágenes de productos).
- Foto de perfil UX: el input file está FUERA del menú del avatar (el menú se desmontaba y el diálogo nunca abría) y se hace click con `setTimeout(() => fileInputRef.current?.click(), 50)`; PerfilEditar ya NO tiene input de URL (solo subida desde navegador) — la columna `FOTO_URL` se mantiene como almacenamiento de la ruta.
- Perfil (frontend): `Perfil.tsx` con hero pegado al navbar (padding-top 0 + body global), avatar con borde rojo clickeable → menú "Ver foto" (modal fullscreen) y "Cargar foto desde el navegador"; fecha con día (`{ weekday: "long" }`); PerfilEditar con sección "Información de tu cuenta" (correo, miembro desde con día, documento, botón "Cambiar contraseña" → `/perfil/seguridad`). Fallback avatar: ui-avatars.com.
- Carrito estático (2026-08-02): FloatingCart es un botón fijo `right:25px; bottom:25px` SIN drag (se eliminó todo el estado de posición `cartButtonX/Y/setCartButtonPos` de CartContext); MiniCartMenu posiciona sobre el botón fijo (`botonX = innerWidth - 65 - 25`, abre hacia la izquierda).
- Favoritos con deshacer (2026-08-02): al eliminar aparece barra fija bottom 90px right 20px con countdown de 5s (`fav-undo-bar`/`fav-undo-btn` en Favoritos.css); deshacer hace `POST /api/favoritos { id_producto }` + recarga. Icono de editar en DireccionesPerfil: `FaPencilAlt` (antes FaStar).
- Retos completos (2026-08-02): tabla `RETO_EVIDENCIAS` (ID_EVIDENCIA, ID_RETO_USUARIO, ID_USUARIO, TIPO imagen|video, RUTA, CANTIDAD, ESTADO pendiente|aprobado|rechazado, FECHA_SUBIDA); 16 retos seed (5-16 nuevos); `DESCUENTOS.USADO` (cupones de un solo uso). Flujo: reportar avance SIN material suma directo; CON material (imagen/video base64 ≤10mb → `backend/uploads/retos/r{id_reto_usuario}/`, mount `./frontend/public/images/retos:/app/uploads/retos`) queda PENDIENTE → admin aprueba/rechaza (`GET/POST /api/retos/admin/evidencias*`) → al aprobar suma CANTIDAD y si llega a META genera cupón `RETO-XXXX-XXXX` con 30 días de vigencia y USADO=0. Checkout: cupones `RETO-` ya usados (USADO=1) no aplican descuento y al aplicarse se marcan USADO=1 dentro de la transacción.
- Retos UI (2026-08-02): `Retos.tsx` con botón atrás, Swal de adjuntar material (file input, preConfirm base64, validación 10MB), mensaje "Deja que nuestros asesores revisen el material... hasta 24 horas", badge de avances en revisión, cupón mostrado con "un solo uso"; panel `AdminRetos.tsx` (`/admin/retos`, tab en AdminNavbar) con tabla de evidencias, modal para ver imagen/video, botones Aprobar/Rechazar.
- Navbar dropdown (2026-08-02): header del menú de usuario es `<Link to="/perfil">` con avatar (foto o ui-avatars), nombre y "Ver perfil" (antes "Miembro JADDA"); se quitaron "Mi Perfil" y "Mi Historial" (ruta `/historial` sigue existiendo).
- Páginas de perfil pegadas al navbar (2026-08-02): `body { padding-top: 65px }` es global (principal.css importado en main.tsx), así que las páginas ya no usan padding-top propio: Perfil/PerfilEditar/Seguridad/MisCompras/DireccionesPerfil/Favoritos con `padding-top: 0`.
- Mega-menú Catálogo compacto (2026-08-02): dropdown de 560px centrado bajo CATÁLOGO (antes full-width, tapaba la pantalla); izquierda = lista de categorías con hover activo (rojo), derecha = panel con hasta 4 productos de la categoría activa (categorías de productosMenu se filtran por `p.CATEGORIA === cat.NOMBRE_CATEGORIA`); pie "Ver todo el catálogo →".
- Footer reestructurado (2026-08-02): reescrito con grid CSS propio (sin Bootstrap cols), 4 columnas (Marca, Navegación, Atención al cliente, Síguenos con iconos react-icons), línea inferior de copyright. CSS `footer.css` reescrito completo.
- Newsletter del footer (2026-08-02): movida de barra roja superior a franja compacta abajo (antes de copyright), sin fondo rojo (borde sutil, input oscuro redondeado, botón outline).
- Recientemente vistos con descuento/stock (2026-08-02): `historialController.obtenerHistorial` ahora trae `ID_DESCUENTO`, `STOCK` (SUM de PRODUCTO_VARIANTES) e `ID_VARIANTE_POR_DEFECTO` (MIN) vía subqueries; entrada de localStorage en ProductDetailPage guarda los 3 campos nuevos; Principal enriquece cada item del historial con datos completos de `productos` y pasa `descuentoPorcentaje` + `onAgregarCarrito` a ProductCard (antes ni descuento ni badge de stock se mostraban).
- Envío: `GET /api/envio/calcular?departamento=&subtotal=` (tarifas por departamento, normaliza tildes); frontend ResumenCompra lo consulta al cambiar departamento (debounce 400ms).
- Checkout resumen (2026-08-02): el panel derecho muestra desglose completo — Precio base, Descuento de productos (si hay ID_DESCUENTO), Descuento cupón (si aplica), Envío y Total; `subtotalBase/descuentoProductos/subtotal` calculados por separado; row del checkout con `align-items: flex-start` (fix: la columna derecha ya no salta al cambiar método de pago — antes se estiraba con la izquierda).
- UX checkout + factura + correo (2026-08-08): (1) documento en ResumenCompra ya NO es obligatorio (se quitaron `*`, mensajes "Requerido" y bordes rojos; no se envía al backend); (2) panel resumen muestra SIEMPRE todas las filas: Precio base, Descuento de productos, **Subtotal** (`.subtotal-jadda` con borde punteado CSS), Descuento cupón, Envío y Total (antes los descuentos solo aparecían si existían y no había subtotal); (3) factura PDF con colores de página — franjas en azul marino `#002244` (antes `#111827`), logo `backend/assets/LogoJadda.png` (copia de `frontend/public/LogoJadda.png`, sincronizar a mano si cambia) en chip blanco redondeado, "JADDA" blanco + "SPORTS" rojo `#e63946`, frase **"Lo mejor en deportes"**; (4) `backend/utils/correo.js` nuevo helper `imagenComoDataUri(url)`: lee la imagen de `/app/uploads` (mount de `frontend/public/images/productos`) y la incrusta como base64 `data:image/...` en el email de checkout — las imágenes YA se ven en Gmail/Outlook/móvil (antes `http://localhost:5173/...` no cargaba en clientes externos); fallback a URL si el archivo no existe (dev fuera de Docker) + caché por ruta. Verificado: `node --check`, `npx tsc -b` y test en contenedor (PDF 324KB con logo, data URI OK). NOTA: las URLs de la BD coinciden con la extensión real (ej. Producto_01 usa `img_1.jpg`).
- Fixes 2da fase (2026-08-08): (1) dirección del checkout SÍ se guarda en DIRECCIONES: el botón "Siguiente" llama `guardarDireccionEnPerfil()` (PUT a la dirección seleccionada/principal o POST nueva si no existe) antes de ir a pago — antes solo se guardaba con el botón "Guardar dirección" explícito; (2) factura PDF: logo centrado dentro del chip blanco (y=15, antes 39 que se salía), título "FACTURA DE COMPRA" (16pt para caber en 150px) + `doc.info.Title`, separadores de la tabla de productos (y-9→y-4) y de totales (y-8→y-2) ya no se montan sobre el texto; (3) asunto del correo de checkout: "¡Gracias {nombre} por tu compra! Aquí está tu factura" (antes "🧾 Factura #N") + adjunto `factura-compra-{id}.pdf`.
- Foto de perfil DAÑADA FIX (2026-08-08): causa raíz — Docker Desktop no propaga carpetas/archivos NUEVOS al contenedor de Vite al instante (el `sirv` de Vite cachea el listado de `public/`), así que la foto recién subida devolvía el `index.html` del SPA (200 text/html) → img "dañada" y navbar sin foto. Solución: (1) `backend/server.js` sirve `/images/perfiles` con `express.static` desde `backend/uploads/perfiles` (SIEMPRE al día, sin depender de la sincronía del mount); (2) `frontend/vite.config.ts` proxy `/images/perfiles` → `http://backend:5000` (como `/api`); (3) `authController.subirFotoPerfil` ahora guarda ARCHIVOS PLANOS `perfil-{id}-{ts}.{ext}` en la raíz de `perfiles/` (no subcarpeta `u{id}` — también evita carpetas nuevas). Las fotos viejas en `u2/u7/` siguen sirviéndose (static recursivo). E2E: login admin → POST foto → PUT foto_url (-X PUT) → GET por el frontend 200 image/jpeg INMEDIATO. Advertencia: al cambiar vite.config se usan los `hostnames` Docker (`backend:5000`), funciona con proxy; prueba de movil usa el mismo proxy.
- Estado de envío admin (2026-08-02): `PUT /api/admin/compras/:id/envio` actualiza `ENVIOS.ESTADO_ENVIO` (PENDIENTE, POR_EMPAQUETAR, EMPACADO, EN_CAMINO, ENTREGADO, CANCELADO); AdminOrdenes tiene columna "Envío" con select + badge de estado (colSpan del detalle: 9).
- MiniCartMenu: botón del footer dice "Comprar" (antes "Ver carrito").
- Cancelar pedido: `POST /api/compras/:id/cancelar` (solo COMPLETADA/PENDIENTE → CANCELADA + ESTADO_ENVIO CANCELADO); botón en MisCompras.
- Dirección de pedido: `PUT /api/compras/:id/direccion` actualiza ENVIOS en servidor; MisCompras ya NO usa sessionStorage (`jadda_direcciones_edit` eliminado).
- CompraExitosa: si se refresca (sin location.state) recupera `GET /api/compras/:id` (trae TOTAL, REFERENCIA_PAGO, productos, planGenerado).
- Rating en cards: `GET /api/productos` incluye RATING (AVG RESENAS) y RESENA_COUNT vía subconsultas (no rompen SUM de stock); ProductCard y Catalogo muestran estrellas; badges "AGOTADO"/"¡Solo quedan X!" (umbral 10) según STOCK.
- Planes: `dias_completados` persistidos en `backend/data/planes-progreso.json` (id_plan → array); `misPlanes` devuelve DIAS_COMPLETADOS; archivo `backend/data/` se crea automáticamente (mkdirSync recursive).
- Newsletter: `POST /api/newsletter` persiste en `backend/data/newsletter.json`; input en Footer.
- Otros fixes: Navbar mega-menú usa `?cat=` (antes `?categoria=`); AuthModal sin delay artificial de 2s; guía de tallas modal (zapatos + ropa) en ProductDetailPage si el atributo parece talla; sección "Recientemente vistos" en Principal (API si hay sesión, localStorage si no); ResumenCompra muestra "Solo quedan X" por item y errores de checkout con Swal (antes alert); bug `selectClass(numeroDocumento)` → `selectClass(tipoDocumento)` corregido.
- Fixes 2026-08-02 tarde: (1) `actualizarPerfil` (authController) ahora actualiza SOLO los campos presentes en el body (antes ponía NULL en todos, rompiendo al subir foto — "Column 'NOMBRE_USUARIO' cannot be null"); (2) ResumenCompra: `descuentosMap/costoEnvio/envioCargando` movidos ANTES del reduce de `subtotal` (TDZ → "Cannot access before initialization"); (3) Navbar: timers separados `megaTimer`/`ofertasTimer` + cada menú cierra al otro al abrir (antes al pasar de CATÁLOGO a OFERTAS se cancelaba el timer y el menú de catálogo quedaba abierto).
- Rate limiting en memoria (rateLimiter.js): se reinicia si el contenedor backend se reinicia; suficiente para dev, no para multi-instancia en producción.
- UX verificación 2026-08-08: `AuthModal.tsx` — link siempre visible "¿No verificaste tu correo? Reenviar código" bajo ENTRAR; `volverOrigen` ('register'|'login') para el botón "VOLVER" de verify; cuenta regresiva en tiempo real "El código expira en X min Y s" (localStorage `token_expira_<email>` = 15 min, alineado con `TOKEN_EXPIRA` del backend) en verify Y en forgot-code; cooldown de reenvío = **100 s** en los 4 flujos (registro, reenvío login, reenviar verify, reenviar forgot). Anti-spam multi-capa: 100s UI + `MAX_REINTENTOS=3/30min` por correo + rateLimiter 5/15min por IP + 404 sin envío si no existe.
- Docs verificados y ampliados (2026-08-08): los 49 RFs revisados contra el código real — corregidos los estados de RF-015 (era "Ni implementado"), RF-025/029/031/032/033/034/035/038/039 (estaban "No implementado" pero SÍ implementados); RF-023 → "Implementado parcialmente" (cancelar ya no menciona reembolso ni liberación de stock; solo cambia estados + notifica); RF-029 → "Implementado parcialmente" (sin MOVIMIENTOS_STOCK); RF-007/008 actualizados (filtros y orden son client-side en `Catalogo.tsx`, el backend solo `search` prefix); RF-018 endpoint real `/api/checkout/procesar`; RF-020 proceso real (Nodemailer síncrono con factura adjunta, sin webhook/Celery). **10 RFs nuevos** (RF-040..049): retos deportivos, plan entrenamiento, métodos de pago perfil, notificaciones in-app, login social, foto perfil, subir imágenes admin, newsletter, contacto, historial navegación. **4 RNFs nuevos** (RNF-012..015): no-bloqueo correo, manejo de subidas, BD portátil, RBAC. RNF-002/003/004 → "Implementado parcialmente" con notas exactas de lo que falta. `docs/README.md` actualizado (49 RF, 15 RNF + nota de verificación).
- Rate limiting en memoria (rateLimiter.js): se reinicia si el contenedor backend se reinicia; suficiente para dev, no para multi-instancia en producción.
- Flujo "cuenta sin verificar" (2026-08-08): `authController.js` — login valida bcrypt ANTES de revisar `CONFIRMADO` (401 genérico "Correo o contraseña incorrectos" evita enumeración de correos; solo con contraseña correcta devuelve 403 `{ message: "Debes verificar tu correo", requiereVerificacion: true }`); `reenviar-codigo` devuelve 404 sin enviar si el correo no existe (anti email-bombing; rate limiter 5/15min ya aplicado). Frontend `AuthModal.tsx`: al recibir 403 con `requiereVerificacion` muestra el bloque `.verify-prompt` ("PRIMERO DEBES VERIFICAR TU CUENTA" + "¿Quieres que te enviemos al correo otro código?") con botón "SÍ, ENVIAR CÓDIGO" → `POST /api/auth/reenviar-codigo` + timer 60s (localStorage) + `setView("verify")`, y link "No, luego" (oculta el aviso). Auto-login post-verificación usa `verifyEmail`/`passwordLogin` (no `form.email` — vacío en vista login). Al escribir el correo o cambiar de vista el aviso se limpia. E2E probado: registro → login malo 401 → login bueno 403+flag → reenviar 200 → confirmar 400 código malo / 200 bueno → login 200. Usuario de prueba `prueba.noverif.*@gmail.com` (ID 4) pendiente de borrar en Workbench por el usuario.
- Rate limiting en memoria (rateLimiter.js): se reinicia si el contenedor backend se reinicia; suficiente para dev, no para multi-instancia en producción.
- Todos los RFs implementados (2026-08-06): RF-027 (CRUD categorías admin), RF-033 (devoluciones) y RF-038 (recomendaciones) fueron los últimos en cerrarse. RF-020 (email confirmación pedido), RF-021 (factura PDF), RF-023 (cancelar pedido), RF-025 (notificaciones de estado), RF-030/031 (admin pedidos), RF-032 (reportes de ventas), RF-034 (más vendidos), RF-035 (aviso stock) y RF-037 (compartir redes) ya estaban implementados.
- Imágenes de producto: 44 carpetas `Producto_01..44` en `frontend/public/images/productos/` (3 imágenes c/u, `img_K.ext`, K = ORDEN en BD); subidas del admin van a `frontend/public/images/productos/subidas/`. Mapeo: carpeta `Producto_NN` ↔ `ID_PRODUCTO NN`. Producto 45 no existe (eliminado).
- Subida de imágenes: el backend escribe en `/app/uploads/subidas` (mount de `./frontend/public/images/productos`) — funciona SOLO en Docker (fuera de Docker esa carpeta no existe). Body JSON ≤ 25mb.
- FRONTEND_URL (backend env, default `http://localhost:5173`) se usa en el correo de checkout y en el aviso de stock (imagenParaCorreo en `utils/correo.js`); la app móvil usa `EXPO_PUBLIC_FRONT_URL` (default `http://10.2.178.124:5173`).
- Verificación final frontend tras este cambio: `npx tsc -b` + `npx vite build` OK; backend con `node --check` OK en todos los archivos tocados.

## Relevant Files
- `backend/database/setup.js`: full DB schema (33 tablas: 32 en CREATE_TABLES_RAW + DEVOLUCIONES por migración) + seed data (imágenes locales, sin producto 45) + `MIGRACIONES` (ALTERs/createSql idempotentes: ENVIOS.COSTO_ENVIO, DESCUENTOS.USADO, RETO_EVIDENCIAS, NOTIFICACIONES, AVISOS_STOCK, DEVOLUCIONES, índice único `uq_categoria_nombre` vía INFORMATION_SCHEMA.STATISTICS).
- `backend/database/schema.sql` + `backend/database/exportarSchema.js`: archivo SQL único para Workbench (BD nuevas), regenerado automáticamente desde setup.js (`node backend/database/exportarSchema.js` tras cada cambio de esquema).
- `backend/controllers/devolucionController.js` + `backend/routes/devolucionRoutes.js`: solicitudes de devolución cliente + procesamiento admin con reingreso de stock (RF-033).
- `frontend/src/admin/AdminCategorias.tsx` (`/admin/categorias`): CRUD de categorías (RF-027).
- `frontend/src/admin/AdminDevoluciones.tsx` (`/admin/devoluciones`): revisión y aprobación/rechazo de devoluciones (RF-033).
- `backend/utils/correo.js`: `imagenParaCorreo()` compartido entre checkout y aviso de stock.
- `backend/utils/facturaPdf.js`: generador de factura PDF con pdfkit (RF-021).
- `backend/utils/estadoPedido.js`: `notificarCambioEstado()` email + notificación in-app al cambiar estado pedido/envío (RF-025).
- `backend/controllers/productoController.js`: CRUD productos/variantes/características/reseñas + avisos de stock (RF-035).
- `backend/database/schema.sql`: único archivo SQL — schema completo (33 tablas) + seeds (imágenes locales, sin producto 45), para BD nuevas (importable en MySQL Workbench; generado desde setup.js con exportarSchema.js).
- `backend/controllers/imagenController.js` + `POST /api/productos/imagenes`: subida de imágenes base64 desde el panel admin.
- `frontend/src/admin/SubirImagenes.tsx`: componente de subida de imágenes reutilizable (admin).
- `docs/README.md`: docs index with state table and stack.
- `docs/plan de trabajo.md`: project phases, module status, cronograma.
- `docs/Restricciones del Proyecto.md`: stack, security, dev workflow constraints.
- `docs/diagrams/arquitectura.md`: system architecture + auth/checkout sequence diagrams.
- `docs/diagrams/base-de-datos.md`: ERD + full table structure.
- `docs/diagrams/flujos.md`: registration, purchase, admin, and navigation flow diagrams.
- `docs/RFs/RF-001.md` through `docs/RFs/RF-049.md`: 49 RFs (implementados/parciales, verificación 2026-08-08).
- `docs/HUs/HU-001.md` through `docs/HUs/HU-039.md`: 39 HUs actualizadas (Implementadas).
- `docs/RNFs/RNF-001.md` through `docs/RNFs/RNF-015.md`: 15 RNFs restructured (7 original modules extracted + 4 new + 4 new 2026-08-08).
- `backend/utils/envio.js`: tarifas de envío por departamento (envío gratis ≥ $200.000).
- `backend/controllers/envioController.js` + `backend/routes/envioRoutes.js`: `GET /api/envio/calcular`.
- `backend/controllers/newsletterController.js` + `backend/routes/newsletterRoutes.js`: `POST /api/newsletter` (persiste en `backend/data/newsletter.json`).
- `backend/controllers/comprasController.js`: `obtenerCompras`, `obtenerCompraPorId`, `cancelarCompra`, `actualizarDireccionCompra`.
- `backend/controllers/vendedorController.js` + `backend/routes/vendedorRoutes.js` (montadas en `/api/vendedor`): solicitud de vendedor (POST/GET /solicitud), listado admin (GET /admin) y procesamiento (POST /admin/:id/procesar) con creación de usuario vendedor + credenciales temporales.
- `backend/utils/numeroPedido.js` + `frontend/src/utils/numeroPedido.ts` + `movil/utils/numeroPedido.ts`: número de pedido estable de 8 dígitos (hash de Knuth sobre ID_VENTA, sin columnas).
- `frontend/src/pages/DevolverPedido.tsx` + `frontend/src/css/DevolverPedido.css` (`/perfil/devolver/:idVenta`): solicitud de devolución/reembolso con productos, tipo, motivo, descripción y evidencias (fotos/videos).
- `frontend/src/pages/DevolucionEstado.tsx` + `frontend/src/css/DevolucionEstado.css` (`/perfil/devolucion/:idVenta`): estado de las solicitudes de devolución/reembolso de un pedido (timeline, evidencias, observación, adjuntar evidencias en MAS_PRUEBAS).
- `backend/controllers/devolucionController.js` + `backend/routes/devolucionRoutes.js`: solicitudes multi-producto con evidencias + procesar con 4 decisiones (devolver/reembolsar/mas_pruebas/rechazar) + subida/adjunte/eliminación de evidencias.
- `frontend/src/admin/AdminDevoluciones.tsx` (`/admin/devoluciones`): revisión con galería de evidencias y 4 decisiones (con observación en más pruebas/rechazo).
- `frontend/src/pages/SerVendedor.tsx` + `frontend/src/css/SerVendedor.css` (`/ser-vendedor`): formulario con políticas, estados de solicitud (APROBADA/RECHAZADA/reenvío).
- `frontend/src/pages/MisCompras.tsx` + `frontend/src/css/MisCompras.css` (`/perfil/compras`): tarjetas compactas en grid de 2 columnas (imagen 72px + badge +N, badge estadoVisible, meta con fecha/artículos/método + total rojo, nombres de producto, "Ver mi compra" → página detalle), centrado max-width 1200px, buscador (pedido/producto/método) y filtro por fecha; acciones: Pedir reembolso (Swal → `/perfil/devolver/:idVenta`), Ver estado de reembolso, Solicitar reembolso (CANCELADA), Cancelar pedido, Factura (descarga blob), Volver a comprar (Swal de confirmación → solo agrega al carrito con ID_VARIANTE, sin navegar).
- `frontend/src/pages/DetalleCompra.tsx` + `frontend/src/css/DetalleCompra.css` (`/perfil/compra/:id`): página de detalle de compra — hero (fecha + "Entregado el…", TOTAL PAGADO, método), timeline 5 pasos de envío (verde completo en ENTREGADO), dirección con edición inline (PUT `/api/compras/:id/direccion`, bloqueada si el envío salió: EN_CAMINO/ENTREGADO con aviso `.dc-dir-bloqueada`), productos con chips de variante + botón Opinar → `/producto/:id`, totales (subtotal/envío/TOTAL), pago (método/referencia/DATOS_PAGO), sección reembolso según estado, Factura PDF (descarga blob).
- `frontend/src/utils/estadoCompra.ts`: util compartida — `DIAS_DEVOLUCION=3`, `DEVOLUCION_ACTIVA=/SOLICITADA|APROBADA|MAS_PRUEBAS/`, `tieneDevolucionActiva`, `estadoVisible`, `estadoEnvioTexto`, `puedeDevolver`, `devolucionVencida`.
- `frontend/src/admin/AdminVendedores.tsx` (`/admin/vendedores`): revisión y aprobación/rechazo de solicitudes de vendedor.
- `frontend/src/admin/AdminUsuarios.tsx` (`/admin/usuarios`): tabla de usuarios sin scroll horizontal (5 columnas: Usuario/Nombre/Correo/Rol/Fecha + "Ver usuario") + modal `au-modal` de detalle (hero con stats, información personal, direcciones, métodos de pago).
- `backend/controllers/adminController.js` + `backend/routes/adminRoutes.js`: `obtenerUsuarioDetalle` (`GET /api/admin/usuarios/:id` → `{ usuario, direcciones, metodos, stats, vendedor }` — tabla real `USUARIOS_METODOS_PAGO` PK `ID`; con rol 6 busca en VENDEDORES y calcula stats de vendedor: productosPublicados/unidadesVendidas/totalVentas/totalIngresos excluyendo CANCELADAS, compras en 0) y `eliminarCompra` (`DELETE /api/admin/compras/:id`, solo CANCELADA, transacción: DEVOLUCIONES → DETALLE_VENTAS → ENVIOS → PLANES → VENTAS).
- `frontend/src/admin/AdminOrdenes.tsx`: botón "Eliminar registro" (`.ao-btn-eliminar`) solo en órdenes CANCELADAS.
- `frontend/src/context/AuthContext.tsx`: `forzarCambioPassword` (Swal obligatorio cuando DEBE_CAMBIAR_PASSWORD=1).
